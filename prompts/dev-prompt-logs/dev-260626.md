
dev-260626 汇总

（dev-260626分支派生于dev-260625，因dev-260625暂时不放心合并到main，所以这么操作）

--------------------------------

问题1： 
    见 dev-260626-NETWORK-FIX.md

--------------------------------

问：

 - 【看起来好像有采纳，但是实现效果不理想，点击强拉后不久看到进入进行中，但是过了不久就被下沉到等待队列】点击强制拉取缓存, 应该尽可能提升所选文件的传输优先级
 - 苹果手机在微信内置浏览器、opera均无法听到对讲机喊话
 - 苹果手机opera打开富文本看不到所发的图片，取而代之的是一堆类似ASCII的乱码
 - 磁链缓存列表支持填写完整磁链或磁链ID以加载文件，且input框应与加载按钮放在同一行。点击”加载文件“按钮后，先检测磁链有效性，如果有效，就在本行与下方的缓存列表夹层塞一个iframe以加载”磁链下载“页开始下载（这个iframe页右上角有并排两个按钮[-]和[x]，用来隐藏或关闭"磁链下载"页）。
 - 传输中的进度UI组件，改为默认为缩小状态 
 - 检查是不是两台设备之间发新发一条数据（纯文本、富文本）时，如果两者存在已经传输中或卡死的别的进程，是不是因此就传输新的要延迟很久。目前测试发现看起来有这种情况。再切换到一个双方设备都已经完全同步好数据的隧道中，新发一条纯文本/富文本，就相对较快同步好了
 - 是不是没有断点传输功能？大文件快传完了，刷新页面、断开连接、突然不在线等情况就要重新来
 - 检查什么情况下会提示“磁链生成失败: File asset is not registered online”，可能不是bug也可能是。因为我测试时，正好只有拥有某个文件的本机唯一在线，是不是因此就有这个提示？但是不要忘记如果本机有这个文件的缓存，那就能做种子
 
 dev-260626-NETWORK-FIX-NEWCODE-STEP1-DEBUG
 
本次修改的需求以及改动的明细要分门别类整理好章节，存放到项目的docs目录中，具体文件路径是 docs/devlog/dev-260628-features.md （请用尾行追加的方式写入）
请切换到分支"dev-260626-NETWORK-FIX-NEWCODE-STEP2"进行此需求的代码改动，但不要暂存，也不要提交。



答：

我先只读检查当前分支、和 3fa9439... 版本相比的传输代码差异，以及文件传输/P2P/磁链/进度 UI 相关入口。当前沙箱是只读，真正切分支和改文件时需要提权执行。




已运行 3 条命令


现在已经在目标分支 dev-260626-NETWORK-FIX-NEWCODE-STEP2，工作区干净。和 3fa9439... 相比，当前有效差异仍集中在进度 UI 和完整文件下行并发，传输链路主体没有明显改动。接下来我查慢传输的具体瓶颈点。




已运行 3 条命令


慢传输这里我看到一个更像瓶颈的点：P2P DataChannel 每发到 512KB 缓冲就等 bufferedamountlow，如果浏览器事件不及时，就走 1 秒 fallback。20MB 文件大约会触发几十次等待，两个文件并发时就可能被拖到分钟级。这个比“P2P 是否连上”更像当前同 WiFi 慢的原因。




已运行 4 条命令


强制拉取优先级和断点传输都在同一个模块里：当前 force 会取消并重拉，但没有明确插队；断点缓存只标记中断，不保存已收到分片，所以刷新/断线后确实基本要重来。短期先做“强制拉取插队 + P2P 缓冲等待修复”，断点续传需要较大结构改造。




已运行 4 条命令


磁链部分也确认了：downloadList.html 已有输入框，但现在提交后直接整页跳转 /downloader；你的新需求是先校验磁链，再在列表页中间插入 iframe 下载面板。这个可以独立改，不影响主传输逻辑。




已运行 3 条命令


我准备改的范围会控制住：client/file-assets.js 只改 P2P 发送缓冲等待和强制拉取插队，不改链路选择策略；app.js/index.html/downloadList.html/server.js 做你列出的 UI/磁链/进度默认收起/本机可做种修复；并追加 devlog。由于当前环境是只读，下一步需要提权写文件。




已运行 8 条命令


iPhone 对讲机问题大概率是移动浏览器自动播放策略：远端音轨到了，但 audio.play() 被拦截或没有真正解锁。当前代码只在 play 抛错时显示启用声音按钮，我会改成远端音频一到就显式提供“启用声音”按钮，并确保 audio 非 muted、音量为 1。




已运行 Get-Content app.js | Select-Object -Skip 1320 -First 270







富文本图片乱码不像是富文本本身的问题，当前富文本图片应通过 Blob + objectURL 渲染。iOS Opera 看到 ASCII 乱码，通常是预览/iframe 把二进制当文本打开，或文件 MIME/Blob URL 降级路径走错。等写入时我会先加更强的 MIME 检查和图片二进制 Blob 渲染保护。




已运行 2 条命令


磁链生成失败的原因也很明确：HTTP /api/magnets 只看服务端是否已经注册了该文件 asset。即使本机有完整缓存，如果 socket 的 file-asset-available 还没被服务端处理，或者之前服务端没记录，就会报 File asset is not registered online。我会让分享磁链时把本机文件元数据一起带给服务端，服务端确认该设备在线后可直接把它注册为种子。




已运行 2 条命令


现在执行写入修改：会保持当前分支，不暂存不提交。因为环境只读，我用一次提权脚本完成文件修改。




已运行 $script = @'
const fs = require('fs');
const path = require('path');

function read(file) { return fs.readFileSync(file, 'utf8'); }
function write(file, text) { fs.writeFileSync(file, text, 'utf8'); }
function replaceOnce(text, search, replacement, label) {
  if (!text.includes(search)) throw new Error(`Missing pattern: ${label}`);
  return text.replace(search, replacement);
}
function ensureDir(dir) { fs.mkdirSync(dir, { recursive: true }); }

// client/file-assets.js
{
  const file = 'client/file-assets.js';
  let text = read(file);
  text = replaceOnce(text,
`    const CHUNK_SIZE = 64 * 1024;
    const BUFFER_LIMIT = 512 * 1024;
    const P2P_TIMEOUT = 1500;`,
`    const RELAY_CHUNK_SIZE = 64 * 1024;
    const P2P_CHUNK_SIZE = 128 * 1024;
    const BUFFER_LIMIT = 4 * 1024 * 1024;
    const BUFFER_LOW_WATER = 1 * 1024 * 1024;
    const BUFFER_POLL_MS = 20;
    const BUFFER_WAIT_TIMEOUT = 5000;
    const P2P_TIMEOUT = 1500;`,
    'file asset buffer constants');
  text = replaceOnce(text,
`            this.downloadQueue = [];
            this.activeDownloads = new Set();`,
`            this.downloadQueue = [];
            this.priorityDownloads = new Set();
            this.activeDownloads = new Set();`,
    'priorityDownloads constructor');
  text = replaceOnce(text,
`            if (options.force) {
                this.cancel(assetId);
                this.forceRequests.set(assetId, `${Date.now()}-${Math.random().toString(36).slice(2, 8)}`);
            }
            this.cancelledAssets.delete(assetId);`,
`            if (options.force) {
                this.cancel(assetId);
                this.forceRequests.set(assetId, `${Date.now()}-${Math.random().toString(36).slice(2, 8)}`);
            }
            if (options.force || options.priority) this.priorityDownloads.add(assetId);
            this.cancelledAssets.delete(assetId);`,
    'force priority request');
  text = replaceOnce(text,
`                this.desiredAssets.delete(assetId);
                this.requestedMetadata.delete(assetId);
                this.forceRequests.delete(assetId);
                this.log('request-skipped-local-cache', { assetId, size: this.dataSize(local.data) });`,
`                this.desiredAssets.delete(assetId);
                this.requestedMetadata.delete(assetId);
                this.forceRequests.delete(assetId);
                this.priorityDownloads.delete(assetId);
                this.log('request-skipped-local-cache', { assetId, size: this.dataSize(local.data) });`,
    'local cache priority cleanup');
  text = replaceOnce(text,
`        enqueueDownload(assetId) {
            if (!this.desiredAssets.has(assetId) || this.activeDownloads.has(assetId) || this.downloadQueue.includes(assetId)) return;
            this.downloadQueue.push(assetId);
            this.deps.onQueue?.(assetId, this.downloadQueue.length, this.activeDownloads.size);
            this.log('download-queued', { assetId, queueLength: this.downloadQueue.length, activeDownloads: this.activeDownloads.size });
            this.dispatchDownloads();
        }`,
`        enqueueDownload(assetId) {
            if (!this.desiredAssets.has(assetId) || this.activeDownloads.has(assetId) || this.downloadQueue.includes(assetId)) return;
            if (this.priorityDownloads.has(assetId)) this.downloadQueue.unshift(assetId);
            else this.downloadQueue.push(assetId);
            this.deps.onQueue?.(assetId, this.downloadQueue.length, this.activeDownloads.size);
            this.log('download-queued', {
                assetId,
                queueLength: this.downloadQueue.length,
                activeDownloads: this.activeDownloads.size,
                priority: this.priorityDownloads.has(assetId)
            });
            this.dispatchDownloads();
        }`,
    'priority enqueue');
  text = replaceOnce(text,
`        downloadPriority(assetId) {
            const size = this.assetSize(assetId);`,
`        downloadPriority(assetId) {
            if (this.priorityDownloads.has(assetId)) return -1;
            const size = this.assetSize(assetId);`,
    'priority scoring');
  text = replaceOnce(text,
`            this.desiredAssets.delete(assetId);
            this.requestedMetadata.delete(assetId);
            this.forceRequests.delete(assetId);
            this.retryCounts.delete(assetId);`,
`            this.desiredAssets.delete(assetId);
            this.requestedMetadata.delete(assetId);
            this.forceRequests.delete(assetId);
            this.priorityDownloads.delete(assetId);
            this.retryCounts.delete(assetId);`,
    'multi-source complete priority cleanup');
  text = replaceOnce(text,
`                this.desiredAssets.delete(assetId);
                this.requestedMetadata.delete(assetId);
                this.forceRequests.delete(assetId);
                this.markInterruptedAsset(assetId, interruptedAsset, reason);`,
`                this.desiredAssets.delete(assetId);
                this.requestedMetadata.delete(assetId);
                this.forceRequests.delete(assetId);
                this.priorityDownloads.delete(assetId);
                this.markInterruptedAsset(assetId, interruptedAsset, reason);`,
    'range retry exhausted priority cleanup');
  text = replaceOnce(text,
`        async waitForBuffer(channel) {
            if (channel.bufferedAmount <= BUFFER_LIMIT) return;
            await new Promise(resolve => {
                const timer = setTimeout(resolve, 1000);
                channel.bufferedAmountLowThreshold = BUFFER_LIMIT / 2;
                channel.addEventListener('bufferedamountlow', () => { clearTimeout(timer); resolve(); }, { once: true });
            });
        }`,
`        async waitForBuffer(channel) {
            if (channel.readyState !== 'open') throw new Error('File asset channel closed');
            if (channel.bufferedAmount <= BUFFER_LIMIT) return;
            channel.bufferedAmountLowThreshold = BUFFER_LOW_WATER;
            const startedAt = Date.now();
            while (channel.readyState === 'open' && channel.bufferedAmount > BUFFER_LOW_WATER) {
                await new Promise(resolve => {
                    let settled = false;
                    const finish = () => {
                        if (settled) return;
                        settled = true;
                        clearTimeout(timer);
                        channel.removeEventListener('bufferedamountlow', finish);
                        resolve();
                    };
                    const timer = setTimeout(finish, BUFFER_POLL_MS);
                    channel.addEventListener('bufferedamountlow', finish, { once: true });
                });
                if (Date.now() - startedAt > BUFFER_WAIT_TIMEOUT && channel.bufferedAmount > BUFFER_LIMIT * 2) {
                    throw new Error('File asset channel backpressure timeout');
                }
            }
            if (channel.readyState !== 'open') throw new Error('File asset channel closed');
        }`,
    'fast buffer wait');
  text = replaceOnce(text,
`            for (let offset = rangeStart; offset < rangeEnd; offset += CHUNK_SIZE) {
                if (this.cancelledAssets.has(asset.id)) throw new Error('File asset transfer cancelled');
                if (channel.readyState !== 'open') throw new Error('File asset channel closed');
                await this.waitForBuffer(channel);
                channel.send(this.sliceData(asset.data, offset, Math.min(offset + CHUNK_SIZE, rangeEnd)));`,
`            for (let offset = rangeStart; offset < rangeEnd; offset += P2P_CHUNK_SIZE) {
                if (this.cancelledAssets.has(asset.id)) throw new Error('File asset transfer cancelled');
                if (channel.readyState !== 'open') throw new Error('File asset channel closed');
                await this.waitForBuffer(channel);
                channel.send(this.sliceData(asset.data, offset, Math.min(offset + P2P_CHUNK_SIZE, rangeEnd)));`,
    'p2p chunk size');
  text = replaceOnce(text,
`            for (let offset = rangeStart; offset < rangeEnd; offset += CHUNK_SIZE) {
                if (this.cancelledAssets.has(asset.id)) throw new Error('File asset transfer cancelled');
                socket.emit('file-asset-relay-chunk', {
                    sessionId: this.deps.getSessionId(), to: deviceId, assetId: asset.id, transferId: transfer?.transferId,
                    chunk: this.sliceData(asset.data, offset, Math.min(offset + CHUNK_SIZE, rangeEnd))`,
`            for (let offset = rangeStart; offset < rangeEnd; offset += RELAY_CHUNK_SIZE) {
                if (this.cancelledAssets.has(asset.id)) throw new Error('File asset transfer cancelled');
                socket.emit('file-asset-relay-chunk', {
                    sessionId: this.deps.getSessionId(), to: deviceId, assetId: asset.id, transferId: transfer?.transferId,
                    chunk: this.sliceData(asset.data, offset, Math.min(offset + RELAY_CHUNK_SIZE, rangeEnd))`,
    'relay chunk size');
  text = replaceOnce(text,
`            this.desiredAssets.delete(assetId);
            this.requestedMetadata.delete(assetId);
            this.forceRequests.delete(assetId);
            this.retryCounts.delete(assetId);`,
`            this.desiredAssets.delete(assetId);
            this.requestedMetadata.delete(assetId);
            this.forceRequests.delete(assetId);
            this.priorityDownloads.delete(assetId);
            this.retryCounts.delete(assetId);`,
    'complete priority cleanup');
  text = replaceOnce(text,
`            this.desiredAssets.delete(assetId);
            this.requestedMetadata.delete(assetId);
            this.forceRequests.delete(assetId);
            this.stopMultiSourceWatchdog(this.multiSourceTransfers.get(assetId));`,
`            this.desiredAssets.delete(assetId);
            this.requestedMetadata.delete(assetId);
            this.forceRequests.delete(assetId);
            this.priorityDownloads.delete(assetId);
            this.stopMultiSourceWatchdog(this.multiSourceTransfers.get(assetId));`,
    'cancel priority cleanup');
  text = replaceOnce(text,
`                this.desiredAssets.delete(assetId);
                this.requestedMetadata.delete(assetId);
                this.deps.onUnavailable(assetId, 'transfer-interrupted');`,
`                this.desiredAssets.delete(assetId);
                this.requestedMetadata.delete(assetId);
                this.priorityDownloads.delete(assetId);
                this.deps.onUnavailable(assetId, 'transfer-interrupted');`,
    'retry exhausted priority cleanup');
  write(file, text);
}

// app.js
{
  const file = 'app.js';
  let text = read(file);
  text = replaceOnce(text, `let progressDrawerCollapsed = false;`, `let progressDrawerCollapsed = true;`, 'default collapsed progress drawer');
  text = replaceOnce(text,
`const progressHideTimers = new Map();
const fileTransferProgressStates = new Map();`,
`const progressHideTimers = new Map();
const progressUiLastPaint = new Map();
const fileTransferProgressStates = new Map();
const PROGRESS_UI_MIN_INTERVAL = 120;`,
    'progress ui throttle globals');
  text = replaceOnce(text,
`            showProgress(progressKey, fileName, progress, status, { route });
            if (terminal) {`,
`            const now = Date.now();
            const lastPaintAt = progressUiLastPaint.get(progressKey) || 0;
            const shouldPaintProgress = terminal || progress === 0 ||
                now - lastPaintAt >= PROGRESS_UI_MIN_INTERVAL ||
                !document.getElementById(progressElementId(progressKey));
            if (shouldPaintProgress) {
                showProgress(progressKey, fileName, progress, status, { route });
                progressUiLastPaint.set(progressKey, now);
            }
            if (terminal) {`,
    'progress ui throttle');
  text = replaceOnce(text,
`    activeFileProgress.delete(fileId);
    const timer = progressHideTimers.get(fileId);`,
`    activeFileProgress.delete(fileId);
    progressUiLastPaint.delete(fileId);
    const timer = progressHideTimers.get(fileId);`,
    'hide progress ui cleanup');
  text = replaceOnce(text,
`                fileId: fileInfo.id
            })`,
`                fileId: fileInfo.id,
                deviceId: state.deviceId,
                asset: {
                    id: fileInfo.id,
                    name: fileInfo.name || storedFile.name || 'file',
                    type: fileInfo.type || storedFile.type || 'application/octet-stream',
                    size: Number(fileInfo.size || storedFile.size || getBinaryDataSize(storedFile.data)),
                    ownerDeviceId: storedFile.ownerDeviceId || fileInfo.ownerDeviceId || state.deviceId,
                    isFolderArchive: fileInfo.isFolderArchive === true || storedFile.isFolderArchive === true,
                    isDirectoryMirror: fileInfo.isDirectoryMirror === true || storedFile.isDirectoryMirror === true,
                    folderName: fileInfo.folderName || storedFile.folderName,
                    entryCount: Number.isInteger(fileInfo.entryCount) ? fileInfo.entryCount : storedFile.entryCount
                }
            })`,
    'magnet post asset metadata');
  text = replaceOnce(text,
`function showRemoteAudioUnlockButton(reason = '') {`,
`function shouldShowPersistentAudioUnlock() {
    return /iPhone|iPad|iPod|MicroMessenger|OPR\//i.test(navigator.userAgent || '');
}

function showRemoteAudioUnlockButton(reason = '') {`,
    'persistent audio helper');
  text = replaceOnce(text,
`        audio.autoplay = true;
        audio.playsInline = true;
        audio.setAttribute('webkit-playsinline', '');
        container.appendChild(audio);
    }
    audio.srcObject = stream;
    audio.play()`,
`        audio.autoplay = true;
        audio.playsInline = true;
        audio.setAttribute('webkit-playsinline', '');
        container.appendChild(audio);
    }
    audio.muted = false;
    audio.volume = 1;
    audio.srcObject = stream;
    if (shouldShowPersistentAudioUnlock()) {
        showRemoteAudioUnlockButton('移动浏览器可能需要点按一次才能播放对讲声音');
    }
    audio.play()`,
    'remote audio unlock before play');
  write(file, text);
}

// server.js
{
  const file = 'server.js';
  let text = read(file);
  text = replaceOnce(text,
`const MAX_MAGNETS = 1000;
const MAGNET_TTL = 24 * 60 * 60 * 1000;`,
`const MAX_MAGNETS = 1000;
const MAX_MAGNET_FILE_ASSET_SIZE = 1024 * 1024 * 1024;
const MAGNET_TTL = 24 * 60 * 60 * 1000;`,
    'magnet max asset size');
  text = replaceOnce(text,
`function isValidMagnetId(id) {
    return typeof id === 'string' && /^[a-zA-Z0-9_-]{12,64}$/.test(id);
}

function getRequestBaseUrl(req) {`,
`function isValidMagnetId(id) {
    return typeof id === 'string' && /^[a-zA-Z0-9_-]{12,64}$/.test(id);
}

function isValidMagnetAssetPayload(asset, fileId) {
    return asset &&
        asset.id === fileId &&
        isValidDeviceId(asset.id) &&
        typeof asset.name === 'string' && asset.name.length > 0 && asset.name.length <= 255 &&
        typeof asset.type === 'string' && asset.type.length > 0 && asset.type.length <= 100 &&
        typeof asset.size === 'number' && asset.size > 0 && asset.size <= MAX_MAGNET_FILE_ASSET_SIZE;
}

function createMagnetFileAssetRecord(asset, providerDeviceId) {
    return {
        metadata: {
            id: asset.id,
            name: sanitizeString(asset.name, 255),
            type: sanitizeString(asset.type || 'application/octet-stream', 100),
            size: asset.size,
            ownerDeviceId: isValidDeviceId(asset.ownerDeviceId) ? asset.ownerDeviceId : providerDeviceId,
            isFolderArchive: asset.isFolderArchive === true,
            isDirectoryMirror: asset.isDirectoryMirror === true,
            folderName: typeof asset.folderName === 'string' ? sanitizeString(asset.folderName, 120) : undefined,
            entryCount: Number.isInteger(asset.entryCount) ? asset.entryCount : undefined
        },
        providers: new Set([providerDeviceId]),
        providerLoads: new Map(),
        assignments: new Map()
    };
}

function getRequestBaseUrl(req) {`,
    'magnet asset helpers');
  text = replaceOnce(text,
`        const { sessionId, fileId, deviceId } = req.body || {};
        if (!isValidSessionId(sessionId) || !isValidDeviceId(fileId)) {
            return res.status(400).json({ error: 'Invalid magnet payload' });
        }

        const session = sessions.get(sessionId);
        const record = session?.fileAssets?.get(fileId);
        if (!session || !record) {
            return res.status(404).json({ error: 'File asset is not registered online' });
        }`,
`        const { sessionId, fileId, deviceId, asset } = req.body || {};
        if (!isValidSessionId(sessionId) || !isValidDeviceId(fileId)) {
            return res.status(400).json({ error: 'Invalid magnet payload' });
        }

        const session = sessions.get(sessionId);
        if (!session) {
            return res.status(404).json({ error: 'Session is not online' });
        }
        if (!session.fileAssets) session.fileAssets = new Map();
        let record = session.fileAssets.get(fileId);
        const canRegisterRequester = isValidDeviceId(deviceId) && session.devices.has(deviceId) && deviceSockets.has(deviceId);
        if (!record && canRegisterRequester && isValidMagnetAssetPayload(asset, fileId)) {
            record = createMagnetFileAssetRecord(asset, deviceId);
            session.fileAssets.set(fileId, record);
            historyLog('magnet-file-asset-registered-from-request', {
                sessionId,
                deviceId,
                clientIp: getHttpClientIp(req),
                asset: record.metadata
            });
        } else if (record && canRegisterRequester && (!asset || isValidMagnetAssetPayload(asset, fileId))) {
            record.providers.add(deviceId);
        }
        if (!record) {
            return res.status(404).json({ error: 'File asset is not registered online' });
        }`,
    'magnet register local seed');
  write(file, text);
}

// downloadList.html
{
  const file = 'downloadList.html';
  let text = read(file);
  text = replaceOnce(text,
`        .magnet-loader {
            display: flex;
            gap: 8px;
            margin: 0 0 14px;
        }`,
`        .magnet-loader {
            display: flex;
            gap: 8px;
            margin: 0 0 14px;
            align-items: center;
        }`,
    'magnet loader row');
  text = replaceOnce(text,
`        @media (max-width: 640px) {
            header, .item {
                grid-template-columns: 1fr;
                display: grid;
            }
            .magnet-loader { grid-template-columns: 1fr; display: grid; }
            .actions { justify-content: flex-start; }
        }`,
`        .inline-downloader {
            display: none;
            margin: 0 0 14px;
            border: 1px solid #dfe5f1;
            border-radius: 10px;
            overflow: hidden;
            background: #fff;
            box-shadow: 0 10px 28px rgba(31, 45, 77, 0.08);
        }
        .inline-downloader.open { display: grid; grid-template-rows: auto minmax(360px, 62vh); }
        .inline-downloader.minimized { grid-template-rows: auto; }
        .inline-downloader.minimized iframe { display: none; }
        .inline-downloader-header {
            display: flex;
            align-items: center;
            justify-content: space-between;
            gap: 8px;
            padding: 8px 10px;
            border-bottom: 1px solid #e6ebf4;
            color: #36445f;
            font-weight: 800;
        }
        .inline-downloader-actions { display: inline-flex; gap: 6px; }
        .inline-downloader-actions button { min-height: 30px; padding: 0 10px; }
        .inline-downloader iframe { width: 100%; height: 100%; border: 0; }
        @media (max-width: 640px) {
            header, .item {
                grid-template-columns: 1fr;
                display: grid;
            }
            .magnet-loader { display: flex; }
            .magnet-loader button { padding: 0 10px; white-space: nowrap; }
            .actions { justify-content: flex-start; }
        }`,
    'inline downloader styles');
  text = replaceOnce(text,
`    <form id="magnetLoadForm" class="magnet-loader">
        <input id="magnetIdInput" type="text" inputmode="text" autocomplete="off" placeholder="输入磁链ID加载文件">
        <button type="submit">加载文件</button>
    </form>
    <div id="list" class="list">`,
`    <form id="magnetLoadForm" class="magnet-loader">
        <input id="magnetIdInput" type="text" inputmode="text" autocomplete="off" placeholder="输入完整磁链或磁链ID">
        <button type="submit">加载文件</button>
    </form>
    <section id="inlineDownloader" class="inline-downloader" aria-live="polite">
        <div class="inline-downloader-header">
            <span id="inlineDownloaderTitle">磁链下载</span>
            <span class="inline-downloader-actions">
                <button id="inlineDownloaderMinimize" type="button" title="隐藏下载页">-</button>
                <button id="inlineDownloaderClose" type="button" title="关闭下载页">x</button>
            </span>
        </div>
        <iframe id="inlineDownloaderFrame" title="磁链下载"></iframe>
    </section>
    <div id="list" class="list">`,
    'inline downloader markup');
  text = replaceOnce(text,
`    const embedded = new URLSearchParams(location.search).get('embedded') === '1';
    if (embedded) document.querySelector('header a.secondary')?.remove();

    magnetLoadForm.addEventListener('submit', event => {
        event.preventDefault();
        const magnetId = magnetIdInput.value.trim();
        if (!magnetId) return;
        window.location.href = `/downloader?magnet=${encodeURIComponent(magnetId)}`;
    });`,
`    const embedded = new URLSearchParams(location.search).get('embedded') === '1';
    const inlineDownloader = document.getElementById('inlineDownloader');
    const inlineDownloaderFrame = document.getElementById('inlineDownloaderFrame');
    const inlineDownloaderTitle = document.getElementById('inlineDownloaderTitle');
    const inlineDownloaderMinimize = document.getElementById('inlineDownloaderMinimize');
    const inlineDownloaderClose = document.getElementById('inlineDownloaderClose');
    if (embedded) document.querySelector('header a.secondary')?.remove();

    function normalizeMagnetInput(value) {
        const raw = String(value || '').trim();
        if (!raw) return '';
        try {
            const url = new URL(raw, location.origin);
            const queryId = url.searchParams.get('magnet');
            if (queryId) return queryId.trim();
            const match = url.pathname.match(/\/magnet\/([a-zA-Z0-9_-]{12,64})/);
            if (match) return match[1];
        } catch {}
        return raw.replace(/^magnet:/i, '').trim();
    }

    async function openInlineDownloader(magnetId) {
        inlineDownloader.classList.add('open');
        inlineDownloader.classList.remove('minimized');
        inlineDownloaderTitle.textContent = '正在校验磁链...';
        const response = await fetch(`/api/magnets/${encodeURIComponent(magnetId)}`, { cache: 'no-store' });
        const payload = await response.json().catch(() => ({}));
        if (!response.ok) throw new Error(payload.error || '磁链无效或已过期');
        inlineDownloaderTitle.textContent = `磁链下载：${payload.asset?.name || magnetId}`;
        inlineDownloaderFrame.src = `/downloader?embedded=1&magnet=${encodeURIComponent(magnetId)}`;
    }

    inlineDownloaderMinimize.addEventListener('click', () => {
        inlineDownloader.classList.toggle('minimized');
        inlineDownloaderMinimize.textContent = inlineDownloader.classList.contains('minimized') ? '+' : '-';
    });
    inlineDownloaderClose.addEventListener('click', () => {
        inlineDownloader.classList.remove('open', 'minimized');
        inlineDownloaderFrame.removeAttribute('src');
        inlineDownloaderMinimize.textContent = '-';
    });

    magnetLoadForm.addEventListener('submit', event => {
        event.preventDefault();
        const magnetId = normalizeMagnetInput(magnetIdInput.value);
        if (!/^[a-zA-Z0-9_-]{12,64}$/.test(magnetId)) {
            alert('请输入有效的磁链ID或完整磁链地址');
            return;
        }
        openInlineDownloader(magnetId).catch(err => {
            inlineDownloader.classList.remove('open', 'minimized');
            alert(`磁链校验失败：${err.message}`);
        });
    });`,
    'inline downloader script');
  write(file, text);
}

// docs append
{
  const file = 'docs/devlog/dev-260628-features.md';
  ensureDir(path.dirname(file));
  const entry = `

## 2026-06-28 STEP2 传输性能与遗留问题处理

### 网络传输性能
- 优化 P2P DataChannel 背压等待：不再依赖可能延迟触发的 bufferedamountlow 事件和 1 秒兜底等待，改为事件 + 20ms 轮询组合，降低同 WiFi 下大文件 P2P 传输被缓冲等待拖慢的概率。
- P2P 传输分片提升到 128KB，Socket.IO relay 保持 64KB，避免触发服务端 relay 分片上限。
- 强制拉取缓存时将目标文件加入优先队列，优先于普通后台补缓存任务调度。
- 进度 UI 更新增加 120ms 节流，减少大文件传输时频繁 DOM 更新拖慢纯文本/富文本同步的概率。

### 进度面板
- 传输进度面板默认缩小，减少多任务传输时遮挡主界面。
- 保留进行中的进度条置顶逻辑，上行和下行只要最近确实有进度增长都会优先显示。

### 磁链与做种
- 分享磁链时随请求携带本机缓存文件元数据，服务端确认设备在线后可直接注册本机为种子，降低 “File asset is not registered online” 的误报。
- 磁链缓存列表支持输入完整磁链地址或磁链 ID，先校验有效性，再在列表上方以内嵌 iframe 打开下载页；iframe 提供最小化和关闭按钮。

### 移动端音频
- 远端对讲/语音音轨到达时，在 iOS、微信内置浏览器、Opera 等环境主动显示声音启用按钮，并确保远端 audio 非静音、音量为 1。

### 暂未完整改造
- 真正断点续传仍需要持久化 partial ranges/chunks 和服务端范围协商，本次只保留中断标记和优先重拉。
- iOS Opera 富文本图片乱码先加强 Blob/MIME 路径保护；若仍复现，需要采集具体 DOM 和缓存文件 type/size 日志定位。
`;
  fs.appendFileSync(file, entry, 'utf8');
}
'@
node -e $script







刚才写入脚本没有执行成功，原因是脚本里匹配代码片段时碰到了源码中的 JS 模板字符串反引号，Node 在解析阶段报错；没有写入任何文件。我改成按行拼接匹配后重新执行。




已运行 $script = @'
const fs = require('fs');
const path = require('path');

function read(file) { return fs.readFileSync(file, 'utf8'); }
function write(file, text) { fs.writeFileSync(file, text, 'utf8'); }
function eolOf(text) { return text.includes('\r\n') ? '\r\n' : '\n'; }
function join(eol, arr) { return arr.join(eol); }
function replaceOnce(text, search, replacement, label) {
  if (!text.includes(search)) throw new Error(`Missing pattern: ${label}`);
  return text.replace(search, replacement);
}
function ensureDir(dir) { fs.mkdirSync(dir, { recursive: true }); }

{
  const file = 'client/file-assets.js';
  let text = read(file);
  const eol = eolOf(text);
  text = replaceOnce(text,
    join(eol, [
      '    const CHUNK_SIZE = 64 * 1024;',
      '    const BUFFER_LIMIT = 512 * 1024;',
      '    const P2P_TIMEOUT = 1500;'
    ]),
    join(eol, [
      '    const RELAY_CHUNK_SIZE = 64 * 1024;',
      '    const P2P_CHUNK_SIZE = 128 * 1024;',
      '    const BUFFER_LIMIT = 4 * 1024 * 1024;',
      '    const BUFFER_LOW_WATER = 1 * 1024 * 1024;',
      '    const BUFFER_POLL_MS = 20;',
      '    const BUFFER_WAIT_TIMEOUT = 5000;',
      '    const P2P_TIMEOUT = 1500;'
    ]),
    'file asset buffer constants');
  text = replaceOnce(text,
    join(eol, [
      '            this.downloadQueue = [];',
      '            this.activeDownloads = new Set();'
    ]),
    join(eol, [
      '            this.downloadQueue = [];',
      '            this.priorityDownloads = new Set();',
      '            this.activeDownloads = new Set();'
    ]),
    'priorityDownloads constructor');
  const forceOld = join(eol, [
    '            if (options.force) {',
    '                this.cancel(assetId);',
    '                this.forceRequests.set(assetId, `${Date.now()}-${Math.random().toString(36).slice(2, 8)}`);',
    '            }',
    '            this.cancelledAssets.delete(assetId);'
  ]);
  const forceNew = join(eol, [
    '            if (options.force) {',
    '                this.cancel(assetId);',
    '                this.forceRequests.set(assetId, `${Date.now()}-${Math.random().toString(36).slice(2, 8)}`);',
    '            }',
    '            if (options.force || options.priority) this.priorityDownloads.add(assetId);',
    '            this.cancelledAssets.delete(assetId);'
  ]);
  text = replaceOnce(text, forceOld, forceNew, 'force priority request');
  text = replaceOnce(text,
    join(eol, [
      '                this.desiredAssets.delete(assetId);',
      '                this.requestedMetadata.delete(assetId);',
      '                this.forceRequests.delete(assetId);',
      "                this.log('request-skipped-local-cache', { assetId, size: this.dataSize(local.data) });"
    ]),
    join(eol, [
      '                this.desiredAssets.delete(assetId);',
      '                this.requestedMetadata.delete(assetId);',
      '                this.forceRequests.delete(assetId);',
      '                this.priorityDownloads.delete(assetId);',
      "                this.log('request-skipped-local-cache', { assetId, size: this.dataSize(local.data) });"
    ]),
    'local cache priority cleanup');
  text = replaceOnce(text,
    join(eol, [
      '        enqueueDownload(assetId) {',
      '            if (!this.desiredAssets.has(assetId) || this.activeDownloads.has(assetId) || this.downloadQueue.includes(assetId)) return;',
      '            this.downloadQueue.push(assetId);',
      '            this.deps.onQueue?.(assetId, this.downloadQueue.length, this.activeDownloads.size);',
      "            this.log('download-queued', { assetId, queueLength: this.downloadQueue.length, activeDownloads: this.activeDownloads.size });",
      '            this.dispatchDownloads();',
      '        }'
    ]),
    join(eol, [
      '        enqueueDownload(assetId) {',
      '            if (!this.desiredAssets.has(assetId) || this.activeDownloads.has(assetId) || this.downloadQueue.includes(assetId)) return;',
      '            if (this.priorityDownloads.has(assetId)) this.downloadQueue.unshift(assetId);',
      '            else this.downloadQueue.push(assetId);',
      '            this.deps.onQueue?.(assetId, this.downloadQueue.length, this.activeDownloads.size);',
      "            this.log('download-queued', {",
      '                assetId,',
      '                queueLength: this.downloadQueue.length,',
      '                activeDownloads: this.activeDownloads.size,',
      '                priority: this.priorityDownloads.has(assetId)',
      '            });',
      '            this.dispatchDownloads();',
      '        }'
    ]),
    'priority enqueue');
  text = replaceOnce(text,
    join(eol, [
      '        downloadPriority(assetId) {',
      '            const size = this.assetSize(assetId);'
    ]),
    join(eol, [
      '        downloadPriority(assetId) {',
      '            if (this.priorityDownloads.has(assetId)) return -1;',
      '            const size = this.assetSize(assetId);'
    ]),
    'priority scoring');
  const cleanupOld = join(eol, [
    '            this.desiredAssets.delete(assetId);',
    '            this.requestedMetadata.delete(assetId);',
    '            this.forceRequests.delete(assetId);',
    '            this.retryCounts.delete(assetId);'
  ]);
  const cleanupNew = join(eol, [
    '            this.desiredAssets.delete(assetId);',
    '            this.requestedMetadata.delete(assetId);',
    '            this.forceRequests.delete(assetId);',
    '            this.priorityDownloads.delete(assetId);',
    '            this.retryCounts.delete(assetId);'
  ]);
  text = replaceOnce(text, cleanupOld, cleanupNew, 'multi-source complete priority cleanup');
  text = replaceOnce(text, cleanupOld, cleanupNew, 'complete priority cleanup');
  text = replaceOnce(text,
    join(eol, [
      '                this.desiredAssets.delete(assetId);',
      '                this.requestedMetadata.delete(assetId);',
      '                this.forceRequests.delete(assetId);',
      "                this.markInterruptedAsset(assetId, interruptedAsset, reason);"
    ]),
    join(eol, [
      '                this.desiredAssets.delete(assetId);',
      '                this.requestedMetadata.delete(assetId);',
      '                this.forceRequests.delete(assetId);',
      '                this.priorityDownloads.delete(assetId);',
      "                this.markInterruptedAsset(assetId, interruptedAsset, reason);"
    ]),
    'range retry exhausted priority cleanup');
  text = replaceOnce(text,
    join(eol, [
      '        async waitForBuffer(channel) {',
      '            if (channel.bufferedAmount <= BUFFER_LIMIT) return;',
      '            await new Promise(resolve => {',
      '                const timer = setTimeout(resolve, 1000);',
      '                channel.bufferedAmountLowThreshold = BUFFER_LIMIT / 2;',
      "                channel.addEventListener('bufferedamountlow', () => { clearTimeout(timer); resolve(); }, { once: true });",
      '            });',
      '        }'
    ]),
    join(eol, [
      '        async waitForBuffer(channel) {',
      "            if (channel.readyState !== 'open') throw new Error('File asset channel closed');",
      '            if (channel.bufferedAmount <= BUFFER_LIMIT) return;',
      '            channel.bufferedAmountLowThreshold = BUFFER_LOW_WATER;',
      '            const startedAt = Date.now();',
      "            while (channel.readyState === 'open' && channel.bufferedAmount > BUFFER_LOW_WATER) {",
      '                await new Promise(resolve => {',
      '                    let settled = false;',
      '                    const finish = () => {',
      '                        if (settled) return;',
      '                        settled = true;',
      '                        clearTimeout(timer);',
      "                        channel.removeEventListener('bufferedamountlow', finish);",
      '                        resolve();',
      '                    };',
      '                    const timer = setTimeout(finish, BUFFER_POLL_MS);',
      "                    channel.addEventListener('bufferedamountlow', finish, { once: true });",
      '                });',
      '                if (Date.now() - startedAt > BUFFER_WAIT_TIMEOUT && channel.bufferedAmount > BUFFER_LIMIT * 2) {',
      "                    throw new Error('File asset channel backpressure timeout');",
      '                }',
      '            }',
      "            if (channel.readyState !== 'open') throw new Error('File asset channel closed');",
      '        }'
    ]),
    'fast buffer wait');
  text = replaceOnce(text,
    join(eol, [
      '            for (let offset = rangeStart; offset < rangeEnd; offset += CHUNK_SIZE) {',
      "                if (this.cancelledAssets.has(asset.id)) throw new Error('File asset transfer cancelled');",
      "                if (channel.readyState !== 'open') throw new Error('File asset channel closed');",
      '                await this.waitForBuffer(channel);',
      '                channel.send(this.sliceData(asset.data, offset, Math.min(offset + CHUNK_SIZE, rangeEnd)));'
    ]),
    join(eol, [
      '            for (let offset = rangeStart; offset < rangeEnd; offset += P2P_CHUNK_SIZE) {',
      "                if (this.cancelledAssets.has(asset.id)) throw new Error('File asset transfer cancelled');",
      "                if (channel.readyState !== 'open') throw new Error('File asset channel closed');",
      '                await this.waitForBuffer(channel);',
      '                channel.send(this.sliceData(asset.data, offset, Math.min(offset + P2P_CHUNK_SIZE, rangeEnd)));'
    ]),
    'p2p chunk size');
  text = replaceOnce(text,
    join(eol, [
      '            for (let offset = rangeStart; offset < rangeEnd; offset += CHUNK_SIZE) {',
      "                if (this.cancelledAssets.has(asset.id)) throw new Error('File asset transfer cancelled');",
      "                socket.emit('file-asset-relay-chunk', {",
      '                    sessionId: this.deps.getSessionId(), to: deviceId, assetId: asset.id, transferId: transfer?.transferId,',
      '                    chunk: this.sliceData(asset.data, offset, Math.min(offset + CHUNK_SIZE, rangeEnd))'
    ]),
    join(eol, [
      '            for (let offset = rangeStart; offset < rangeEnd; offset += RELAY_CHUNK_SIZE) {',
      "                if (this.cancelledAssets.has(asset.id)) throw new Error('File asset transfer cancelled');",
      "                socket.emit('file-asset-relay-chunk', {",
      '                    sessionId: this.deps.getSessionId(), to: deviceId, assetId: asset.id, transferId: transfer?.transferId,',
      '                    chunk: this.sliceData(asset.data, offset, Math.min(offset + RELAY_CHUNK_SIZE, rangeEnd))'
    ]),
    'relay chunk size');
  text = replaceOnce(text,
    join(eol, [
      '            this.desiredAssets.delete(assetId);',
      '            this.requestedMetadata.delete(assetId);',
      '            this.forceRequests.delete(assetId);',
      '            this.stopMultiSourceWatchdog(this.multiSourceTransfers.get(assetId));'
    ]),
    join(eol, [
      '            this.desiredAssets.delete(assetId);',
      '            this.requestedMetadata.delete(assetId);',
      '            this.forceRequests.delete(assetId);',
      '            this.priorityDownloads.delete(assetId);',
      '            this.stopMultiSourceWatchdog(this.multiSourceTransfers.get(assetId));'
    ]),
    'cancel priority cleanup');
  text = replaceOnce(text,
    join(eol, [
      '                this.desiredAssets.delete(assetId);',
      '                this.requestedMetadata.delete(assetId);',
      "                this.deps.onUnavailable(assetId, 'transfer-interrupted');"
    ]),
    join(eol, [
      '                this.desiredAssets.delete(assetId);',
      '                this.requestedMetadata.delete(assetId);',
      '                this.priorityDownloads.delete(assetId);',
      "                this.deps.onUnavailable(assetId, 'transfer-interrupted');"
    ]),
    'retry exhausted priority cleanup');
  write(file, text);
}

{
  const file = 'app.js';
  let text = read(file);
  const eol = eolOf(text);
  text = replaceOnce(text, 'let progressDrawerCollapsed = false;', 'let progressDrawerCollapsed = true;', 'default collapsed progress drawer');
  text = replaceOnce(text,
    join(eol, [
      'const progressHideTimers = new Map();',
      'const fileTransferProgressStates = new Map();'
    ]),
    join(eol, [
      'const progressHideTimers = new Map();',
      'const progressUiLastPaint = new Map();',
      'const fileTransferProgressStates = new Map();',
      'const PROGRESS_UI_MIN_INTERVAL = 120;'
    ]),
    'progress ui throttle globals');
  text = replaceOnce(text,
    join(eol, [
      '            showProgress(progressKey, fileName, progress, status, { route });',
      '            if (terminal) {'
    ]),
    join(eol, [
      '            const now = Date.now();',
      '            const lastPaintAt = progressUiLastPaint.get(progressKey) || 0;',
      '            const shouldPaintProgress = terminal || progress === 0 ||',
      '                now - lastPaintAt >= PROGRESS_UI_MIN_INTERVAL ||',
      '                !document.getElementById(progressElementId(progressKey));',
      '            if (shouldPaintProgress) {',
      '                showProgress(progressKey, fileName, progress, status, { route });',
      '                progressUiLastPaint.set(progressKey, now);',
      '            }',
      '            if (terminal) {'
    ]),
    'progress ui throttle');
  text = replaceOnce(text,
    join(eol, [
      '    activeFileProgress.delete(fileId);',
      '    const timer = progressHideTimers.get(fileId);'
    ]),
    join(eol, [
      '    activeFileProgress.delete(fileId);',
      '    progressUiLastPaint.delete(fileId);',
      '    const timer = progressHideTimers.get(fileId);'
    ]),
    'hide progress ui cleanup');
  text = replaceOnce(text,
    join(eol, [
      '                fileId: fileInfo.id',
      '            })'
    ]),
    join(eol, [
      '                fileId: fileInfo.id,',
      '                deviceId: state.deviceId,',
      '                asset: {',
      "                    id: fileInfo.id,",
      "                    name: fileInfo.name || storedFile.name || 'file',",
      "                    type: fileInfo.type || storedFile.type || 'application/octet-stream',",
      '                    size: Number(fileInfo.size || storedFile.size || getBinaryDataSize(storedFile.data)),',
      '                    ownerDeviceId: storedFile.ownerDeviceId || fileInfo.ownerDeviceId || state.deviceId,',
      '                    isFolderArchive: fileInfo.isFolderArchive === true || storedFile.isFolderArchive === true,',
      '                    isDirectoryMirror: fileInfo.isDirectoryMirror === true || storedFile.isDirectoryMirror === true,',
      '                    folderName: fileInfo.folderName || storedFile.folderName,',
      '                    entryCount: Number.isInteger(fileInfo.entryCount) ? fileInfo.entryCount : storedFile.entryCount',
      '                }',
      '            })'
    ]),
    'magnet post asset metadata');
  text = replaceOnce(text,
    'function showRemoteAudioUnlockButton(reason = \'\') {',
    join(eol, [
      'function shouldShowPersistentAudioUnlock() {',
      "    return /iPhone|iPad|iPod|MicroMessenger|OPR\\//i.test(navigator.userAgent || '');",
      '}',
      '',
      "function showRemoteAudioUnlockButton(reason = '') {"
    ]),
    'persistent audio helper');
  text = replaceOnce(text,
    join(eol, [
      '        audio.autoplay = true;',
      '        audio.playsInline = true;',
      "        audio.setAttribute('webkit-playsinline', '');",
      '        container.appendChild(audio);',
      '    }',
      '    audio.srcObject = stream;',
      '    audio.play()'
    ]),
    join(eol, [
      '        audio.autoplay = true;',
      '        audio.playsInline = true;',
      "        audio.setAttribute('webkit-playsinline', '');",
      '        container.appendChild(audio);',
      '    }',
      '    audio.muted = false;',
      '    audio.volume = 1;',
      '    audio.srcObject = stream;',
      '    if (shouldShowPersistentAudioUnlock()) {',
      "        showRemoteAudioUnlockButton('移动浏览器可能需要点按一次才能播放对讲声音');",
      '    }',
      '    audio.play()'
    ]),
    'remote audio unlock before play');
  write(file, text);
}

{
  const file = 'server.js';
  let text = read(file);
  const eol = eolOf(text);
  text = replaceOnce(text,
    join(eol, ['const MAX_MAGNETS = 1000;', 'const MAGNET_TTL = 24 * 60 * 60 * 1000;']),
    join(eol, ['const MAX_MAGNETS = 1000;', 'const MAX_MAGNET_FILE_ASSET_SIZE = 1024 * 1024 * 1024;', 'const MAGNET_TTL = 24 * 60 * 60 * 1000;']),
    'magnet max asset size');
  text = replaceOnce(text,
    join(eol, [
      'function isValidMagnetId(id) {',
      "    return typeof id === 'string' && /^[a-zA-Z0-9_-]{12,64}$/.test(id);",
      '}',
      '',
      'function getRequestBaseUrl(req) {'
    ]),
    join(eol, [
      'function isValidMagnetId(id) {',
      "    return typeof id === 'string' && /^[a-zA-Z0-9_-]{12,64}$/.test(id);",
      '}',
      '',
      'function isValidMagnetAssetPayload(asset, fileId) {',
      '    return asset &&',
      '        asset.id === fileId &&',
      '        isValidDeviceId(asset.id) &&',
      "        typeof asset.name === 'string' && asset.name.length > 0 && asset.name.length <= 255 &&",
      "        typeof asset.type === 'string' && asset.type.length > 0 && asset.type.length <= 100 &&",
      "        typeof asset.size === 'number' && asset.size > 0 && asset.size <= MAX_MAGNET_FILE_ASSET_SIZE;",
      '}',
      '',
      'function createMagnetFileAssetRecord(asset, providerDeviceId) {',
      '    return {',
      '        metadata: {',
      '            id: asset.id,',
      '            name: sanitizeString(asset.name, 255),',
      "            type: sanitizeString(asset.type || 'application/octet-stream', 100),",
      '            size: asset.size,',
      '            ownerDeviceId: isValidDeviceId(asset.ownerDeviceId) ? asset.ownerDeviceId : providerDeviceId,',
      '            isFolderArchive: asset.isFolderArchive === true,',
      '            isDirectoryMirror: asset.isDirectoryMirror === true,',
      "            folderName: typeof asset.folderName === 'string' ? sanitizeString(asset.folderName, 120) : undefined,",
      '            entryCount: Number.isInteger(asset.entryCount) ? asset.entryCount : undefined',
      '        },',
      '        providers: new Set([providerDeviceId]),',
      '        providerLoads: new Map(),',
      '        assignments: new Map()',
      '    };',
      '}',
      '',
      'function getRequestBaseUrl(req) {'
    ]),
    'magnet asset helpers');
  text = replaceOnce(text,
    join(eol, [
      '        const { sessionId, fileId, deviceId } = req.body || {};',
      '        if (!isValidSessionId(sessionId) || !isValidDeviceId(fileId)) {',
      "            return res.status(400).json({ error: 'Invalid magnet payload' });",
      '        }',
      '',
      '        const session = sessions.get(sessionId);',
      '        const record = session?.fileAssets?.get(fileId);',
      '        if (!session || !record) {',
      "            return res.status(404).json({ error: 'File asset is not registered online' });",
      '        }'
    ]),
    join(eol, [
      '        const { sessionId, fileId, deviceId, asset } = req.body || {};',
      '        if (!isValidSessionId(sessionId) || !isValidDeviceId(fileId)) {',
      "            return res.status(400).json({ error: 'Invalid magnet payload' });",
      '        }',
      '',
      '        const session = sessions.get(sessionId);',
      '        if (!session) {',
      "            return res.status(404).json({ error: 'Session is not online' });",
      '        }',
      '        if (!session.fileAssets) session.fileAssets = new Map();',
      '        let record = session.fileAssets.get(fileId);',
      '        const canRegisterRequester = isValidDeviceId(deviceId) && session.devices.has(deviceId) && deviceSockets.has(deviceId);',
      '        if (!record && canRegisterRequester && isValidMagnetAssetPayload(asset, fileId)) {',
      '            record = createMagnetFileAssetRecord(asset, deviceId);',
      '            session.fileAssets.set(fileId, record);',
      "            historyLog('magnet-file-asset-registered-from-request', {",
      '                sessionId,',
      '                deviceId,',
      '                clientIp: getHttpClientIp(req),',
      '                asset: record.metadata',
      '            });',
      '        } else if (record && canRegisterRequester && (!asset || isValidMagnetAssetPayload(asset, fileId))) {',
      '            record.providers.add(deviceId);',
      '        }',
      '        if (!record) {',
      "            return res.status(404).json({ error: 'File asset is not registered online' });",
      '        }'
    ]),
    'magnet register local seed');
  write(file, text);
}

{
  const file = 'downloadList.html';
  let text = read(file);
  const eol = eolOf(text);
  text = replaceOnce(text,
    join(eol, ['        .magnet-loader {', '            display: flex;', '            gap: 8px;', '            margin: 0 0 14px;', '        }']),
    join(eol, ['        .magnet-loader {', '            display: flex;', '            gap: 8px;', '            margin: 0 0 14px;', '            align-items: center;', '        }']),
    'magnet loader row');
  text = replaceOnce(text,
    join(eol, [
      '        @media (max-width: 640px) {',
      '            header, .item {',
      '                grid-template-columns: 1fr;',
      '                display: grid;',
      '            }',
      '            .magnet-loader { grid-template-columns: 1fr; display: grid; }',
      '            .actions { justify-content: flex-start; }',
      '        }'
    ]),
    join(eol, [
      '        .inline-downloader {',
      '            display: none;',
      '            margin: 0 0 14px;',
      '            border: 1px solid #dfe5f1;',
      '            border-radius: 10px;',
      '            overflow: hidden;',
      '            background: #fff;',
      '            box-shadow: 0 10px 28px rgba(31, 45, 77, 0.08);',
      '        }',
      '        .inline-downloader.open { display: grid; grid-template-rows: auto minmax(360px, 62vh); }',
      '        .inline-downloader.minimized { grid-template-rows: auto; }',
      '        .inline-downloader.minimized iframe { display: none; }',
      '        .inline-downloader-header {',
      '            display: flex;',
      '            align-items: center;',
      '            justify-content: space-between;',
      '            gap: 8px;',
      '            padding: 8px 10px;',
      '            border-bottom: 1px solid #e6ebf4;',
      '            color: #36445f;',
      '            font-weight: 800;',
      '        }',
      '        .inline-downloader-actions { display: inline-flex; gap: 6px; }',
      '        .inline-downloader-actions button { min-height: 30px; padding: 0 10px; }',
      '        .inline-downloader iframe { width: 100%; height: 100%; border: 0; }',
      '        @media (max-width: 640px) {',
      '            header, .item {',
      '                grid-template-columns: 1fr;',
      '                display: grid;',
      '            }',
      '            .magnet-loader { display: flex; }',
      '            .magnet-loader button { padding: 0 10px; white-space: nowrap; }',
      '            .actions { justify-content: flex-start; }',
      '        }'
    ]),
    'inline downloader styles');
  text = replaceOnce(text,
    join(eol, [
      '    <form id="magnetLoadForm" class="magnet-loader">',
      '        <input id="magnetIdInput" type="text" inputmode="text" autocomplete="off" placeholder="输入磁链ID加载文件">',
      '        <button type="submit">加载文件</button>',
      '    </form>',
      '    <div id="list" class="list">'
    ]),
    join(eol, [
      '    <form id="magnetLoadForm" class="magnet-loader">',
      '        <input id="magnetIdInput" type="text" inputmode="text" autocomplete="off" placeholder="输入完整磁链或磁链ID">',
      '        <button type="submit">加载文件</button>',
      '    </form>',
      '    <section id="inlineDownloader" class="inline-downloader" aria-live="polite">',
      '        <div class="inline-downloader-header">',
      '            <span id="inlineDownloaderTitle">磁链下载</span>',
      '            <span class="inline-downloader-actions">',
      '                <button id="inlineDownloaderMinimize" type="button" title="隐藏下载页">-</button>',
      '                <button id="inlineDownloaderClose" type="button" title="关闭下载页">x</button>',
      '            </span>',
      '        </div>',
      '        <iframe id="inlineDownloaderFrame" title="磁链下载"></iframe>',
      '    </section>',
      '    <div id="list" class="list">'
    ]),
    'inline downloader markup');
  text = replaceOnce(text,
    join(eol, [
      "    const embedded = new URLSearchParams(location.search).get('embedded') === '1';",
      "    if (embedded) document.querySelector('header a.secondary')?.remove();",
      '',
      "    magnetLoadForm.addEventListener('submit', event => {",
      '        event.preventDefault();',
      '        const magnetId = magnetIdInput.value.trim();',
      '        if (!magnetId) return;',
      '        window.location.href = `/downloader?magnet=${encodeURIComponent(magnetId)}`;',
      '    });'
    ]),
    join(eol, [
      "    const embedded = new URLSearchParams(location.search).get('embedded') === '1';",
      "    const inlineDownloader = document.getElementById('inlineDownloader');",
      "    const inlineDownloaderFrame = document.getElementById('inlineDownloaderFrame');",
      "    const inlineDownloaderTitle = document.getElementById('inlineDownloaderTitle');",
      "    const inlineDownloaderMinimize = document.getElementById('inlineDownloaderMinimize');",
      "    const inlineDownloaderClose = document.getElementById('inlineDownloaderClose');",
      "    if (embedded) document.querySelector('header a.secondary')?.remove();",
      '',
      '    function normalizeMagnetInput(value) {',
      "        const raw = String(value || '').trim();",
      "        if (!raw) return '';",
      '        try {',
      '            const url = new URL(raw, location.origin);',
      "            const queryId = url.searchParams.get('magnet');",
      '            if (queryId) return queryId.trim();',
      "            const match = url.pathname.match(/\\/magnet\\/([a-zA-Z0-9_-]{12,64})/);",
      '            if (match) return match[1];',
      '        } catch {}',
      "        return raw.replace(/^magnet:/i, '').trim();",
      '    }',
      '',
      '    async function openInlineDownloader(magnetId) {',
      "        inlineDownloader.classList.add('open');",
      "        inlineDownloader.classList.remove('minimized');",
      "        inlineDownloaderTitle.textContent = '正在校验磁链...';",
      '        const response = await fetch(`/api/magnets/${encodeURIComponent(magnetId)}`, { cache: \'no-store\' });',
      '        const payload = await response.json().catch(() => ({}));',
      "        if (!response.ok) throw new Error(payload.error || '磁链无效或已过期');",
      '        inlineDownloaderTitle.textContent = `磁链下载：${payload.asset?.name || magnetId}`;',
      '        inlineDownloaderFrame.src = `/downloader?embedded=1&magnet=${encodeURIComponent(magnetId)}`;',
      '    }',
      '',
      "    inlineDownloaderMinimize.addEventListener('click', () => {",
      "        inlineDownloader.classList.toggle('minimized');",
      "        inlineDownloaderMinimize.textContent = inlineDownloader.classList.contains('minimized') ? '+' : '-';",
      '    });',
      "    inlineDownloaderClose.addEventListener('click', () => {",
      "        inlineDownloader.classList.remove('open', 'minimized');",
      "        inlineDownloaderFrame.removeAttribute('src');",
      "        inlineDownloaderMinimize.textContent = '-';",
      '    });',
      '',
      "    magnetLoadForm.addEventListener('submit', event => {",
      '        event.preventDefault();',
      '        const magnetId = normalizeMagnetInput(magnetIdInput.value);',
      '        if (!/^[a-zA-Z0-9_-]{12,64}$/.test(magnetId)) {',
      "            alert('请输入有效的磁链ID或完整磁链地址');",
      '            return;',
      '        }',
      '        openInlineDownloader(magnetId).catch(err => {',
      "            inlineDownloader.classList.remove('open', 'minimized');",
      '            alert(`磁链校验失败：${err.message}`);',
      '        });',
      '    });'
    ]),
    'inline downloader script');
  write(file, text);
}

{
  const file = 'docs/devlog/dev-260628-features.md';
  ensureDir(path.dirname(file));
  const entry = `\n\n## 2026-06-28 STEP2 传输性能与遗留问题处理\n\n### 网络传输性能\n- 优化 P2P DataChannel 背压等待：不再依赖可能延迟触发的 bufferedamountlow 事件和 1 秒兜底等待，改为事件 + 20ms 轮询组合，降低同 WiFi 下大文件 P2P 传输被缓冲等待拖慢的概率。\n- P2P 传输分片提升到 128KB，Socket.IO relay 保持 64KB，避免触发服务端 relay 分片上限。\n- 强制拉取缓存时将目标文件加入优先队列，优先于普通后台补缓存任务调度。\n- 进度 UI 更新增加 120ms 节流，减少大文件传输时频繁 DOM 更新拖慢纯文本/富文本同步的概率。\n\n### 进度面板\n- 传输进度面板默认缩小，减少多任务传输时遮挡主界面。\n- 保留进行中的进度条置顶逻辑，上行和下行只要最近确实有进度增长都会优先显示。\n\n### 磁链与做种\n- 分享磁链时随请求携带本机缓存文件元数据，服务端确认设备在线后可直接注册本机为种子，降低 “File asset is not registered online” 的误报。\n- 磁链缓存列表支持输入完整磁链地址或磁链 ID，先校验有效性，再在列表上方以内嵌 iframe 打开下载页；iframe 提供最小化和关闭按钮。\n\n### 移动端音频\n- 远端对讲/语音音轨到达时，在 iOS、微信内置浏览器、Opera 等环境主动显示声音启用按钮，并确保远端 audio 非静音、音量为 1。\n\n### 暂未完整改造\n- 真正断点续传仍需要持久化 partial ranges/chunks 和服务端范围协商，本次只保留中断标记和优先重拉。\n- iOS Opera 富文本图片乱码先加强 Blob/MIME 路径保护；若仍复现，需要采集具体 DOM 和缓存文件 type/size 日志定位。\n`;
  fs.appendFileSync(file, entry, 'utf8');
}
'@
node -e $script







第二次提权脚本仍在解析阶段失败，依然没有写入成功。这里继续用大脚本不划算，我改用小块补丁逐项写入，便于定位每一步。




编辑了 1 个文件


小块补丁可以正常写入。现在继续按模块逐步改，避免再出现大脚本解析问题。




编辑了 1 个文件


强制拉取现在会进入优先队列：如果用户点某个文件的强制拉取，它会排到普通后台补缓存任务前面。




编辑了 1 个文件


P2P 发送缓冲已经改成 20ms 轮询加事件，不再每次可能等满 1 秒；P2P 分片单独提升到 128KB，Socket.IO 中继仍保持 64KB，避免碰服务端限制。




编辑了 1 个文件


进度抽屉默认状态已改为缩小，避免一上来遮挡功能区。接着给进度 UI 做节流，减少大文件传输时 DOM 更新拖慢其它消息。




编辑了 1 个文件


磁链分享请求现在会带上本机缓存文件元数据，服务端可以把当前在线的本机直接注册为种子，不再完全依赖先前 socket 注册是否已经到达。




编辑了 1 个文件


iOS/微信/Opera 远端音频到达时会主动显示“启用声音”按钮，并强制远端 audio 非静音、音量为 1。这个解决的是移动端自动播放策略问题。




编辑了 1 个文件


服务端磁链接口已补上“当前请求设备在线且有文件元数据时可注册为种子”的路径。接下来处理磁链缓存列表页的内嵌下载 iframe。




编辑了 1 个文件


缓存列表页输入框和按钮在移动端也保持同一行；内嵌下载面板的样式已经加上。




编辑了 1 个文件正在编辑 1 个文件执行了 1 条命令编辑了 1 个文件正在编辑 1 个文件执行了 1 条命令


内嵌 iframe 容器已插到输入框和缓存列表之间。接下来把原来的整页跳转改成“校验磁链后在本页 iframe 下载”。




编辑了 1 个文件


磁链缓存列表现在会接受完整 /magnet/...、/downloader?magnet=... 或纯磁链 ID，先请求 /api/magnets/:id 校验，有效后才在本页插入下载 iframe。




已运行 Test-Path docs\devlog\dev-260628-features.md







接下来追加 devlog 文件；由于该文件还不存在，这次会新建 docs/devlog/dev-260628-features.md。




已创建 1 个文件


devlog 已写入。现在做静态检查，重点看有没有漏掉 CHUNK_SIZE 引用和 JS 语法问题。




已运行 4 条命令


三个 JS 文件语法检查通过。接下来检查 downloadList.html 内嵌脚本是否有明显拼写/结构问题，并看当前工作区改动范围。




已运行 3 条命令


我看到 PowerShell 输出里中文被本机代码页显示成乱码，这不代表文件实际坏了；但我需要确认模板字符串里的 $ 没被吞掉。现在针对 downloadList.html 内脚本做一次机器级抽取检查。




已运行 4 条命令


downloadList.html 和 downloader.html 的内嵌脚本都通过语法解析。现在复查三个主 JS 文件和最终 diff。




已运行 4 条命令


语法检查全部通过。还有一个点：git diff --stat 不显示未跟踪 devlog，我再确认工作区状态和关键代码位置。




已运行 4 条命令


我把 P2P 分片大小再收敛一下：性能瓶颈主要是缓冲等待，不是 64KB 分片本身。为了避免 iOS/Safari 系 WebRTC DataChannel 对较大 message size 的兼容风险，P2P 单片保持 64KB，只扩大缓冲和修复等待逻辑。




编辑了 2 个文件


针对 iOS Opera 富文本图片乱码，我加一个保守防线：富文本图片资源如果本地缓存的 MIME 不是 image/*，不再尝试把它当图片 Blob 渲染，改为明确标记不可用并记录日志，避免浏览器把二进制走成文本/乱码路径。




编辑了 2 个文件


富文本图片 MIME 防线已加。现在重新跑所有检查。




已运行 4 条命令


检查通过。最后确认分支、未暂存状态和最终改动统计。




已运行 3 条命令





已在 dev-260626-NETWORK-FIX-NEWCODE-STEP2 上完成修改，未暂存、未提交。
主要改动：
[client/file-assets.js (line 2)](D:/Workspace/github/Ltre/file-tunnel/client/file-assets.js:2)：修复 P2P DataChannel 背压等待逻辑，不再可能每 512KB 等 1 秒；改为 4MB 缓冲窗口 + 20ms 轮询。P2P/relay 分片都保持 64KB，避免 iOS/Safari 兼容风险。
[client/file-assets.js (line 30)](D:/Workspace/github/Ltre/file-tunnel/client/file-assets.js:30)：强制拉取缓存会进入优先队列，优先于普通后台补缓存任务。
[app.js (line 89)](D:/Workspace/github/Ltre/file-tunnel/app.js:89)：传输进度面板默认缩小。
[app.js (line 2021)](D:/Workspace/github/Ltre/file-tunnel/app.js:2021)：进度 UI 增加 120ms 节流，减少大文件传输时频繁 DOM 更新拖慢纯文本/富文本同步。
[app.js (line 2115)](D:/Workspace/github/Ltre/file-tunnel/app.js:2115)：iOS/微信/Opera 收到远端音频时主动显示“启用声音”按钮，并确保 audio 非静音。
[app.js (line 3214)](D:/Workspace/github/Ltre/file-tunnel/app.js:3214)、[server.js (line 184)](D:/Workspace/github/Ltre/file-tunnel/server.js:184)：分享磁链时，如果本机有完整缓存，服务端可直接注册本机为种子，降低 File asset is not registered online。
[downloadList.html (line 150)](D:/Workspace/github/Ltre/file-tunnel/downloadList.html:150)：磁链缓存列表支持输入完整磁链或磁链 ID，校验后在本页夹层 iframe 打开下载页，带 - 和 x 控制。
[docs/devlog/dev-260628-features.md (line 1)](D:/Workspace/github/Ltre/file-tunnel/docs/devlog/dev-260628-features.md:1)：已记录本次需求和改动明细。
验证已通过：
node --check client\file-assets.js
node --check app.js
node --check server.js
downloadList.html 内嵌脚本语法解析
说明：真正“断点续传”这次没有完整实现，因为需要持久化 partial ranges/chunks 和范围协商，改动面比较大；这次先做了中断标记、强制拉取优先级和传输阻塞优化。


问：

对于20MB的文件P2P还是很慢，甚至还有个别几百K的小图进度条卡死

答：

我先把这次现象拆开看：20MB P2P 慢和几百 K 小图卡死可能不是同一个点。前者更像 DataChannel 背压/并发策略问题；后者更像小文件请求或完成事件没有正确收尾。我先检查当前 P2P 发送、接收完成、超时重试和进度隐藏逻辑。




已运行 3 条命令


我看到两个更具体的问题点：第一，当前 P2P 背压等待如果 bufferedAmount 卡在 1MB 到 8MB 之间可能无限等，不一定触发超时；第二，如果 P2P 中途失败后改走 relay，旧的 P2P 发送进度条可能被遗留，看起来像几百 K 文件“卡死”。我会先修这两个，再限制同一接收端的大文件并发上传，避免两个 20MB 同时抢同一条 WebRTC SCTP 通道。




编辑了 1 个文件


我把“两个 20MB 同时给同一设备发”的情况改成同一接收端只允许一个大完整文件上传在路上；小文件仍可插队和大文件并行。




编辑了 1 个文件


这样队列里有大文件时，小图不会被第二个大文件占满上传槽，能更快获得发送机会。接着修背压无限等待。




编辑了 1 个文件


背压等待现在如果缓冲长期没有下降，会主动失败并触发后续重试/relay，而不是挂在那里。下一步处理 P2P 失败后旧进度条遗留。




编辑了 1 个文件


如果 P2P 发送路线中途失败转 relay，旧 P2P 发送条现在会被收尾隐藏，不会留一个“卡死”的旧进度条。接着把最终不可用的接收进度也清掉。




编辑了 1 个文件


接收端如果最终判定资源不可用，也会清掉该文件的接收进度条，避免失败后进度 UI 留在旧百分比。现在更新 devlog 并跑检查。




编辑了 1 个文件


我现在执行语法检查和关键差异检查。




已运行 4 条命令


检查通过。最后确认工作区状态。




已运行 2 条命令


我注意到工作区里出现了 package-lock.json 和 prompts/dev-prompt-logs/dev-260626.md 的修改，但这两个不是我本次按需求编辑的文件。我先快速确认它们是不是无关改动，不会在最终说明里混到本次改动范围里。




已运行 3 条命令


确认了：prompts/dev-prompt-logs/dev-260626.md 是这次对话日志自动追加，不属于功能代码；package-lock.json 没有有效 diff，像是换行/文件状态噪声。我不去回滚它们，避免误伤工作区。




已运行 2 条命令





已继续在 dev-260626-NETWORK-FIX-NEWCODE-STEP2 上修了一轮，未暂存、未提交。
这次重点修了两个直接相关点：
P2P 慢传
[client/file-assets.js (line 837)](D:/Workspace/github/Ltre/file-tunnel/client/file-assets.js:837) 修复 DataChannel 背压等待可能“半卡死”的问题：如果 bufferedAmount 长时间不下降，会主动失败并进入重试/降级，不再无限等。
[client/file-assets.js (line 679)](D:/Workspace/github/Ltre/file-tunnel/client/file-assets.js:679) 同一接收设备不再同时跑多个大完整文件上传，避免两个 20MB 文件抢同一条 WebRTC/SCTP 通道；小文件仍可插队并行。

小图进度条卡死
[client/file-assets.js (line 785)](D:/Workspace/github/Ltre/file-tunnel/client/file-assets.js:785) P2P 发送失败切 relay 时，会主动收尾旧 P2P 进度条，避免旧进度停在某个百分比。
[app.js (line 2057)](D:/Workspace/github/Ltre/file-tunnel/app.js:2057) 文件最终不可用时会清理接收进度条，不再遗留卡死 UI。

已更新 devlog：
[docs/devlog/dev-260628-features.md (line 1)](D:/Workspace/github/Ltre/file-tunnel/docs/devlog/dev-260628-features.md:1)
验证通过：
node --check client\file-assets.js
node --check app.js
node --check server.js
另外工作区里 prompts/dev-prompt-logs/dev-260626.md 是自动追加的对话日志，package-lock.json 也显示有变动但不是我这次功能修改范围；我没有回滚它们。了



问：

- 【这条不提交】看到一直针对一个20MB的文件拉缓存。接收端的进度条面板显示“1个任务 · 1个停滞”，但是总是有一个进度条（没有标注类型，此刻同时在供源端出现一个特别快的socket.io中继类型的进度条从0到100一闪而过）在动，还有模有样地、快速地从短到长一直到100%以为要拉取缓存完毕了，结果进度条消失，再产生一个针对该文件的新的进度条（这次是P2P类型，此刻同时在供源端也出现一个P2P的进度条，双方几乎一致的进度，走得很慢），速度很慢，感觉快要完成时，接收端和供源端的进度条小时，又变成供源端一闪而过一个socket.io中继类型从0到100的进度条，而接收端则是出现一个相对较快的没有标注类型的进度条在增长，过了一会儿，进度条在快满时消失，接着又变成供源端和接收端同时出现几乎相同进度且慢速的P2P进度条。如此往复，接收端的文件就是一直处于无缓存完成的状态
- 看到设备A（供源端）的一个20MB文件有缓存，设备B（接收端）没有该缓存。
    -- 两者好像在上演多种传输阶段的行为，但就是无法让B对该文件完成拉取缓存。
    阶段1：设备A的进度条面板显示“1个任务 · 1个建链中”，接着显示“1个任务 · 1个进行中”，出现一个进度条以P2P慢速速度从0到100（偶有进度条突然变0再立即恢复进度值），与此同时，设备B先的进度条面板显示“1个任务 · 1个建链中”，并迅速变成“1个任务 · 1个停滞”。
    阶段2：设备B进度快要90时，设备A的进度条突然消失，没多久，设备A出现一个进度为0的P2P进度条，中间又变成socket.io relay类型，再变为P2P重来，而之后设备B的进度条也没了，变成等待队列进度条为0（进行中0，排队1）。
    这两个阶段是随机采集到的，不一定重复，也可能是我没有采集到完整的更多阶段。
    -- 甚至见过接收端的进度条面板显示“1个任务 · 1个停滞”，但是总是有一个进度条（没有标注类型，此刻同时在供源端出现一个特别快的socket.io中继类型的进度条从0到100一闪而过）在动，还有模有样地、快速地从短到长一直到100%以为要拉取缓存完毕了，结果进度条消失，再产生一个针对该文件的新的进度条（这次是P2P类型，此刻同时在供源端也出现一个P2P的进度条，双方几乎一致的进度，走得很慢），速度很慢，感觉快要完成时，接收端和供源端的进度条小时，又变成供源端一闪而过一个socket.io中继类型从0到100的进度条，而接收端则是出现一个相对较快的没有标注类型的进度条在增长，过了一会儿，进度条在快满时消失，接着又变成供源端和接收端同时出现几乎相同进度且慢速的P2P进度条。总之，接收端的文件就是一直处理无缓存完成的状态
    -- 有时两者页面刷新几次，好不容易终于给B完全拉取到缓存了

答：

这个现象描述很有价值，尤其是“供源端 P2P 到 100 但接收端停滞/重来”和“relay 一闪而过后又回到 P2P”。我现在按这个轨迹查：发送端是否把“已塞进 DataChannel 缓冲”误判成“对方已收完”，以及 P2P 失败转 relay 时接收端旧传输状态有没有被正确隔离/取消。




已运行 3 条命令


我找到了更像根因的协议问题：完整文件传输没有“传输尝试 ID”。P2P 失败后转 relay 时，旧 P2P 通道可能还在慢慢吐数据，新 relay 又开始写同一个 assetId 的接收缓冲，接收端就会把两条链路的数据互相覆盖/混入，表现就是你看到的 P2P、relay 来回切、快到 90% 又重来，最后始终无缓存完成。这个要补传输尝试隔离。




编辑了 1 个文件


现在每次请求都会带 requestId，不只是强制重试。接着让 P2P 和 relay 各自带不同 attemptId，避免旧链路污染新链路。




编辑了 1 个文件


已引导对话

没卡住，刚才是在定位根因并做协议隔离改动。你这次描述让我确认了一个重要问题：P2P 失败转 relay 时，旧 P2P 数据流可能还没完全停，和新 relay 流写入同一个 assetId 接收缓冲，导致快到 90% 又重来/混乱。我正在补 transferAttemptId，把每次传输尝试隔离开。




编辑了 1 个文件



上下文已自动压缩



不是卡住，是刚好在一个比较容易误判的点上多绕了几圈：我基本确认这是同一个文件的 P2P / relay / 重试尝试互相串包，接收端没有按“本次传输尝试”隔离缓存，所以才会出现 90% 后又回 0、relay 一闪而过、最后文件仍然没完成的怪现象。现在我继续把这个隔离补完整，不会提交。




已运行 3 条命令


我看了一下，发送端已经带上 attemptId 了，但接收端和服务端 relay 还没完整用它隔离，所以现在正补这半截。这个点如果只改发送、不改接收，症状会更像“进度条换着法子演戏”。




已运行 3 条命令


现在的代码里还有一个明显风险：P2P 二进制 chunk 没有携带元数据，只能靠这个 DataChannel 当前属于哪次尝试来识别；所以我会在 start 消息时把 channel 标记成本次 attempt，后续 chunk/complete 都必须匹配，否则丢弃。relay 则直接把 attemptId 透传。




已运行 3 条命令


我会顺手把发送端“100%”也稍微校正：P2P 写完 complete 消息后等 DataChannel 缓冲真正排空，再把供源端进度标成 100。否则供源端看起来完成了，接收端其实还卡在浏览器缓冲或后续校验，会进一步制造错觉。




编辑了 1 个文件


客户端这一段已经补上了：同一个文件的旧 chunk/旧 complete 现在不会再污染当前正在接收的那次尝试。接下来改服务端 relay，把 attemptId 原样转发，否则 socket.io 中继这条兜底链路还是会混在一起。




编辑了 1 个文件


这里还有个细节：relay 的服务端缓存 key 以前对完整文件只用 full，同一文件同一对设备如果重试，就可能把前后两次中继写进同一个 relay 状态。现在完整文件中继会按 attemptId 单独建账，避免服务端也把账算混。




已运行 3 条命令


顺便说明一下：刚才 PowerShell 的红字是本机 profile 执行策略提示，不是项目命令失败。真正需要看的 grep 输出已经出来了。




已运行 4 条命令


改动已经集中在传输核心，没有动 UI 布局和其它功能。现在我先跑语法检查，避免这种底层文件一处括号问题把整页拖白。




已运行 4 条命令


语法检查过了，client/file-assets.js、server/file-assets.js、app.js、server.js 都是 OK。接下来把这次“尝试隔离”的原因和改动补进 devlog，方便后面回看。




已运行 2 条命令


devlog 这个文件在终端里看起来有编码显示问题，但文件本身之前已经是中文内容；我只追加一段新的中文记录，不去重写已有内容，避免把历史说明越改越乱。




正在编辑 1 个文件正在编辑 1 个文件


追加时因为终端看到的是乱码上下文，补丁没匹配上；我换成 EOF 追加方式，只在文件尾部加内容。




编辑了 1 个文件执行了 2 条命令


等等，我又抓到一个更关键的点：20MB 文件走的是多源分片，刚才补的是“完整文件”链路隔离；但你的现象里很多是分片重试，分片也必须带 attemptId。现在继续把多源分片的接收端也隔离，否则大文件还可能继续“快满了又重来”。




已运行 2 条命令


这里真正要解决的就是“分片也有身份”。否则某个 part-7 第一次 P2P 发了一半，第二次 relay 或 P2P 重试又来了，接收端还把它们当成同一个 part-7，最后文件自然就很难完整落库。




编辑了 1 个文件


服务端也要跟上：分片 relay 以前 key 只有 part-3 这种 transferId，如果同一个分片重试，服务端仍可能把两次 relay 混账。现在把 relay 内部 key 扩成 transferId + attemptId。




编辑了 1 个文件


这次才算把 20MB 那条多源分片链路也覆盖到了。现在再跑一遍检查，然后我会把 devlog 再追加一条“多源分片也隔离”的说明。




已运行 3 条命令


检查通过。补丁覆盖点现在包括完整文件和多源分片两类。再追加一条 devlog，然后我会看一下最终 diff 范围，确保没有误动别的东西。




编辑了 1 个文件


我会再做最后一轮语法检查和状态确认。没有启动服务端实测，因为你要求只改代码、不提交，这里主要保证文件级别没有语法炸点。




已运行 4 条命令


我又顺手补一个隐藏坑：强制重试/分片重试之前用的 requestId 不是 req-... 格式，刚加的 attempt 时间排序就读不出来。现在把所有普通拉取和分片拉取都统一生成可排序的 req-... 基础 ID，强制/重试只作为后缀，不影响隔离判断。




已运行 Get-Content -Path client\file-assets.js | Select-Object -Skip 210 -First 90







这里我还发现一个之前就不太对的地方：因为普通请求也生成了 requestId，force: Boolean(requestId) 会变成永远 true。这个会让普通拉取看起来像强制拉取，不一定直接坏，但会污染调度语义。我一起改成 forced 单独判断。




编辑了 1 个文件


现在普通请求还是有 requestId 供 attempt 隔离使用，但不会再伪装成强制请求；真正点击“强制拉取缓存”或重试才会是 forced。这个对调度语义更干净。




已运行 3 条命令


还有 manifest 退回单源下载那条路径也要统一，不然“大文件但只有一个供源”的时候仍可能拿到不可排序的强制 ID。我把这条也补齐。




编辑了 1 个文件


这轮补完了。再检查一次客户端文件，没问题的话我就收口。




已运行 4 条命令


检查全部通过。现在我取一下关键改动位置，方便你后面看代码时能直接定位。




已运行 3 条命令





不是卡住，刚才确实是在补传输核心。现在这一轮改完了，没有暂存、没有提交。
我重点修了你描述的 20MB 文件 P2P/relay 反复切换后无法落库的问题：在 [client/file-assets.js (line 106)](D:/Workspace/github/Ltre/file-tunnel/client/file-assets.js:106) 给每次传输尝试加了 attemptId，完整文件和多源分片都会按本次尝试隔离 chunk/complete，旧 P2P 或 relay 链路迟到的数据会被忽略；在 [server/file-assets.js (line 26)](D:/Workspace/github/Ltre/file-tunnel/server/file-assets.js:26) 也把 relay 状态按 attemptId 区分，避免服务端把同一分片的前后两次中继混账。
另外修了一个隐藏问题：普通请求和强制请求的 requestId/force 语义分开了，避免普通拉取也被当成强制拉取。P2P 发送端现在会等 DataChannel 缓冲排空后再把供源端进度标成 100%，减少“供源端满了、接收端没完成”的错觉。
已检查通过：
node --check client\file-assets.js
node --check server\file-assets.js
node --check app.js
node --check server.js
devlog 也追加到了 [docs/devlog/dev-260628-features.md](D:/Workspace/github/Ltre/file-tunnel/docs/devlog/dev-260628-features.md)。当前仍在 dev-260626-NETWORK-FIX-NEWCODE-STEP2 分支。


问：

    1、多阶段链路反复横跳覆盖(你说的链路污染？)导致很难完整传输的问题，看起来像是解决了，不过发现一个新问题：我提前将8台设备全部处于同一个隧道且全部数据已同步一致，然后把其中一个设备[设备A]退出隧道（这个操作会清空这个设备在这个隧道的所有缓存），再重新进这个隧道，期望向在线的同隧道设备群请求拉取全部缓存，但是当进入该隧道后，发现根本不会从别的设备拉缓存，一条数据都没有同步，也没有出现任何传输进度条！刷新设备A的网页，也是没有一条数据进来，也没有一个进度条产生。
      诡异的是，出现此问题的设备是android chrome，我把另一台设备[设备B]（windows 11 chrome）也退出隧道再重新进来，看到立马就把完整的列表框架拉了进来，然后立即开始向周围设备拉取文件缓存，接着很快就全部同步完成了。
      更诡异的是隔了特别特别久大概有十几分钟的样子，设备A好像"开窍了"，传输列表终于自动加载进来了。（已经确认当时很长一段时间，在线的8台设备都很空闲，没有任何任务进行）。
 
    2、另外一个老问题【检查是不是两台设备之间发新发一条数据（纯文本、富文本）时，如果两者存在已经传输中或卡死的别的进程，是不是因此就传输新的要延迟很久。目前测试发现看起来有这种情况。再切换到一个双方设备都已经完全同步好数据的隧道中，新发一条纯文本/富文本，就相对较快同步好了】看起来没怎么解决，而且还发现个问题（不知道是旧的还是新的），在多台设备繁忙互相传输数据时其中一台发了条纯文本，其它设备没有立即同步这条纯文本，过了一会儿看到个别设备同步到了这条纯文本，但是也看到某个设备重复同步了这条纯文本（即2条一样的，刷新其页面后显示1条）。
    
    3、目前有没有在线心跳机制？是不是有了会更好让同隧道设备发现？因为我发现几台属于同一隧道的且同时在线的设备的功能首页”连接设备“区域，有时各自显示的在线设备数不同。不过再据我观察，隧道里又能随时显示哪些设备在线，所以我现在也搞不清楚到底底层出什么问题了。
    
    4、之前提到的【点击强制拉取缓存, 应该尽可能提升所选文件的传输优先级】，看起来实现效果不明显，偶尔点击强拉后不久看到进入进行中，但是过了不久就被下沉到等待队列，有时点击强拉后队列没有反应。

本次修改的需求以及改动的明细要分门别类整理好章节，存放到项目的docs目录中，具体文件路径是 docs/devlog/dev-260628-features.md （主要以尾行追加的方式写入，如有某些需要修正才以部分修正方式写入）
请切换到分支"dev-260626-NETWORK-FIX-NEWCODE-STEP2"进行此需求的代码改动，但不要暂存，也不要提交。

--------------------------------

    5、之前期望局域网设备越多，传输越靠谱，以为有很多设备可以作为供源者能确保可靠性。但是当前版本实际测试下来，则是设备越少越稳定（尤其是只有两台设备，即时其中一台是0记录的、另一台是大量大文件，也能很快将数据同步完毕;甚至两者同时向隧道丢进大批量大文件+小文件，也基本能很快同步好大部分文件，虽有一些被放弃同步。这是设备2台在线时的优点，值得肯定）。
       我感觉[commit 3f3ca566fab3cadb76c7e4ab1fff16ddf0d85c6c]版本的网络传输逻辑在生产环境响应挺快，即时有时传输很慢，但是进度条基本是稳定的，一般进度条传完就真的结束了，相对会少出现进度条卡住的情况（不是没有，是比现在更少），而且没有我上面说的多阶段链路反复横跳覆盖(你说的链路污染？)导致很难完整传输的问题。
        这么看来，目前的网络传输逻辑还是有点不稳定的。至少[commit 3f3ca566fab3cadb76c7e4ab1fff16ddf0d85c6c]还是有一些可圈可点的东西可以学习。
    

--------------------------------

缓存拉取进度要区分是上传中还是获取中

--------------------------------

【不一定提问】
    我感觉[commit 3f3ca566fab3cadb76c7e4ab1fff16ddf0d85c6c]版本的网络传输逻辑在生产环境响应挺快，即时有时传输很慢，但是进度条基本是稳定的，一般进度条传完就真的结束了，相对会少出现进度条卡住的情况（不是没有，是比现在更少），而且没有我上面说的多阶段链路反复横跳覆盖(你说的链路污染？)导致很难完整传输的问题。
    这么看来，目前的网络传输逻辑非常不稳定。[commit 3f3ca566fab3cadb76c7e4ab1fff16ddf0d85c6c]还是有一些可圈可点的东西可以学习。



--------------------------------



--------------------------------



--------------------------------

 - 有必要做发送一组数据（合辑）的功能，方便用户一键保存等批量操作，不用一个一个来
 - 支持把某条数据、某组数据（合辑）转发到另一个隧道、某个设备（即使创建一个隧道并备注为“和什么设备”）
 
--------------------------------

chatgpt分析项目后，提到
    【
        存储层建议：大文件不要长期堆 IndexedDB，转向 OPFS/分块
        你现在大量使用 IndexedDB 存文件 ArrayBuffer。短期可以，但如果要做“个人存储库”，IndexedDB 会越来越吃力：大文件、部分下载、断点恢复、清理策略、浏览器配额、迁移都会变复杂。
        MDN 对 File System API/OPFS 的描述很契合你的场景：OPFS 是 origin 私有、用户不可见的存储端点，适合持久上传器、断点上传、大文件离线、部分下载、附件缓存等。�
        MDN Web Docs
        建议路线：
        小文本、小图、小文件：IndexedDB 直接存。
        大文件：OPFS 分块存，IndexedDB 只存元数据和 chunk index。
        临时分享队列：IndexedDB。
        长期库：OPFS + hash 去重。
        导出/下载：用 File System Access API 或普通 Blob download。
        桌面 Chrome/Edge：可以考虑 showDirectoryPicker() 做“本地库目录”。
        Android/iOS：优先保持浏览器沙盒库，必要时用原生壳补能力。
    】
    你怎么看，怎么改

--------------------------------

支持消息组

--------------------------------

开发一个更简单、更凸显核心功能的界面
 - 近场发现页
    - 可以参考三星S Share的UI
    - 浏览器里的JS能不能监听某个端口（用于凸显网络特征易被扫描到）
 - 传输记录区域可以有另一个UI表现形式（提供一个按钮打开这个UI），即打开一个资源管理器，文件按方阵或列表平铺，方便管理
 - admin管理页增加网络拓扑图：显示所有在线的设备，以及所加入的隧道（设备与隧道用一条线连接，表示其加入，设备采用设备机型LOGO为图示，隧道用动态的多个广播状同心圆线为图示）
 

--------------------------------
 
【还没提需求】
 
 - 关于 Google Authenticator 管理页身份校验流程，请参考 `USAGE_GUIDE.md`，现在请你实现：增加 TOTP secret 生成、隐藏标记文件、管理会话 Cookie 和高风险管理 API 鉴权。
 - 需求：主页的UI太土气，一股国产拼多多营销味，要看起来专业、高级，你可以提供几套主题可以切换，主题不仅可以更换颜色、质感，还可以变更UI组件的位置。 
 - 需求：新增联系人支持：显示已关注设备的列表页，每个设备有“ℹ️"（profile页）、“📞”（语音通话）、“📢”（对讲机）按钮。当发起以不受隧道会话管理的语音通话时，呼叫者会弹出一个拨号中的浮层（显示被叫者的简单信息），接收者设备会弹出含有“接听”和“拒接”按钮的居中顶部浮层，接听后，双方都会弹出正在通话中的浮层（用于显示两者通话中状态，有通话时长显示）。
 - 需求：设备可联性升级：在设备列表区域，点击某个设备的名称，弹出的浮层中有这个设备的各种信息、profile二维码（关联设备主页链接）、“关注”按钮（点击按钮可以加入联系人）。有了联系人列表，就可以不通过隧道页建立联系。
 - 关于 Google Authenticator 管理页身份校验流程，请参考 `USAGE_GUIDE.md`，现在请你实现：增加 TOTP secret 生成、隐藏标记文件、管理会话 Cookie 和高风险管理 API 鉴权。
 
 - 装好PWA的两台设备用NFC碰一碰，即可进入同一个隧道（选隧道策略1：优先选择两台设备都曾加入过的隧道list，再在这个list里选择最近使用的一个；选隧道策略2：设备A正开着某个隧道，设备B什么都没开，A和B相碰，设备B自动打开A所在的隧道）
 - 难题攻关：A/B双方如果都安装了PWA，那A向B喊话，即使B关着屏幕或应用切出去，只要不关掉隧道所在页面，就能收到A的通信(如语音聊天/对讲机/数据传输/协同编辑)
 - 计划跟telegram打通，具体怎么打通要好好想。例如 把文件转发给bot，根据bot引导，提供隧道暗号，即可让参与隧道的所有设备都收到文件。打算让传输记录区域，支持大部分telegram消息类型。
    telegram bot也可以注册隧道服务端，还可以配置中继，管理更多后台功能

 - 客户端设备之间可以互相请求远程控制，可做的功能有：远程播放音乐、视频等，要好好想
 - 难题攻关：客户端远程投屏到另一个客户端的页面上。
 - 对于这个【为了同时激活设备在多个隧道内的活跃状态，以保持多个隧道内正在进行的数据传输进程不被中断（技术上我不知道你有没有先进的方法实现，我只知道可以利用多个iframe同时加载多个隧道页，让其中一个iframe显示出来，其它的隐藏起来）】需求，你提到【“多个隧道 iframe 同时常驻以保持多隧道活跃”的方案暂未直接实现，因为当前前端全局状态、Socket、IndexedDB 读写、媒体控制和 service worker 共享同一页面上下文，强行 iframe 常驻会带来较大的状态隔离风险】，那就研究下方案【将单隧道运行时拆成可多实例加载的子应用，再由外层 shell 管理多个 iframe】的可行性

本次修改的需求以及改动的明细要分门别类整理好章节，存放到项目的docs目录中，具体文件路径是 docs/devlog/dev-260625-multi-relay-step5.md （请用尾行追加的方式写入）
请切换到分支"dev-260625-multi-relay-NEWCODE"进行此需求的代码改动，但不要暂存，也不要提交。

--------------------------------

【还没提需求】

 - 之前提到了设备之间发通知，那就需要做一个设备收推送通知时的消息列表了

--------------------------------

【还没提需求】

 - 计划新增服务端中继支援，组成一块中继网。
    这样可以让所有参与隧道传输的客户端设备有更多中继节点选择，网络体验更好。
    服务器需要邀请别的服务器中继并得到同意，才能合法加入中继网（在A节点提供的admin管理页面填写被邀请成为中继节点的服务端http/https地址，假定邀请了B节点，那么在B节点提供的admin管理页会收到A的邀请，可以同意或拒绝）。
    为了最大化组建中继网，允许不直接认识的节点之间，通过认识的节点，间接形成中继关系（例如A与B形成中继关系，B与C形成中继关系，那么A和C间接形成中继关系，日后A和C之间可以直接中继，只要物理空间上允许的话）。
    每个中继节点，都运行同一套程序，提供自己的HTTP/HTTPS/WebSocket服务。
 - 计划跟微信打通。例如 看看微信怎么将消息、各种内容通过某些渠道（微信机器人、小程序或其它）最终转发到暗号指定的传输隧道。
 - 计划跟QQ打通。例如 看看微信怎么将消息、各种内容通过某些渠道（微信机器人、小程序或其它）最终转发到暗号指定的传输隧道。
 - 计划跟facebook/threads/messenger/instagram等meta系产品打通，例如 分享时可以选择 传输隧道PWA
 - 计划跟youtube打通。例如分享视频时选择 传输隧道PWA，会弹出提示选择纯链接分享，还是由服务器下载视频后分享到 暗号指定的传输隧道（不过要注意合规，如果日后要做成android app上架到google play的话，就要禁止下载youtube视频）
 
 本次修改的需求以及改动的明细要分门别类整理好章节，存放到项目的docs目录中，具体文件路径是 docs/devlog/dev-260625-multi-relay-step6.md （请用尾行追加的方式写入）
 请切换到分支"dev-260625-multi-relay-NEWCODE"进行此需求的代码改动，但不要暂存，也不要提交。
 
 
--------------------------------
 
