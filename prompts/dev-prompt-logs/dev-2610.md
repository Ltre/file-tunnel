

下期考虑做：

    1、功能首页要支持发送/查看/管理markdown，其中引用的多媒体资源可以存到网盘上，读取时利用网盘API。在PC端要支持左右拆分视图，左编写，右预览。编辑器支持markdown自动补全、语法高亮。
    
    2、隧道的传输记录支持像telegram那样的标签索引，标签直接写在传输记录的备注文本中。标签支持任意非空字符开头结尾，也支持字符串中间含有空格的标签。当需要标签字符串中间含有空格时，可以使用这样的语法 `#[]` 将含有空格的标签字符串括住。建立标签索引应该缓存，当有数据新增/修改/删除时才更新索引。应尽可能减少因建立索引消耗的浏览器资源。
    
    3、   有个需求，挺常用的，看youtube，总有很多想截取保存的画面，一两个还好，但是经常这样截取就感觉工作量很大。
        希望有工具能够随机提取视频中的一些帧（可以设置帧切割粒度、每次取几张，还可以选择性地指定随机取帧的范围（用形如12:34-56:01表示，如需多个前段则用半角逗号隔开）），可记录提取帧日志（带图、毫秒播放时刻），可保存自己想要的帧，可将提取到的帧跟提取帧日志中做相似度比对，也可将提取到的帧跟自己临时上传的图片集比对，可以在提取日志中标记是否已保存，可以对提取日志中按播放时刻或提取时间排序。
        在对同一个视频进行画面提取时，一般不会短时间一次性提取完，所以要创建一个概念“提取会话”，以方便管理提取日志、已保存帧等，也可避免同一个视频产生多次相同截取帧而不易察觉。
        
        数据库直接用sqlite, 文件直接保存到服务器本地，操作使用web打开 。
        （不用下载视频到本地，可以配置好yt-dlp(包含cookie以及deno支持)。当然有条件缓存部分视频片段用以重复利用更好。）
    
        为了减少使用yt-dlp抓取youtube，可以采用现有的网盘API（或是采用网盘基础之上的S3协议存储）来长久缓存完整视频（有效期一年），之后所有的片段截取/帧截取/选定范围内截取任意内容等操作，都基于网盘存储的视频。

    4、颜文字库、形象文字库

    5、开放telegram网盘API（其实应该已经开发好API了），用于MusicoletWeb系统的需求【考虑复用Dr2T bot管理的telegram网盘系统，来保存歌曲文件，而且要以歌曲文件的原手机存储文件路径来创建在telegram网盘中的虚拟目录。】
    
    6、后台sns-dl页要新增网易云音乐下载支持（这个也要考虑提示用小号cookie下载）
    
    7、SNS包括YT截取idol静态图或短clip，可以打标签，也可以按人物/组织区分，存储采用telegram网盘API。
    
    8、本系统的基础数据库，也要采用telegram网盘备份和恢复。
    
    9、yt-dlp下载文件到s3存储中（即保存到以telegram网盘API为基础的S3存储中）
    
------------------------------------------------

遥遥无期：
    
    1、youtube premium 下载页 日后会跟musicolet系统联动，看看怎么为将歌曲任务所打标签，同步设置到musicolet app中具体歌曲的所加入歌单。
    
    2、目前客户端大多数数据是跟域名绑定的，如果服务器域名改了，就找不到数据，有什么办法能够让用户的客户端在切换到新的域名后还能继续使用隧道内的原有数据。
    
    3、客户端设备之间可以互相请求远程控制，已经做了：远程切换图片/音乐/视频，要好好想还能做什么合适
    
    4、难题攻关：客户端远程投屏到另一个客户端的页面上。
    
    5、打开功能首页有时感觉手机会发热，不知道是不是用了音乐播放器还是其它的预览操作。前面还操作了网盘的目录移动。还是有隧道传输方面的一些异常资源占用，推测不出来到底哪在消耗资源。
    
    
    6、Drop2Tunnel客户端linux-cli版（可用于服务器作为客户端运行）
    
    7、移植到android/ios/ipadOS
    
    8、【X】装好PWA的两台设备用NFC碰一碰，即可进入同一个隧道（选隧道策略1：优先选择两台设备都曾加入过的隧道list，再在这个list里选择最近使用的一个；选隧道策略2：设备A正开着某个隧道，设备B什么都没开，A和B相碰，设备B自动打开A所在的隧道；选隧道策略3：设备A通过一个按钮进入到NFC寻找模式界面，不论设备B有没有打开任何页面，都以A的隧道为准）。
    
    9、研究用户分享任何其打开的网页，看看PWA能不能读取到用户运行网页的所有离线数据，采集到后，按照原网页的结构重新复刻，生成带有完整数据的本地可浏览网页，并发送到隧道。

    10、支持各种SNS将帖子等内容转发到隧道。
    
    11、支持将完整版网页打包发送到隧道，直接在隧道里打开（现已实现网页zip包，但和网络上直接打包的zip不太一样）
    
    12、支持在隧道中运行、发送小程序
    
    13、计划新增服务端中继支援，组成一块中继网。
        这样可以让所有参与隧道传输的客户端设备有更多中继节点选择，网络体验更好。
        服务器需要邀请别的服务器中继并得到同意，才能合法加入中继网（在A节点提供的admin管理页面填写被邀请成为中继节点的服务端http/https地址，假定邀请了B节点，那么在B节点提供的admin管理页会收到A的邀请，可以同意或拒绝）。
        为了最大化组建中继网，允许不直接认识的节点之间，通过认识的节点，间接形成中继关系（例如A与B形成中继关系，B与C形成中继关系，那么A和C间接形成中继关系，日后A和C之间可以直接中继，只要物理空间上允许的话）。
        每个中继节点，都运行同一套程序，提供自己的HTTP/HTTPS/WebSocket服务。
        
    14、看看有什么实用/有意思的功能，能够充分利用telegram存储。	
	
            初答：
                `prompts/ideas/Make full use of Telegram storage 260920.md`
                
    
------------------------------------------------   
    
    