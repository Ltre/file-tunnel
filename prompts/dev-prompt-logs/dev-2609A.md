

【待提问】问：

    
    16、目前的网盘文件托管频道是public状态，想要改成私有链接，不过我目前看到频道里的文件caption里的channel写的是public的"艾特username"格式，你调研下网盘底层数据存储是不是保存频道的chat_id，如果不是，那以后要改成频道的chat_id关联。不过现在已有的数据是不是也要兼容性处理，写个数据自动修复脚本？（这条你可以不用立即处理，而是先给出方案，方案简明扼要，不要浪费token写太详细）。
    
    详见docs/devlog/dev-2608C-features.md中的"26.5 私有频道与 Telegram OIDC 调研方案"部分。


    99、后台新增功能/video-transcode ，用于视频转码。目前初期先用ffmpeg，支持自定义转码方案。所谓转码方案，就是定义一到多条指定一系列相对固定参数格式的命令。通过调用转码方案，可以减少每次重写复杂的命令参数。
    
    系统会根据方案的定制情况，提供不同的ffmpeg任务提交表单。
    
    方案探讨详见：prompts/ideas/ffmpeg-programs-design-260910.md
    
    约定一些方案语法：
        `{$INPUT_FILE}` - 输入文件（如果通过WEB则先上传后再保存到临时路径）
        `{$CRF}` - ffmpeg的crf参数
        `{$START_TIME}` - ffmpeg的ss参数
        `{$END_TIME}` - ffmpeg的to参数
        `{$VFSC}` - ffmpeg的"-vf scale="参数
        `RENAME(({$参数名}))` - 这是一个非输入参数。该语法根据情况修改参数值，当和INPUT_FILE组合时，会根据输出视频的合适扩展名为准修改输入文件的扩展名，并指定保存到统一的输出路径。
        `AND` - 参数连接"且"
        `OR` - 参数连接"或"
        `[[`和`]]` - 参数可选符号，在命令行中用，也可在条件式里用。
        `((`和`))` - 参数逻辑组合符号，括号里用AND或OR隔开参数。仅在条件式里用。
        `{$参数名} NEED {$参数名}` - 参数条件示例1
        `{$参数名} NEED (({$参数名} AND {$参数名}))` - 参数条件示例2
        `(({$参数名} AND {$参数名})) NEED {$参数名}` - 参数条件示例3
        `(({$参数名} AND {$参数名})) NEED (({$参数名} OR {$参数名}))` - 参数条件示例4

    方案示例1：
        命令行: 
            `ffmpeg -i {$INPUT_FILE} -c:v libx265 [[-crf {$CRF}]] -c:a copy -movflags +faststart RENAME(({$INPUT_FILE}))` # 转成h265
        条件式: 
            无
    方案示例2：
        命令行：
            `ffmpeg [[-ss {$START_TIME}]] [[-to {$END_TIME}]] -i {$INPUT_FILE} -c:v libx265 [[-vf scale={$VFSC}]] -c:a copy -movflags +faststart RENAME(({$INPUT_FILE}))` # 截取视频，并选择性缩放
        条件式: 
            `{$INPUT_FILE} NEED (({$START_TIME} OR {$END_TIME}))`

    
	
	1、开放telegram网盘API，用于MusicoletWeb系统的需求【考虑复用Dr2T bot管理的telegram网盘系统，来保存歌曲文件，而且要以歌曲文件的原手机存储文件路径来创建在telegram网盘中的虚拟目录。】



	2、youtube premium 下载页 日后会跟musicolet系统联动，看看怎么为将歌曲任务所打标签，同步设置到musicolet app中具体歌曲的所加入歌单。
	
	1、目前客户端大多数数据是跟域名绑定的，如果服务器域名改了，就找不到数据，有什么办法能够让用户的客户端在切换到新的域名后还能继续使用隧道内的原有数据。




    3、客户端设备之间可以互相请求远程控制，可做的功能有：远程播放音乐、视频等，要好好想
    4、难题攻关：客户端远程投屏到另一个客户端的页面上。

    
    
---------------------------

    【已提】2、需求：当向设备发起以不受隧道会话管理的语音通话时，呼叫者会弹出一个拨号中的浮层（显示被叫者的简单信息），接收者设备会弹出含有“接听”和“拒接”按钮的居中顶部浮层，接听后，双方都会弹出正在通话中的浮层（用于显示两者通话中状态，有通话时长显示）。
 
 
-------------------------------


【未提问】


Drop2Tunnel客户端linux-cli版（可用于服务器作为客户端运行）

移植到android/ios/ipadOS
 

--------------------------------

【还没提需求】

 - 【X】装好PWA的两台设备用NFC碰一碰，即可进入同一个隧道（选隧道策略1：优先选择两台设备都曾加入过的隧道list，再在这个list里选择最近使用的一个；选隧道策略2：设备A正开着某个隧道，设备B什么都没开，A和B相碰，设备B自动打开A所在的隧道；选隧道策略3：设备A通过一个按钮进入到NFC寻找模式界面，不论设备B有没有打开任何页面，都以A的隧道为准）。
 - 研究用户分享任何其打开的网页，看看PWA能不能读取到用户运行网页的所有离线数据，采集到后，按照原网页的结构重新复刻，生成带有完整数据的本地可浏览网页，并发送到隧道。
 - 支持各种SNS将帖子等内容转发到隧道。
 - 支持将完整版网页打包发送到隧道，直接在隧道里打开
 - 支持在隧道中运行、发送小程序


--------------------------------

【还没提需求】

 - 之前提到了设备之间发通知，那就需要做一个设备收推送通知时的消息列表了

--------------------------------

【还没提需求】

 - 计划新增服务端中继支援，组成一块中继网。
    这样可以让所有参与隧道传输的客户端设备有更多中继节点选择，网络体验更好。
    服务器需要邀请别的服务器中继并得到同意，才能合法加入中继网（在A节点提供的admin管理页面填写被邀请成为中继节点的服务端http/https地址，假定邀请了B节点，那么在B节点提供的admin管理页会收到A的邀请，可以同意或拒绝）。
    为了最大化组建中继网，允许不直接认识的节点之间，通过认识的节点，间接形成中继关系（例如A与B形成中继关系，B与C形成中继关系，那么A和C间接形成中继关系，日后A和C之间可以直接中继，只要物理空间上允许的话）。
    每个中继节点，都运行同一套程序，提供自己的HTTP/HTTPS/WebSocket服务。

 
--------------------------------

