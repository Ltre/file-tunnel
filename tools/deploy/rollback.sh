#!/usr/bin/env bash
set -euo pipefail
echo "Rollback is intentionally a server-side operation for now." >&2
echo "Recommended layout: /opt/drop2tunnel/releases/<build> and current -> releases/<build>." >&2
exit 2
