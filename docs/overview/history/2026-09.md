# 2026-09：Telegram 网盘、网页工坊、转码与内容管理收敛

## 1. 9 月初：网盘成为完整子系统

9 月 3-4：

- 通用 Passkey 用户；
- App access token；
- disk_space；
- Operation；
- centered loading；
- multi-select；
- directory tree；
- public share；
- admin audit；
- browser cache；
- cross-tunnel。

网盘从“Telegram 上传 UI”变成逻辑文件系统。

## 2. Telegram Logical Multipart

云 Bot API 的单文件下载限制推动：

- 20,000,000 byte physical part；
- logical file；
- part metadata；
- merge on read；
- client still sees one file。

随后增加：

- upload plan；
- thumbnail；
- file_id reuse；
- Range cache。

## 3. 上传 pipeline 与 Recovery

9 月 8-9：

- browser produce parts；
- Node staging；
- Telegram FIFO；
- backpressure；
- Operation progress；
- rollback；
- restart manifest recovery。

关键设计：

> 远程 Telegram 网络阶段和本地逻辑 file commit 分开。

这为后来 SQLite WAL 讨论奠定基础。

## 4. 网盘 UI 多轮修复

9 月 11-16 重点：

- preview/media；
- thumbnail；
- selection；
- bottom menu；
- breadcrumb drag；
- task resume；
- mobile single tap；
- inertia；
- theme integration。

宽屏 context menu / history 在 9 月 17 以后仍有复现，说明它是持续高风险 UI。

## 5. JSON → SQLite WAL 设计

9 月 15 之后用户识别出：

- drive index；
- directories；
- auth；
- operations；
- shares；
- cache owners；

多个 JSON 强关联。

最终设计方向：

- 未来 `disk.sqlite`；
- native SQLite WAL；
- Repository/Adapter；
- short transaction；
- Telegram network outside transaction；
- Saga/recovery；
- old test JSON can be discarded。

截至源码锚点 Commit `b422e...` 未实施。

## 6. Bilibili 与主题

260916-1：

- SNS Bilibili；
- Bilibili cookie；
- disk task restore；
- mobile inertia；
- theme entry refactor。

## 7. Web ZIP / Web Workshop

260916-2 起新增。

演进：

### 初始

- `.html.zip`；
- draft；
- preview；
- publish；
- edit permission；
- notification。

### 260917

- tree CRUD；
- duplicate；
- independent preview；
- existing-file update；
- publish version；
- topbar/route interactions。

### 260917-2

从 srcdoc/blob 转向：

- Service Worker virtual runtime；
- normal relative paths；
- runtime ID；
- standalone page。

### 260918

处理：

- Runtime actual file exists but wrapper Cannot GET；
- target Telegram menu；
- existing ZIP update；
- Telegram content manager。

### 260921-22

处理：

- external JS；
- malformed `<script src>`；
- SW controller takeover；
- runtime compatibility protocol；
- official guide。

### 260923-24

- minimize；
- editable package filename moved from topbar to content；
- whole-row tree toggle；
- hide outer frame；
- import tunnel resources。

## 8. Video Transcode

260916 起 schema-driven：

- profile；
- params；
- steps；
- FFmpeg；
- task queue；
- help page。

260917：

- built-in H.265；
- CRF 28；
- optional preset；
- cache cleanup；
- FFmpeg output expansion state。

260924：

- SNS/YouTube completed video task → transcode；
- secure sourceKind/taskId；
- hard link/copy fallback；
- no browser re-upload。

## 9. Audio Track Repair

最初是 synchronous action，随后改为：

- background queue；
- progress；
- polling；
- cached result；
- separate output。

最终按钮限制：

- only section-cut tasks；
- not music-only。

## 10. Telegram Content Manager

260918 开始后台 Chat 浏览。

260921：

- three-day cache；
- anchor pagination；
- text+media send。

260922：

- new group/channel updates；
- webhook subscriptions；
- group/channel management；
- private user stop service。

## 11. 260924 人工验收

用户在 `prompts/dev-prompt-logs/dev-2609.md` 明确记录：

- 下载任务前往转码：通过；
- hide web ZIP frame：通过；
- disk local drag upload：通过；
- full transfer timestamp：通过；
- wide 3-column equal height：勉强通过；
- workshop import tunnel resources：通过。

文档应把“勉强通过”保留，不能升级成“完全解决”。

## 12. 当前基线结束点

本轮首轮整理的源码锚点 Commit：

`b422e438fe50f78fdacd84ac1dff34a30a3d43ba`

提交标题：

`feat: 接通下载任务转码、网页 ZIP 资源导入与网盘拖放上传`

本 overview 首轮扫描以此为源码锚点。

用户在此之后提出的：

- Android 平板三栏高度；
- transcode output extension 新要求；
- Web ZIP manifest/fullscreen overlay；
- workshop media drag insert；
- Telegram Drive collaborative directory/file；

不属于这个源代码锚点已经实现的事实。未来这些改动合入 `dev/doc-overview` 对应源代码后，应以 `VERSION.md` 增量更新。
