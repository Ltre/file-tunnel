# dev/2608C-step1：隧道审计与独立缓存节点

日期：2026-08-24  
分支：`dev/2608C-step1`  
状态：代码修改完成，未提交、未暂存

## 一、任务恢复与现状检查

本轮从网络中断后的工作区继续。恢复时，后台隧道统计、VClient 控制面、缓存进程和 `/vclient` 页面已经有初步实现，但复核协议路径和进程生命周期后，仍发现以下不能算“完成”的缺口：

1. 普通历史消息已入库，但 `file-asset` 直接文件协议可以绕过传输记录，导致后台少算文件和记录。
2. 富文本编辑器附件与跨隧道转发没有完整保留文件名、MIME、大小等元数据。
3. 未知记录的删除请求会生成没有原始记录的假删除留痕；晚到的普通回调又可能把已删除隧道恢复为未删除状态。
4. 老版本 `devices` 数据尚未迁移到历史成员表，历史设备数会漏掉升级前仍保留的成员。
5. VClient 文件接收在供源设备掉线或中继无后续数据时可能无限等待。
6. 新控制进程接管后，旧进程已建立的数据连接仍可能留在隧道中；管理员停止缓存节点时，如果控制进程离线，也不能保证旧数据连接立即退出。
7. 缓存文件只校验大小，同尺寸损坏文件可能被重新宣告为可用供源。
8. 浏览器会把 VClient 当作普通 WebRTC 对端发起连接，而第一阶段 VClient 只支持 Socket.IO 文件中继，会产生无效协商和超时。

因此，本轮不是只确认已有按钮，而是按“每条协议是否留痕、每种断线是否收敛、缓存内容是否可信”继续完成剩余工作。

## 二、需求 3：持久化隧道审计

### 2.1 数据模型

服务器基础设施 SQLite 增加或完善以下持久化数据：

- `tunnels`：隧道 ID、创建时间、最后活动时间、删除时间、访问配置和备注。
- `tunnel_members`：设备首次出现、最后出现、设备类型，用于历史总设备数；升级时从老 `devices` 表补迁移。
- `transfer_records`：传输记录快照、发送设备、记录类型、创建/更新时间、删除状态。
- `transfer_files`：记录关联的文件 ID、文件名、MIME、声明大小及来源类型。
- `file_assets`：直接文件资产的当前元信息与供源状态。
- `asset_transfer_events`：直接文件、富文本附件等资产协议的不可变事件快照，记录宣告、请求、客户端完成、服务端中继完成或失败等事件。

`asset_transfer_events` 保存事件发生时的文件名、MIME、声明大小、实际中继字节数、源/目标设备、传输 ID、请求 ID、传输方式和时间。后续同一文件的元数据变化不会覆盖已经发生的事件，便于追溯当时看到的内容。

### 2.2 后台指标口径

`/api/sessions` 和后台隧道列表现在提供并显示：

- 隧道 ID；
- 活跃设备数，并区分真实设备和在线缓存节点；
- 历史总设备数，并区分真实设备和缓存节点；
- 最后活动时间；
- 创建时间；
- 传输记录数；
- 传输文件数；
- 唯一文件数（额外辅助审计）；
- 文件占用总大小。

文件总大小按隧道内唯一文件 ID 取已知最大大小后求和，避免同一文件在多条记录中重复引用而重复累计。没有标准历史记录、但通过直接资产协议宣告过的文件，会以 `direct-file:<file_id>` 合成一条只读审计记录，同时计入文件数和总大小，堵住直接协议旁路。

已删除隧道保留审计行，不因普通活动回调被复活。只有确实存在的记录才能被标记删除，未知 ID 不再生成假 tombstone。

### 2.3 前台与服务端上报覆盖

逐条检查并补齐了以下路径：

| 路径 | 审计来源 | 完成情况 |
| --- | --- | --- |
| 文本、普通文件、文件集合、富文本历史 | 前台 `history-sync` / 服务端历史广播 | 记录和关联文件均持久化 |
| 跨隧道转发 | 服务端转发前验证发送设备属于源隧道 | 保留源资产的文件名、MIME、大小 |
| 直接 `file-asset` 宣告与请求 | 服务端 Socket.IO 事件 | 记录不可变资产事件 |
| 直接文件中继完成 | 服务端按实际转发范围计数 | 记录 `relay-completed` 和实际字节数 |
| 富文本编辑器资产 | 编辑器资产宣告、请求、中继完成 | 记录不可变资产事件，保留 `editor` 类型 |
| 删除记录 | 已有记录的删除事件 | 只更新真实存在记录的删除状态 |

服务端中继的 `bytes_transferred` 是服务端实际观察的中继字节数。P2P 内容本身不经过服务器，服务器只能保存客户端上报的文件身份、声明大小与记录元数据；这是协议事实，不应把客户端声明伪装成服务端做过内容取证。

### 2.4 历史边界

本实现保证升级后的所有受支持协议路径进入审计存储，并迁移老设备成员数据。升级前从未上报、也未留在旧数据库或当前客户端历史中的传输，服务器没有事实来源，无法事后凭空恢复；后台数据从可迁移的旧数据和升级后的新流量开始完整累计。

隧道审计用于运营排查和线索定位，不等同于司法级内容鉴定。若后续需要更强取证能力，应增加服务端内容留存策略、管理员访问审计、可信时间戳、保留期限和隐私合规方案。

## 三、需求 4：独立 VClient 缓存节点

### 3.1 进程架构

缓存节点实现为独立入口 `vclient/index.js`，通过 `npm run vclient` 启动，不嵌入现有 HTTP 服务进程。它在网络协议层是特殊客户端：

- 使用单独的 `/vclient-control` 控制连接获取管理员启用的隧道分配；
- 每个已启用隧道建立隔离的数据连接，以 VClient 设备身份加入隧道；
- 同一个物理 VClient 进程同时承载多个隧道，不为每个隧道重复拉起 OS 进程；
- 每个隧道使用独立稳定 UUID、独立目录和独立资产索引，避免跨隧道文件串用；
- 第一阶段只实现 Socket.IO 文件/编辑器资产中继，不实现 WebRTC DataChannel，符合需求文档的阶段选择。

控制令牌默认由主服务生成在 `.tunnel-data/vclient-control.token`。VClient 支持：

- `VCLIENT_SERVER_URL` 或 `--server`：主服务地址；
- `VCLIENT_DATA_DIR` 或 `--data-dir`：独立缓存目录；
- `VCLIENT_TOKEN` / `VCLIENT_CONTROL_TOKEN`：直接令牌；
- `VCLIENT_TOKEN_FILE` / `VCLIENT_CONTROL_TOKEN_FILE` 或 `--token-file`：令牌文件；
- `VCLIENT_CONTROL_NAMESPACE`：控制命名空间，默认 `/vclient-control`。

在同机默认目录运行时，可直接执行：

```powershell
npm run vclient
```

跨主机部署时，至少需要明确配置主服务 URL、控制令牌或令牌文件，以及持久缓存目录。要实现“全天在线”，应由 systemd、PM2、Windows 服务或同等进程管理器分别守护主服务和 `npm run vclient`；后台按钮只保存/撤销隧道分配，不会违反“独立进程”要求而在主服务内部偷偷派生子进程。

### 3.2 管理后台

每个隧道现在有以下状态和操作：

- 未启用时显示“启用缓存节点”；
- 启用后变为“停止缓存节点”；
- 有启用状态或缓存历史时显示“查看缓存节点”，链接到 `/vclient?sessionId=<隧道ID>`；
- 显示独立进程在线状态、隧道节点状态、已缓存文件数、已缓存字节数和错误信息。

若管理员启用时独立进程尚未连接，启用意图仍会持久化，页面明确提示启动 `npm run vclient`。进程稍后上线会自动领取所有已启用隧道。停止操作即使在控制进程离线时也会强制断开该隧道已有的 VClient 数据连接。

### 3.3 `/vclient` 页面

新页面仅提供当前阶段要求的只读传输记录管理视图：

- 独立进程在线状态；
- 当前隧道缓存状态；
- 已缓存文件数和大小；
- 文本、普通文件、文件集合、富文本记录列表；
- 集合和富文本附件详情；
- 每个资产的等待供源、请求、重试、接收、完成、失败、中断等状态；
- 分页与定时刷新。

直接资产协议形成的合成审计记录也会出现在此页，因此不会只显示标准历史消息而漏掉直接传输。

### 3.4 生命周期与内容完整性

本轮补齐以下异常收敛：

- 接收任务有空闲超时；供源设备离开、服务端报告不可用或连接中断时，关闭临时文件并释放下载槽，再进入有限重试。
- 文件和编辑器中继在任一端断线时通知对端，不再无限等待。
- 新 VClient 控制进程接管时，旧控制连接收到 `superseded` 并停止全部隧道；控制连接意外丢失时，数据隧道同样暂停，避免失控驻留。
- VClient 心跳会重新核对管理员期望状态；已停止隧道的旧连接会被服务端断开。
- 停止进程时短暂等待正在上传的数据收敛，然后安全退出。
- 缓存文件首次或重启后作为供源前按 SHA-256 校验。即使文件大小一致，只要摘要不匹配也会隔离为 `.corrupt-*` 并重新下载，避免损坏缓存污染其他设备。
- 浏览器识别 `clientType: vclient`，不再对第一阶段 VClient 发起无效 WebRTC 协商；文件与编辑器附件走 Socket.IO 中继。

## 四、修改范围说明

核心修改集中在：

- `server/infra-store.js`：持久审计模型、统计和 VClient 状态；
- `server/file-assets.js`、`server.js`：资产协议留痕、断线收敛和 VClient 数据连接鉴权；
- `server/vclient-control.js`：独立进程控制面和管理员 API；
- `vclient/`：独立多隧道缓存进程、缓存索引、校验和重试；
- `pages/admin.html`、`pages/vclient.html`：后台入口及只读记录页；
- `app.js`：浏览器对特殊客户端的最小协议兼容；
- `tests/infra-store-audit.test.cjs`、`tests/vclient-runtime.test.cjs`、`tests/features-2608C.test.cjs`：回归覆盖。

需求 3、4 横跨持久化、服务端协议、独立进程和两个页面，无法只改一个文件。实现避免重写已有传输协议，优先复用现有历史同步和 Socket.IO 文件资产事件。

## 五、验证记录

### 5.1 语法与页面脚本

已通过：

```text
node --check server.js server/infra-store.js server/file-assets.js server/vclient-control.js vclient/runtime.js vclient/cache-store.js app.js
```

`pages/admin.html`、`pages/vclient.html` 的内嵌脚本已抽取并用 `new Function` 完成语法检查。

### 5.2 自动化回归

已执行：

```text
node --test tests/*.test.cjs
```

结果：102 个测试全部通过，0 失败、0 跳过。

重点覆盖：

- 审计数据库关闭后重开仍保留统计；
- 老设备成员迁移；
- 直接资产计入记录、文件和总大小；
- 不可变事件快照；
- 未知删除不造假记录、晚到回调不复活已删除隧道；
- 一进程多隧道及目录隔离；
- 同尺寸缓存损坏的 SHA-256 隔离；
- 接收空闲超时和供源离开后的任务释放；
- 控制连接被替换/断开后的隧道暂停；
- 富文本附件使用编辑器协议并保留审计元数据；
- 浏览器不对 VClient 发起 WebRTC；
- Admin 指标/按钮和 `/vclient` 只读页面静态契约。

### 5.3 浏览器验收说明

尝试使用内置浏览器访问隔离的本地模拟服务时，本地和局域网地址被浏览器安全策略阻止；策略同时禁止用 `data:` 页面绕过，因此没有伪造“真实浏览器已通过”的结论。页面 DOM、内嵌脚本和交互契约已由自动化覆盖，实际部署后仍建议用管理员登录态做一次人工烟雾测试：启用缓存节点、启动独立进程、查看 `/vclient`，再停止节点并确认设备退出。

### 5.4 Git 状态

- 当前分支：`dev/2608C-step1`；
- 本轮没有执行 `git add`；
- 本轮没有执行 `git commit`；
- `git diff --check` 通过。

工作区还包含同一批未提交的 YouTube Premium 需求修改，以及用户已有的 `prompts/dev-prompt-logs/dev-2608B.md` 修改；本轮没有回滚、提交或暂存这些内容。

## 六、建议提交日志

Title：

```text
feat: 完善隧道审计与独立缓存节点
```

Description：

```text
- 持久化隧道成员、传输记录、文件元数据及直接资产协议事件，补齐后台审计统计和旧成员迁移
- 新增独立 VClient 多隧道缓存进程、Admin 启停/查看入口及 /vclient 只读记录页
- 完善中继断线收敛、控制面单实例、缓存 SHA-256 校验和浏览器特殊客户端兼容
- 增加审计持久化、缓存完整性、进程生命周期及页面契约回归测试
```

---

## 七、YouTube Premium 封面状态与专辑作者补丁（2026-08-24）

### 7.1 本轮约束

本轮只处理两个已明确的小问题：

1. “编辑歌曲元信息”重新打开时，预览必须来自歌曲成品中的内嵌封面，而不是任务列表使用的方形参考封面；编辑器与 Telegram 转发可以复用封面数据，但不能覆盖或丢失方形封面。
2. 指定歌曲 `https://music.youtube.com/watch?v=XnWxihjgR-E` 的专辑作者为空，需要在有专辑证据时恢复真实专辑作者，同时继续保护“群星”和未知专辑作者场景。

修改控制在 `pages/youtube-premium-dl.html`、`server.js` 和两份相关回归测试内，没有重构 YouTube Premium 服务或跨文件改写整套下载流程。

### 7.2 内嵌封面读取根因

歌曲封面写入函数只替换 M4A 成品里的 attached picture，故意不调用 `setCoverPath`；这是为了保留下载阶段已经抓取、裁切好的方形封面。此前编辑浮层初始化却直接使用公开任务字段 `task.cover`，而该字段正是外层列表和 Telegram“正方形”选项使用的 `coverPath`，所以自定义内嵌封面虽然写入成功，重新打开浮层仍会显示旧方形图。

修复方式：

- 新增只读 `GET /api/youtube-premium/tasks/:taskId/song-cover`。
- 服务端用 ffprobe 找出 `attached_pic` 流，再用 ffmpeg 抽取为临时 JPEG；响应完成立即清理临时文件。
- 编辑浮层打开后并行读取该接口，初始预览只显示成品文件内嵌封面；读取失败时显示明确错误，不再用任务方形图冒充。
- 保持当前封面时不上传图片，也不会触发二次封面写入。

### 7.3 与 Telegram 封面数据互通

页面增加按任务 ID 隔离的轻量封面资源槽，分别保存：

- `squareUrl`：既有方形封面 URL；
- `originalBlob/originalDataUrl`：按需取得的原尺寸图；
- `embeddedBlob/embeddedDataUrl`：当前歌曲文件内嵌封面；
- `uploadedBlob/uploadedDataUrl`：两个浮层共享的用户上传图。

两处功能共享图片资源，不强行同步 Base/Pro/Ultimate 三个独立下拉框的选择值：

- 元信息编辑器上传的自定义图，可以在 Telegram 的“使用自定义封面”中直接复用。
- Telegram 上传的自定义图，可以在元信息编辑器选择“上传...”时复用；同时提供“重新选择上传文件”按钮。
- Telegram 打开时也会读取歌曲当前内嵌封面，页面重新加载后仍可把已经写入歌曲的封面当作自定义封面使用。
- 原尺寸图在两处之间缓存复用，避免同一页面会话中重复抓取。
- 自定义图、内嵌图、原尺寸图从不写入或替换 `squareUrl/coverPath`；因此上传自定义封面后，原方形裁切结果仍保留，选择“正方形”不需要重新抓取和裁剪。

### 7.4 指定歌曲专辑作者根因

使用当前 yt-dlp 对公开链接做了真实元数据复现：

```text
视频 ID：XnWxihjgR-E
歌曲：ボーイフレンド
专辑：サマー・イン・ブルー
单曲艺术家：Yuri Kunizane
频道：Yuri Kunizane - Topic
album_artist：原始单曲响应没有该字段
```

随后遍历 YouTube Music 专辑搜索的首个匹配专辑，确认：

```text
专辑：Album - Summer in Blue
播放列表：OLAK5uy_n2VlxbpMqN9Jm-2up1Oig_cNzSjJf81ew
目标歌曲位于第 3 首
该专辑条目均来自 Yuri Kunizane - Topic
```

现有新鲜解析逻辑能够用这组专辑条目确认 `Yuri Kunizane`，而不是从单曲艺术家字段盲猜。真正遗漏发生在持久元数据缓存的再次富化：只要缓存中的 Track 和 Disc 已齐全，函数就提前返回，即使 `album_artist` 仍为空；而且再次富化的输入没有携带专辑名，无法发起专辑匹配。

修复方式：

- 提前返回条件改为 Track、Disc、Album artist 三者都存在。
- 重新富化时补传专辑名、已有专辑作者和单曲艺术家上下文。
- 将专辑遍历得到的 `album_artist` 同时回填到 `songMetadata.album_artist` 和 `referenceInfo.albumArtist`。
- 仍然不把单曲艺术家直接复制为专辑作者；只有显式 `album_artist`、编译/群星证据或命中包含目标视频的专辑条目时才填写。

这使旧缓存中的空专辑作者也会在下次加载格式时得到修复，不要求管理员手工删除整个持久缓存。

### 7.5 验证

专题回归：

```text
node --test tests/youtube-premium.test.cjs tests/youtube-album-artist-regression.test.cjs tests/telegram-cover-regression.test.cjs tests/features-2608B.test.cjs
```

结果：49/49 通过。

全量回归：

```text
node --test tests/*.test.cjs
```

结果：103/103 通过，0 失败、0 跳过。

另外用临时文件执行了真实媒体烟雾测试：生成一段带 `attached_pic` 的 M4A，按新接口相同的 ffprobe 流索引和 ffmpeg `-map 0:<stream.index>` 方式抽取封面，成功得到 JPEG；临时目录随后已清理。

新增/更新覆盖：

- 编辑浮层初始化调用歌曲内嵌封面接口，且不再把 `task.cover` 当作当前内嵌封面。
- 服务端只提取 `attached_pic`，并保持封面写入逻辑不调用 `setCoverPath`。
- 上传封面在元信息编辑器与 Telegram 之间按任务复用。
- 方形封面使用独立槽位，不被自定义封面覆盖。
- Track/Disc 已存在但专辑作者为空的旧缓存仍会继续富化。
- 指定视频的专辑条目 fixture 能确认 `Yuri Kunizane`，同时保留群星和歧义保护。

### 7.6 本补丁建议提交日志

Title：

```text
fix: 修复歌曲内嵌封面预览与专辑作者回填
```

Description：

```text
- 编辑歌曲元信息时直接读取成品内嵌封面，并与 Telegram 转发共享原图和自定义封面资源
- 独立保留方形裁切封面，避免上传自定义图片后重复抓取和裁剪
- 修复旧缓存 Track/Disc 已齐时跳过专辑作者富化的问题，补全指定 YT Music 歌曲的专辑作者
- 增加内嵌封面、共享封面状态及缓存专辑作者回填的回归测试
```

---

## 八、服务器 Shell 推送、专辑核验提速与 Telegram 内嵌封面（2026-08-25）

### 8.1 任务边界与现状核对

本轮在 `dev/2608C-step1` 上继续修改，不提交、不暂存，并保留用户已有的 `prompts/dev-prompt-logs/dev-2608B.md` 工作区修改。需求分为四条独立链路：

1. 从服务器 Shell 指定本地文件/合辑、备注、隧道短码或长 ID，推送给该隧道的 VClient 缓存节点；
2. 修复 `XnWxihjgR-E` 的专辑作者、Track 序号，并解释 HEAD 相比旧版的额外耗时；
3. Telegram 图文封面增加“使用缓存歌曲文件的元数据之封面”；
4. `sendAudio.thumbnail` 必须来自缓存歌曲文件本身的内嵌封面，并满足 Bot API 缩略图约束。

### 8.2 HEAD 抓取变慢和序号错误的根因

对比 `0c57a525994cdf501f3de99ef36646d5d6cee658` 与旧线上版本 `db7d6f8d1aed604d57b8f56e32d9bd2253abbf6f`，并逐段核对附件中的两次服务端日志后，确认不是基础音频格式选择整体退化，而是后加的专辑作者修复与旧序号兜底产生了叠加回归：

- HEAD 在新鲜解析中先调用 `enrichYoutubeMusicOrdinalMetadata`；当专辑搜索超时、`album_artist` 仍为空时，又立即调用缓存修复函数，重复同一轮专辑搜索。
- 附件日志中每轮专辑搜索都耗尽 45 秒，两轮合计额外等待约 90 秒；这正是 HEAD 明显慢于旧版的主要增量。旧版只做第一轮，因此即使同样存在 45 秒超时和错误兜底，表面耗时也较短。
- 旧逻辑在没有原生值、也没有匹配到专辑时强行写 `Track=1`、`Disc=1`。这不是抓到了正确序号，而是用一个看起来合法的值掩盖失败。
- 更严重的是，这些 `1` 已经进入旧缓存。后续即使专辑遍历找到了真正的第 3 首，`currentTrack || derivedTrack` 仍会让旧 `1` 压住新结果。

修复策略：

- 新鲜解析只执行一次专辑核验，并写入 `musicAlbumLookupCompleted` 标记；同一分析对象不再进入第二轮缓存修复。
- 专辑搜索使用“专辑名 + 单曲结构化艺术家”缩小候选集。单曲艺术家只参与检索，不直接当作专辑作者证据；最终专辑作者仍必须来自匹配专辑条目的 Topic 频道，避免把群星合辑误写成某一首歌的艺术家。
- 专辑核验共享一个总时限，默认 12 秒、最大 15 秒，可用 `YOUTUBE_MUSIC_ALBUM_LOOKUP_TIMEOUT_MS` 调整；搜索和最多 3 个候选遍历共同消耗这一个预算，不再每个子请求各等 45 秒。
- 老缓存没有核验标记时，历史 `Track=1`、`Disc=1` 被视为旧版占位值并重新验证。能匹配专辑则写真实 Track；不能验证则清空，不继续保留伪造的 `1`。
- Disc 只接受原生正整数；无法取得时按需求留空。

### 8.3 指定歌曲的真实验证

本轮再次使用当前 yt-dlp 对指定歌曲关联专辑做联网验证：

```text
搜索：https://music.youtube.com/search?q=サマー・イン・ブルー Yuri Kunizane#albums
搜索耗时：约 2.1 秒
命中 browse ID：MPREb_Vo6hJMDSJ0T
展开专辑耗时：约 3.4 秒
专辑播放列表：OLAK5uy_n2VlxbpMqN9Jm-2up1Oig_cNzSjJf81ew
XnWxihjgR-E 的位置：第 3 首
专辑条目 Topic 频道：Yuri Kunizane - Topic
```

因此当前证据链会得到：`Track=3`、`album_artist=Yuri Kunizane`、`Disc` 留空。实测搜索加展开约 5.5 秒，位于新的 12 秒总预算内；如果线上网络异常，最坏情况也会在总预算到期后留下空值，而不是再阻塞第二个 45 秒或伪造序号。

### 8.4 服务器 Shell 到 VClient 的推送接口

新增 `npm run vclient:push`，用法包括：

```text
npm run vclient:push -- --tunnel <5位短码或长ID> --file <文件> --remark <备注>
npm run vclient:push -- --tunnel <短码或长ID> --file <文件1> --file <文件2> --name <合辑名>
npm run vclient:push -- --tunnel <短码或长ID> --collection <目录> --name <合辑名>
```

设计要点：

- `--collection` 读取目录直属普通文件；重复 `--file` 也生成合辑记录。单文件最大 1GB，单合辑最多 500 个文件，与现有文件资产协议上限一致。
- 命令使用 `.tunnel-data/vclient-control.token`（或 `VCLIENT_TOKEN` / `VCLIENT_TOKEN_FILE` / `--token-file`）鉴权，以临时的 `server-shell` 内部客户端加入隧道。
- 服务端只允许通过控制令牌认证的 `server-shell` 获得发送权限；目标隧道必须已经存在且已启用缓存节点，避免长 ID 输入错误时意外创建隧道。
- 先登记文件资产，再写普通文件/合辑传输记录；VClient 看到记录后沿用既有 Socket.IO relay 请求文件。
- Shell 按 240KB 分块发送，输出每个文件的进度。只有 `file-asset-relay-complete` 已得到 VClient 的完整性校验和落盘确认，命令才报告成功并退出。
- 多个隧道仍由既有独立 VClient 进程复用；本接口不会在主服务内创建第二套缓存实现。

README 已补充操作示例与令牌、服务器地址、超时参数说明。

### 8.5 Telegram 内嵌封面

Base、Pro、Ultimate 三个图文封面下拉框均新增独立选项“使用缓存歌曲文件的元数据之封面”。该选项调用既有只读 `song-cover` 接口取得当前 M4A attached picture；“使用自定义封面”只读取显式上传槽，不再把内嵌封面冒充成自定义上传。

`sendAudio.thumbnail` 不受三个图文封面下拉框影响，服务端每次发送前直接从该任务的缓存歌曲成品提取 attached picture，再生成 JPEG thumbnail。根据 Telegram Bot API 当前约束，ffmpeg 使用保持比例的最大 `320x320` 缩放，并逐级降低 JPEG 质量直到严格小于 `200000` 字节；生成后及异常时都清理临时图片。

这保证：

- 图文记录仍可分别选择方形、内嵌、原尺寸、自定义封面；
- 音频消息的 `thumbnail` 始终忠实反映缓存歌曲文件当前元数据中的封面；
- 修改歌曲内嵌封面后，无需同步覆盖任务的方形封面，下一次 Telegram 音频发送会自动使用新内嵌图。

### 8.6 测试记录

已完成：

```text
node --check server.js
node --check server/youtube-premium.js
node --check scripts/vclient-push.js
node --test tests/youtube-premium.test.cjs tests/youtube-album-artist-regression.test.cjs tests/telegram-cover-regression.test.cjs tests/vclient-shell-push.test.cjs
```

专题测试 33 项通过。Shell 测试包含真实本地 Socket.IO 协议烟雾测试：创建临时文件、解析短码、加入模拟 VClient 隧道、写入消息、分块中继，并在接收端完成确认后退出；测试临时目录已清理。

全量回归：

```text
node --test tests/*.test.cjs
```

结果：107/107 通过，0 失败、0 跳过。首次全量执行发现旧的 `features-2608B` 契约仍断言“失败时强写 Track=1”；已将该断言更新为新的“无法验证则留空”诊断语义，再次执行后全绿。

还执行了目标文件范围的 `git diff --check`，没有新增空白错误。用户原有 `prompts/dev-prompt-logs/dev-2608B.md` 修改本身含历史尾随空格，本轮未触碰、未格式化该文件。

### 8.7 本轮建议提交日志

Title：

```text
feat: 增加 VClient Shell 推送并修复歌曲专辑核验
```

Description：

```text
- 新增服务器 Shell 单文件/合辑推送命令，通过受控文件中继等待 VClient 确认缓存落盘
- 合并并限时执行 YouTube Music 专辑核验，修复重复搜索、旧 Track/Disc 占位值及专辑作者空缺
- Telegram 图文增加歌曲内嵌封面选项，sendAudio 缩略图改为读取缓存歌曲元数据封面
- 补充 Shell 协议、专辑证据、封面来源与 Telegram 尺寸约束回归测试
```

---

## 九、大量持久化历史导致首页刷新阻塞及端口占用诊断（2026-08-25）

### 9.1 现象与排除项

线上 `tun-test.miku.us` 表现为：同一大量文件隧道首次进入较快，但浏览器接收过历史后，每次手动刷新都可能等待数分钟；缓存节点开关与独立 VClient 进程是否运行的四种组合均不能改变结果。

启动顺序提供了直接证据：`startTunnelApplication()` 原先必须先 `await loadSessionData()`，完成后才调用 `initSocket()`。因此首页刷新阶段尚未建立 Socket，更没有开始与 VClient 通信，缓存节点不是首屏阻塞源。

近期才稳定出现的回归点是提交 `ce8450f`：该提交为隧道审计和 VClient 增加了服务端持久化历史恢复。服务端重启后不再丢失内存历史，这是正确且必须保留的行为；但它使六月以来前台已有的“刷新时全量恢复 DOM”策略长期面对完整历史，从而暴露性能问题。

### 9.2 根因链路

刷新时原前台会执行以下同步前置链路：

1. 从 IndexedDB 读取当前隧道全部消息并排序；
2. 对每条消息串行 `await addMessageToChat()`；
3. 文件记录再次读取 `files` 存储，检查完整缓存、创建 Blob URL、读取媒体封面并创建 DOM；
4. 全部完成后才初始化 Socket；
5. Socket 加入后，服务端再下发最多 1000 条、总元数据不超过 16 MB 的快照，客户端原先又对每条快照单独查询一次 IndexedDB 并尝试补渲染；
6. 加入时的快照没有标记为权威快照，客户端随后执行 `reconcileLocalHistory()`，又把最多 1000 条本地消息元数据整批上报服务器。因此每次刷新实际存在一次全量元数据下行和一次全量元数据回传，虽不含文件正文，在线上网络下仍是明显的重复开销。

这不是浏览器每次向服务器重新上传全部文件。主要阻塞发生在本地全量读取与 DOM/媒体重建；服务端快照是元数据下行和本地对账。刷新会清空 DOM、内存 Blob URL 和 JS 状态，因此即使 IndexedDB 已有缓存，每次仍会重复这些工作。首次进入的新浏览器本地历史为空，所以首屏反而较快，随后服务端快照在 Socket 建立后后台落库；第二次刷新便命中完整本地历史并触发问题。

### 9.3 修复设计

完整审计记录、服务端持久化历史与 VClient 缓存能力均保留，不通过截断数据规避问题：

- 首页只构建最近 80 条记录的 DOM；完整消息仍保存在 IndexedDB；
- 记录区顶部增加“加载更早记录”，每次向前恢复 80 条，并保持点击前的滚动位置；
- 首屏和旧记录批次使用 `deferFileCache`，只先显示元数据骨架；文件或合辑进入视口上下 320px 范围后，才串行读取文件缓存与封面，避免一次克隆大量 Blob/ArrayBuffer；
- 深链接记录和已保存滚动锚点即使位于最近 80 条之外，也会额外渲染并定位，不破坏记录跳转；
- 服务端权威快照继续完整写入 IndexedDB，但仅允许最近 80 条及明确定位记录进入 DOM；
- 快照对账先一次读取本地消息并建立 ID Map，取消“每条服务端记录一次 IndexedDB 查询”的 N 次事务；
- `HISTORY_DEBUG=false` 时不再预先构造全部消息摘要数组。

80 条只作为 DOM 可见窗口的保险丝，并不是协议优化的主体。进一步从源头增加了历史版本指纹协议：

- 服务端对当前历史和删除墓碑生成 SHA-256 版本指纹；
- 客户端在完整快照处理完成后，把指纹与本地消息数量持久化到 session 记录；
- 下次加入或补请求时只携带指纹、消息数量和协议版本；
- 指纹及数量均一致时，服务端仅返回 `unchanged=true`、指纹与空消息数组，消息正文下发为 0，客户端也不会进入 `reconcileLocalHistory()`，历史回传为 0；
- 指纹不同、数量不一致或旧客户端不支持该协议时，仍走原有完整快照兼容路径；新协议客户端把变化快照视为服务端权威数据，只做本地对账，不反向回传同一批历史；
- 移除加入隧道后的 `0ms` 立即补请求；`join-session` 本身已经返回快照，现在只在 3 秒、12 秒仍未收到响应时重试，正常加入不再重复下发同一份大历史；
- 文件资产传输、VClient Socket.IO 中继、资产恢复、历史审计写入均未修改。

### 9.4 `EADDRINUSE` 与失败重启

用户提供的启动日志中，`Restored 337 Telegram server assets from disk` 只是端口监听前的正常恢复信息。真正失败点是：

```text
Error: listen EADDRINUSE: address already in use 0.0.0.0:8080
```

这表示旧进程仍占用 8080，新执行的 `npm start` 没有启动成功，线上继续由旧实例提供服务；它会造成“以为新代码已部署、实际仍在测试旧代码”的干扰，但不是 Telegram 缓存恢复导致。

服务端现在把 `listen` 包装为可等待 Promise，使端口监听错误进入 `startServer().catch()`，不再作为未处理的 `error` 事件抛出；`EADDRINUSE` 会明确输出端口、本次启动失败、Linux/Windows 查询命令及“先确认再通过原进程管理器停止，不要批量杀死 Node”的安全提示，并以退出码 1 结束。

### 9.5 验证

新增 `tests/history-startup-regression.test.cjs`，覆盖：

- 5000 条本地缓存记录只触发 80 次首屏渲染，4920 条保持延迟状态；
- 5000 条服务端历史在首次同步时完整下发；指纹及数量一致的再次刷新下发 0 条消息正文、0 条删除正文，指纹相同但本地数量异常时自动退回完整快照；
- 首屏记录均关闭自动资产请求并启用可见区域缓存读取；
- 服务端快照完整落库、仅渲染最近窗口；
- 旧记录按批加载入口与样式存在；
- `EADDRINUSE` 被启动 Promise 捕获并输出明确失败提示。

执行结果：

```text
node --check app.js
node --check server.js
node --test tests/history-startup-regression.test.cjs
# 6/6 通过

node --test tests/*.test.cjs
# 113/113 通过，0 失败、0 跳过
```

### 9.6 本轮建议提交日志

Title：

```text
fix: 优化大量隧道历史刷新并明确端口占用错误
```

Description：

```text
- 将隧道首页历史改为最近 80 条首屏窗口与更早记录按批加载，保留完整 IndexedDB 和服务端审计数据
- 文件及合辑缓存改为进入视口后按需读取，避免刷新时全量重建 Blob URL、封面和文件记录 DOM
- 优化权威历史快照的本地 ID 对账并限制补渲染范围，消除大量逐条 IndexedDB 查询
- 增加历史 SHA-256 指纹握手，未变化刷新不再双向传输最多 1000 条重复消息元数据
- 捕获 EADDRINUSE 启动失败并输出端口占用排查提示，避免把旧进程继续运行误判为重启成功
- 增加 5000 条历史首屏上限、快照渲染与服务端监听错误回归测试
```

---

## 十、按 2026-08-24 05:28 边界恢复首页传输链路并隔离审计开销（2026-08-25）

> 本节结论取代第九节的“最近 80 条 + 延迟文件水合 + 历史指纹”方案。实际联机表现证明该方案会干扰实时传输进度，第九节所述实现已经从代码中撤销；端口占用错误处理仍保留。

### 10.1 时间边界与对照基线

用户确认“隧道审计信息大量上报”需求提出于北京时间 2026-08-24 05:28。相关提交时间如下：

- `97761182cbb34c3a1f009794f413b72be49d1b7c`：2026-08-24 02:01:44 +08:00，早于需求提出时间，作为普通浏览器功能首页传输链路基线；
- `ce8450f768806f36eda49f035156703d9c6bbe01`：2026-08-24 10:47:11 +08:00，开始引入隧道审计与 VClient；
- 当前 HEAD `46fcc27d11853fef313b0ce1a000afc7b37e3ec9`：2026-08-25 09:36:32 +08:00，包含后来为大量历史设计的首屏窗口、延迟水合和历史指纹逻辑。

恢复原则不是整仓回退，也不撤销审计需求，而是把普通浏览器的实时消息、文件传输、缓存恢复、进度条和历史渲染链路恢复至 `97761182` 的行为；仅保留 VClient 特殊客户端所需的 relay-only 分支、缓存节点设备过滤、编辑器 relay 请求标识与闲置清理等条件化差异。后台管理、VClient 独立进程、服务端 Shell、YouTube Premium、Telegram 等后续非首页链路功能均保留。

### 10.2 根因证据

对 `97761182` 与当前 HEAD 做函数级比较后确认：进度条的创建、更新、完成和失败函数本身并没有发生实质变化。进度条“一闪又消失”的直接新增调用链来自后来加入的历史延迟水合：

```text
IntersectionObserver
  -> enqueueDeferredHistoryHydration
  -> refreshFileMessage
  -> hideCompletedFileReceiveProgress
  -> hideProgress
```

历史记录进入视口时，这条链会按消息 ID 查找并移除进度项；当同一文件正在实时接收、历史恢复与实时事件交错时，旧记录刷新会误伤正在活动的进度条。与此同时，“仅渲染最近 80 条”让其余记录更早进入后台恢复队列，导致大量 IndexedDB、Blob、封面和 DOM 工作与实时 WebRTC/Socket.IO 事件竞争主线程。这解释了为什么进度函数看似没改，联机体验仍明显变差。

服务端还有一个独立但会放大问题的同步阻塞源：浏览器会在加入、定时器、`focus`、`pageshow` 等时机声明本地文件资产；审计实现此前会对每个资产至少执行元数据写入和“announced”事件写入，每次写入又同步执行整库 `db.export()`、临时文件写入与重命名。资产数为 N 时，一轮声明会产生约 2N 次完整数据库导出，数据库本身又被重复事件持续放大，表现接近随历史规模恶化的二次开销。它与是否启动 VClient 进程无关，因此此前四种 VClient 开关组合都会慢。

### 10.3 修改记录

前端链路恢复：

- `app.js` 与 `pages/index.html` 的普通浏览器首页行为恢复到 `97761182` 基线；
- 删除最近 80 条窗口、加载更早记录按钮、`IntersectionObserver` 延迟水合、`deferFileCache` 和前端历史 v2/指纹协商；
- 恢复原有 IndexedDB 历史渲染、文件缓存读取、Socket 加入和 `[0, 3000, 12000]` 补请求节奏；
- 保留缓存节点客户端必须使用服务端 relay、VClient 设备不进入普通互联 UI，以及编辑器 relay 可靠性补丁。

服务端审计隔离：

- 普通浏览器实时 session 不再从审计数据库同步灌入历史；重启后的客户端仍按旧链路从各自 IndexedDB 对账，审计数据库继续供后台与 VClient 查询；
- 普通客户端收到与 `97761182` 相同的历史快照结构，不再为每次加入同步计算 SHA-256；可选 v2 兼容入口保留，但首页不启用；
- 新增审计旁路队列，按最多 32 项让出事件循环，并按隧道、消息或资产键合并待处理更新；广播、provider 转发、完成确认和运行日志均先执行，审计失败或变慢不再阻塞传输协议；
- 旁路任务保持同步、最多重试 3 次，成功后才更新活动节流时间；文件处理层不再吞掉审计异常，最终失败会输出明确日志；rich 消息所引用资产的名称、大小等元数据在入队前完成快照；
- 文件可用事件只在首次发现资产时补齐资产元数据，不再把每次在线声明记录成永久 `announced` 传输事件；请求、完成、失败和 relay 完成等有审计意义的事件继续保存；
- 修复 `started` 状态曾被误记为 `failed` 的审计错误。

持久化降频：

- `InfraStore` 的高频更新改为内存中立即可见、磁盘写入尾随防抖；默认 1.5 秒防抖、最长 15 秒必须落盘，可通过环境变量调整；
- 同一批数百次历史、设备、资产与进度更新只做一次物理 `db.export()` 和原子替换；
- 管理员显式修改备注/权限、删除隧道/短码和切换缓存节点仍立即保存；删除消息的墓碑也立即落盘；`SIGINT`、`SIGTERM`、`SIGHUP` 正常退出会先清空审计旁路队列，再 `flush()` 持久化队列。

这套处理没有要求前台新增“全量上报”。审计所需的隧道 ID、活跃设备数、历史总设备数、最后活动时间、创建时间、传输记录数、文件数及总大小继续由已有加入/离线、历史消息和文件资产事件增量维护；后台数据完整性与实时传输链路彼此隔离。服务重启时只从审计库恢复最近的删除墓碑，不把完整历史重新灌入普通浏览器 session；客户端对账时再用一次至多 1000 个候选 ID 的分块索引查询核验墓碑，即使删除记录超过内存窗口也不会被旧设备复活。

当前存储仍基于 `sql.js`，单次 `db.export()` 本身是同步操作；本轮把它从“每个资产/事件一次”压缩为“一批更新一次”，消除了随资产数倍增的主因，但不把它描述成绝对零停顿。若未来审计库增长到单次批量导出也不可接受，应将 InfraStore 整体迁移到独立 worker/进程或原生 SQLite WAL，而不是再次改动浏览器传输链路。

### 10.4 验证记录

静态与自动化验证：

```text
node --check server.js server/infra-store.js server/file-assets.js app.js
node tests/p2p-connection-regression.test.cjs
# 38/38 通过

node tests/infra-store-audit.test.cjs
node --test tests/infra-store-write-batching.test.cjs
node --test tests/file-asset-audit-isolation.test.cjs
# 审计恢复、批量落盘与协议非阻塞测试全部通过

node --test tests/*.test.cjs
# 118/118 通过，0 失败、0 跳过
```

新增或改写的回归断言覆盖：

- 5000 条 IndexedDB 记录全部走稳定基线渲染，不再存在 80 条截断与延迟水合；
- 旧版客户端历史快照与 `97761182` 完全同构，普通 session 不注入审计持久化历史；
- 服务重启仅恢复删除墓碑，并在本地对账时按候选 ID 核验持久化墓碑，旧设备无法把窗口外的已删记录重新带回来；审计同步异常最多重试 3 次，三种退出信号都会执行队列与磁盘 flush；
- 200 次连续历史写入在显式 flush 前不触发物理整库写，flush 后恰好写一次且可重新打开校验；
- 审计 Promise 永不结束时，文件请求仍能立即转发给 provider；文件可用广播、状态确认和 relay ack 均先于审计排队；
- P2P 优先、单 offer、relay 回退、背压、重连和终态去重等原有 38 项链路契约继续通过。

浏览器双设备烟雾测试使用两个独立 origin 加入同一隧道：两端显示 2 台在线设备与“P2P 已连接”，实时消息可互收；刷新其中一端的 `DOMContentLoaded` 约 569 ms，约 500 ms 后恢复设备、P2P 和历史消息；控制台无业务错误。测试结束后临时服务、端口、浏览器标签和测试目录均已清理。

目标文件范围 `git diff --check` 通过。全局检查仅报告用户原有且本轮未触碰的 `prompts/dev-prompt-logs/dev-2608B.md` 尾随空格；该文件未被格式化或修改。

### 10.5 本轮建议提交日志

Title：

```text
fix: 恢复稳定传输链路并隔离隧道审计开销
```

Description：

```text
- 按 8 月 24 日 05:28 前的 97761182 基线恢复普通浏览器消息、文件缓存与进度条链路
- 移除会误删活动进度项的历史窗口、延迟水合及首页历史指纹逻辑，保留 VClient 专用 relay 分支
- 将隧道历史、设备及文件审计移入非阻塞旁路队列，先完成实时广播、转发和确认
- 合并重复资产在线声明并修正传输状态审计，保留后台统计和 VClient 所需完整数据
- 对高频审计写入增加防抖、最大等待与退出 flush，避免按资产重复同步导出整个数据库
- 恢复删除墓碑并为审计任务增加有限重试，覆盖 SIGHUP 重启落盘且避免旧设备复活已删记录
- 增加 5000 条历史、审计隔离、批量落盘及 P2P/relay 全链路回归测试
```

---

## 十一、后台短码、缓存节点切换、全局语音通话与远程预览（2026-08-26）

### 11.1 需求边界与实现判断

本轮不改普通浏览器的消息、文件传输、P2P/relay、进度条及历史恢复链路。新增实时事件全部放在媒体控制模块内，远程预览只调用既有本地预览入口；这样可以满足跨设备控制，同时不重新引入第九节已经撤销的首页历史水合和审计耦合。

后台隧道 API 原本已经返回 `shortCode`，缺失的是管理页面的显式展示；因此只补 UI，不重复建立短码数据源。VClient 状态表原本只保存长隧道 ID，页面切换器需要把状态行与审计隧道表中的 `short_code` 组合。服务端仍返回完整状态供后台使用，`/vclient` 页面严格按 `desired_enabled === 1` 过滤，确保停用节点不会出现在切换器中。

语音通话原实现只是无状态转发，任意设备都可伪造接听、结束或媒体信令，也没有占线和超时。正式实现改为服务端呼叫状态机：

```text
空闲 -> 振铃（45 秒超时） -> 已接听/通话中 -> 结束
                 \-> 拒接/离线/无人接听 -> 结束
```

每个设备同一时间只能属于一个呼叫；接听、拒接、挂断和 WebRTC 信令都必须来自呼叫双方，并与 `callId` 匹配。呼叫不检查当前隧道是否相同，符合“不受隧道会话管理”；但浏览器与服务端仍要以设备身份建立全局路由。

远程预览采用两阶段能力验证，不能由控制端单方面声称目标有缓存：

```text
控制端发起缓存检查
  -> 服务端确认双方仍在同一隧道且目标为在线浏览器客户端
  -> 目标端用本机 IndexedDB/缓存引用核验文件完整性
  -> 服务端记录短期能力凭据
  -> 控制端才能发出打开命令
  -> 目标端再次核验、打开既有预览浮层并进入全屏、回传结果
```

缓存节点 `vclient` 被排除在被控设备列表和服务端目标校验之外；检查凭据 20 秒失效，设备离线时立即清理。图片、视频、音频都属于“可预览文件”，因此同步补齐了音频全屏卡片，避免 `#️⃣` 对音频可见却无法完成全屏。

### 11.2 修改记录

- `pages/admin.html`：在隧道卡片标题和元信息中显示 5 位短码，缺失值明确显示 `-----`；
- `server/vclient-control.js`、`pages/vclient.html`：VClient 状态附带短码；页面顶部增加醒目 select，定时同步启用列表，切换时保留分页、状态和 URL；
- `server/media-session.js`：新增呼叫占线、超时、参与者校验、断线清理和媒体信令授权；新增同隧道远程预览缓存检查/打开凭据状态机；
- `client/media.js`：在请求麦克风前显示准备状态，增加拨号超时、重复通话保护、UUIDv4 兼容生成、通话资源和远端音频清理；
- `pages/index.html`、`app.js`：打磨来电/拨号/通话中顶部浮层，展示联系人、接听/拒接/取消/挂断与通话时长；在音乐按钮左侧增加 `#️⃣`，提供目标设备缓存检测列表、执行反馈和音频全屏；
- `server.js`：媒体模块读取“当前隧道设备身份或全局资料设备身份”，并在仅注册全局资料的设备断线时清理通话。文件传输模块仍只读取隧道身份，没有扩大权限。

### 11.3 联调发现与修正

自动化状态机最初全部通过，但真实浏览器来电没有出现。使用两个独立 origin（`127.0.0.1` 与 `localhost`）建立两个独立客户端，再由仅注册全局资料的第三客户端发起呼叫，确认根因是媒体处理器只读取 `currentDevice`：未加入任何隧道的联系人设备只有 `profileDevice`，会被错误当成无身份。

修正为媒体模块使用 `currentDevice || profileDevice` 后复测：接收端顶部浮层正确显示“来电 · 全局联系人通话”、主叫名称、“接听”和“拒接”；点击拒接后浮层进入 idle 并隐藏，主叫收到 `reason: rejected`。文件处理器的身份读取保持 `currentDevice`，避免把这项全局语音修正扩散到文件传输权限。

浏览器测试未代替用户接受麦克风权限，因此没有采集真实环境声音；接听后的状态转换、参与者限制、WebRTC 信令授权、挂断和断线清理由动态服务端测试覆盖，通话时长由前端定时器与 `startedAt` 回归断言覆盖。

### 11.4 验证记录

```text
node --check app.js
node --check client/media.js
node --check server/media-session.js
node --check server/vclient-control.js

node --test tests/features-2608C.test.cjs tests/vclient-runtime.test.cjs tests/p2p-connection-regression.test.cjs
# 55/55 通过

node --test tests/*.test.cjs
# 120/120 通过，0 失败、0 跳过
```

新增回归覆盖：后台短码、VClient 只列启用隧道、呼叫占线与参与者校验、非通话方信令拒绝、同隧道限制、目标完整缓存回执前禁止远程打开、VClient 排除、按钮顺序和音频全屏。原有 P2P、relay、VClient、审计、YouTube Premium、Telegram 与大历史恢复测试全部继续通过。

`git diff --check` 对本轮目标文件通过；全局检查仍只报告用户原有且本轮未修改的 `prompts/dev-prompt-logs/dev-2608B.md` 两处尾随空格。临时服务器、18083 端口、浏览器标签和测试目录均已清理。

### 11.5 本轮建议提交日志

Title：

```text
feat: 完善隧道管理并新增全局通话与远程预览
```

Description：

```text
- 在后台隧道列表展示 5 位短码，并为缓存节点传输页增加仅含已启用隧道的短码切换器
- 将联系人语音升级为服务端呼叫状态机，支持拨号、来电接听/拒接、占线、超时、断线清理和通话计时
- 修复未加入隧道的全局联系人无法发起语音呼叫的问题，并限制媒体信令只能在呼叫双方之间转发
- 在文件预览音乐按钮左侧增加远程打开入口，逐设备验证完整缓存后自动打开预览并进入全屏
- 排除 VClient 被控目标，增加短期缓存能力凭据和同隧道校验，防止伪造远程打开命令
- 补齐音频全屏预览，并增加后台、VClient、呼叫状态机和远程预览权限回归测试
```

---

## 十二、远程预览重复全屏、浮层层级与语音入口修复（2026-08-27）

### 12.1 复现结论与根因

“选择被控设备”沿用了通用 `.modal-overlay` 的 `z-index: 4000`，而文件预览浮层 G 单独使用 `z-index: 5000`，因此选择器虽然已经激活，视觉上仍被 G 完整遮住。打开选择器的函数没有主动关闭 G；问题是纯粹的层叠顺序错误。

被控端重复全屏不是服务端把设备移出隧道，也不是缓存凭据只能使用一次。原实现收到每条远程打开命令后都会无条件执行：

```text
退出当前全屏
  -> 销毁当前文件预览
  -> 重新打开文件预览
  -> 顺序扫描当前隧道全部历史记录和文件缓存，构造全屏文件列表
  -> 再进入全屏
```

当目标设备本来就在全屏显示同一文件时，前两步属于不必要的破坏。隧道文件很多时，`getFullscreenPreviewItems()` 又会逐条等待 IndexedDB 文件查询，容易超过控制端原来的 8 秒等待窗口；控制端于是显示“目标设备不可用”，而被控端已经退出旧全屏、尚未完成新全屏，形成用户看到的交替成功/失败。失败状态还会禁用按钮，并且刚使用过的短期能力凭据已被服务端清理，所以在原浮层里继续点击不会产生有效动作。

联系人语音的服务端呼叫状态机和 `/device/:id` 资料页入口确实已经存在，但功能首页的在线设备行、已关注联系人行只创建了 `📢` 对讲按钮，功能首页设备资料浮层也没有语音按钮。这是入口遗漏，不是通话状态机未实现。

### 12.2 修改记录

- 为 `#remotePreviewDeviceModal` 设置 `z-index: 5600`，高于文件预览 G 的 `5000`；打开选择器时继续保留 G 的 active 状态，不关闭、不销毁预览；
- 被控端把重复命令改为幂等操作：当前全屏已经显示目标文件时直接回报成功，保持媒体、播放位置与全屏状态，不再先退出；
- 切换到其他文件时只走一次统一的预览关闭路径，并清理当前 History state 中失效的预览/全屏层标记，避免重复关闭和陈旧浏览器历史干扰后续操作；
- 为远程控制增加 `focusedOnly` 全屏快速路径，直接使用刚核验过的活动文件缓存构造单个全屏项，不再为一次远程命令遍历整个隧道历史；普通用户主动打开全屏时原有同记录/同隧道浏览能力保持不变；
- `openActivePreviewFullscreen()` 改为返回明确的成功布尔值，并修正“找不到当前文件却把索引强制变成 0”的旧判断，远程端可准确回报预览或全屏失败；
- 控制端打开等待窗口调整为 15 秒；能力凭据过期、打开超时或目标回报失败后会自动重新检测缓存，不可用行也提供“重新检测”按钮，不再留下一个看似可点但无响应的死状态；
- 功能首页的在线设备行和已关注联系人行增加独立的 `☎` 语音按钮，设备资料浮层增加“☎ 语音通话”按钮；保留 `📢` 为隧道对讲，两种语义不混用；
- 通话状态浮层提升到 `z-index: 10000`，确保来电、拨号和通话中状态不会被文件预览或媒体全屏遮住；
- 本轮没有修改消息恢复、文件发送/接收、P2P、relay、进度条或审计链路。

### 12.3 验证记录

静态、动态状态机与全量回归：

```text
node --check app.js
node --check client/media.js
node --check server/media-session.js

node --test tests/features-2608C.test.cjs
# 9/9 通过

node --test tests/*.test.cjs
# 123/123 通过，0 失败、0 跳过
```

新增动态客户端测试直接执行实际 `handleRemotePreviewOpen()`：同一文件已全屏时关闭/重建调用次数均为 0 且回报成功；切换另一文件时只关闭一次，并确认调用 `focusedOnly` 快速路径。服务端状态机测试还连续完成两组不同 `requestId` 的缓存核验、打开和结果回传，证明一次成功不会使目标设备失效。

浏览器双设备烟雾测试使用 `127.0.0.1` 与 `localhost` 两个独立 origin 加入同一隧道，两端均显示 2 台在线设备。实测功能首页在线设备行显示 `☎`，设备资料浮层显示“☎ 语音通话”；浏览器计算样式为文件预览 G `5000`、选择被控设备 `5600`、通话状态 `10000`。内置浏览器没有捕获本地页面的文件选择事件，因此未以伪造 DOM 状态冒充一次端到端文件上传；远程重复打开由上述真实函数动态测试覆盖。临时浏览器、服务端和测试目录在验证后清理。

`git diff --check` 对本轮目标文件通过；代码保持未暂存、未提交。

### 12.4 本轮建议提交日志

Title：

```text
fix: 修复远程预览重复全屏并补齐语音入口
```

Description：

```text
- 提升被控设备选择浮层层级，在文件预览 G 上方显示并保留原预览状态
- 将同文件远程全屏改为幂等操作，避免重复命令先退出当前全屏
- 为跨文件远程打开增加单文件快速路径，移除全量历史扫描导致的超时和假离线
- 清理远程替换时的陈旧预览历史状态，并允许失败后自动或手动重新检测缓存
- 在首页设备行、联系人行和设备资料浮层补充全局语音通话入口
- 提升来电与通话状态浮层层级，增加连续远程打开和入口可见性回归测试
```

---

## 十三、远程预览顶层选择器与持续控制面板（2026-08-27）

### 13.1 再次复现后的修正判断

上一轮只把“选择被控设备”从通用浮层的 `z-index: 4000` 提升到 `5600`。这个数字虽然高于文件预览 G 的 `5000`，但页面里还存在资源浏览器、记录详情、富文本历史、媒体全屏和音乐播放器等 `6200–9200` 的独立层级；继续靠人工挑选更大的普通 `z-index`，无法从机制上保证选择器永远位于当前预览之上。

本轮将选择器从普通 `<div>` 浮层改为原生模态 `<dialog>`，通过 `showModal()` 进入浏览器 top layer。top layer 独立于页面普通层叠上下文，因此不再与 G 或其他定位元素比较 `z-index`。打开函数只展示 dialog，不调用 `closeFilePreview()`，文件预览 G 的 DOM、媒体和 active 状态保持不变。对缺少 `showModal()` 的旧环境保留 `2147483000` 的固定层降级实现。

远程打开此前是一次性“缓存核验—打开—回执”，成功后服务端立即删除凭据，没有权限上下文可承载后续控制命令。本轮新增持续控制会话：

```text
缓存核验凭据
  -> 目标成功打开全屏
  -> 服务端将 requestId 提升为 controlId
  -> 控制端显示持续控制面板
  -> 上一个 / 下一个 / 播放暂停 / 退出命令
  -> 目标执行并回传当前文件、媒体类型和播放状态
  -> 退出、替换或任一设备离线时销毁控制会话
```

每条控制命令都由服务端校验 controlId、控制者、被控者、当前隧道在线状态和动作白名单；同一控制者或同一目标建立新控制时，旧会话会被替换并通知双方结束。控制事件没有进入普通文件传输、P2P、relay、历史恢复或审计链路。

### 13.2 修改记录

- `pages/index.html`：将设备选择器改为原生 `<dialog>` top layer，补充 backdrop 和旧浏览器固定层降级；
- `pages/index.html`：新增固定于控制端右下角的远程预览控制面板，提供“上一个”“下一个”“播放/暂停”“退出全屏”，移动端自动改为两列布局；
- `app.js`：统一使用 `showModal()/close()` 管理选择器，按 Esc、取消或点击 dialog 空白处关闭时只清理选择状态，不关闭文件预览 G；
- `app.js`：打开成功后保存控制端 controlId、目标设备、当前文件、媒体类型和播放状态；命令执行期间禁用重复点击，超时或失效时给出明确状态；
- `app.js`：被控端支持左右查找相邻的完整缓存可预览文件。查找只展开传输记录元数据，并沿目标方向逐个核验候选缓存，命中第一个可用文件即停止，不预先物化全部历史文件；
- `app.js`：音频、视频从当前全屏媒体元素读取实际播放状态，远程切换播放/暂停；目标本地退出全屏时主动结束控制会话；
- `server/media-session.js`：成功打开后建立受认证控制会话，新增控制命令、结果和结束事件；拒绝跨隧道、非控制者、非目标设备及白名单外动作；
- `server/media-session.js`：退出全屏、建立替代控制或设备断线时清理控制会话，并通知仍在线的另一端；
- `tests/features-2608C.test.cjs`：增加 top layer、控制面板、参与者授权、连续命令、相邻文件、音视频播放和退出全屏的回归覆盖。

### 13.3 验证记录

```text
node --check app.js
node --check server/media-session.js

node --test tests/features-2608C.test.cjs
# 11/11 通过

node --test tests/*.test.cjs
# 125/125 通过，0 失败、0 跳过
```

动态测试实际执行服务端和被控端函数，而不只检查源代码文字：

- 服务端连续建立两次远程控制，正确转发上一项、播放暂停和退出命令；隧道外第三方控制被拒绝，退出后旧 controlId 不能重放；
- 被控端切换到相邻视频后回传新文件信息，播放命令使媒体从 paused 进入 playing，退出命令关闭全屏并清空目标控制状态；
- 同文件重复打开仍保持幂等，不销毁已有全屏；切换到另一初始文件仍走单文件快速打开路径。

浏览器双设备验证使用 `127.0.0.1` 与 `localhost` 两个独立 origin，两端进入同一隧道并显示 2 台在线设备。实际 DOM 中选择器为 `DIALOG`，固定层降级值为 `2147483000`；控制面板初始隐藏并完整包含四项操作。两端页面控制台均无 error。选择器真正打开时由标准 `showModal()` 进入浏览器 top layer，自动化回归同时断言打开路径不含任何关闭 G 的调用。

临时服务端、浏览器标签和测试目录在验证后清理。本轮目标文件 `git diff --check` 通过；保留用户已有的 `prompts/dev-prompt-logs/dev-2608B.md` 未暂存修改，不格式化、不覆盖。所有本轮代码保持未暂存、未提交。

### 13.4 本轮建议提交日志

Title：

```text
feat: 完善远程预览顶层选择与持续控制
```

Description：

```text
- 将被控设备选择器改为浏览器原生 top layer，彻底脱离文件预览 G 的普通层叠竞争
- 保持选择设备期间的原文件预览、缓存和媒体状态，不再通过关闭 G 暴露选择器
- 在控制端增加持续控制面板，支持左右切换文件、退出全屏以及音视频播放暂停
- 为远程操作建立受认证的持续控制会话，限制控制双方、当前隧道和允许动作
- 被控端按方向查找首个完整缓存的可预览文件，避免为单次切换物化全部历史文件
- 在控制替换、主动退出和设备离线时清理状态并通知对端
- 增加 top layer、权限隔离、连续控制、媒体播放和全屏退出回归测试
```

---

## 十四、远程预览控制面板重排与剪贴板图片快捷发送（2026-08-28）

### 14.1 设计判断

远程预览控制已经是一个持续会话，因此“最小化”不能销毁 `controlId`，也不能向被控端发送退出命令。本轮只在控制端状态中增加 `minimized`，完整保留目标设备、当前文件、播放状态及待执行动作；悬浮球恢复时继续操作同一个控制对象。右上角 `Ｘ` 则使用原有、经服务端认证的 `exit` 动作，先隐藏本机面板，收到被控端回执后销毁控制状态并确认目标全屏退出。

同时修正一个在交互叠加时可能出现的竞态：若“上一个/下一个”尚未回执就点击 `Ｘ`，迟到的旧回执不得覆盖当前 `exit` 状态、重新显示面板。控制端现在只接受与 `pendingAction` 相符的回执；退出命令响应超时时关闭本机面板并明确提示，不让一个永久等待的隐藏状态残留。

浏览器无法在所有平台上无提示地读取剪贴板。为避免首页加载时反复申请权限或轮询读取非图片内容，图片检测采用两条互补路径：

- 用户实际粘贴时，只观察 `ClipboardEvent.clipboardData.items` 中的 `image/*` 项，不阻止输入框、编辑器原有的粘贴行为；
- 页面获得焦点或重新可见时，仅在 `clipboard-read` 已授权的情况下静默读取类型列表；未授权时不主动弹权限框，浏览器不支持 Permissions API 时仍可通过标准 paste 事件工作。

检测到图片后只在内存中保存图片 `File`，不会读取或记录剪贴板文本。点击 paste 区域复用现有 `sendSelectedFiles()`，因此权限校验、传输记录、本机缓存、P2P/relay 发送与进度展示均沿用原文件发送链路，没有新建旁路协议。

### 14.2 修改记录

- `pages/index.html`：将 `#dropZone` 包进 `#fileUploadComposer`；图片可用时采用 `2fr / 1fr` 两列，右侧 `#pasteImageZone` 保持 `aspect-ratio: 1`，并由同一 grid 行保证与 dropZone 等高；
- `app.js`：增加 Clipboard API 已授权探测与标准 paste 事件检测，只提取 `image/*`，为无文件名的图片生成带时间戳的安全文件名；
- `app.js`：paste 区域点击后复用 `sendSelectedFiles()` 把图片发送到当前隧道，保留原发送权限和错误反馈；
- `pages/index.html`：远程控制面板按标题栏、设备与文件、状态、前后切换、可选播放控制五行重排；右上角 `_` 和 `Ｘ` 为无边框窗口按钮；
- `app.js`：最小化后显示只含 `#️⃣` 的固定悬浮球，支持 Pointer Events 拖动、视口边界约束、拖动后抑制误点击以及窗口尺寸变化后的坐标校正；
- `app.js`：非音视频时隐藏整行播放控制；设备图标与设备名分开显示，避免设备名原有 emoji 重复；
- `tests/features-2608C.test.cjs`：增加五行结构、按钮样式、最小化/恢复、悬浮球拖动、图片 MIME 过滤与 2:1 布局回归；
- `tests/features-2608B.test.cjs`：同步上一提交已经升级到 `instant-tunnel-v27` 的 Service Worker 缓存版本断言，修复与本轮功能无关但会阻断全量测试的陈旧预期。

本轮没有修改服务端远程控制协议、文件传输协议、P2P/relay、历史恢复、审计或 VClient 链路。

### 14.3 验证记录

语法、定向测试与全量回归：

```text
node --check app.js
node --test tests/features-2608C.test.cjs
# 13/13 通过

node --test tests/*.test.cjs
# 127/127 通过，0 失败、0 跳过
```

全量测试首次运行时仅有一项旧断言失败：`service-worker.js` 已在上一提交升级为 `instant-tunnel-v27`，而 `features-2608B.test.cjs` 仍断言 v26。同步为 v27 后全量回归通过。

浏览器测试使用独立临时服务端和两个不同 origin 的客户端加入同一测试隧道，并使用生成的 1×1 PNG 模拟剪贴板图片，不读取本机真实剪贴板内容：

- paste 事件后 `#fileUploadComposer` 自动进入 `clipboard-image-ready`；实测总宽 443px，dropZone 为 288.66×144.33px，paste 区域为 144.33×144.33px，左右约 2:1、两区等高且右区为正方形；
- 点击“点此粘贴图片”后，70B 测试 PNG 正常生成传输记录并由另一客户端完整缓存；
- 控制端从文件预览 G 选择另一客户端，目标端实际进入图片全屏，控制端显示独立设备图标、设备名和文件名、状态、上一个/下一个；图片不是音视频，因此播放行完整隐藏；
- `_` 可将面板变为只显示 `#️⃣` 的悬浮球，点击悬浮球恢复原控制会话；拖动坐标与边界处理另由实际函数状态测试覆盖；
- 点击 `Ｘ` 后控制端面板和悬浮球均关闭，被控端 `mediaFullscreenViewer` 的 active 状态变为 false。

`git diff --check` 对本轮目标文件通过。临时浏览器标签、测试服务端和临时目录均在验证后清理；用户已有的 `prompts/dev-prompt-logs/dev-2608B.md` 修改保持原样，未格式化、未覆盖。所有本轮代码保持未暂存、未提交。

### 14.4 本轮建议提交日志

Title：

```text
feat: 完善远程预览控制并支持剪贴板图片发送
```

Description：

```text
- 将远程预览控制面板重排为清晰的五行结构，分离设备图标、文件信息与操作状态
- 增加无边框最小化和退出按钮，支持可拖动的 #️⃣ 悬浮球及同一控制会话恢复
- 退出时联动关闭目标设备全屏，并隔离迟到回执避免面板竞态重现
- 检测剪贴板图片后将上传区按 2:1 分栏，提供等高正方形粘贴区域
- 复用现有文件发送链路把剪贴板图片传入隧道，保留权限、缓存、进度和传输策略
- 增加面板状态机、悬浮球拖动、剪贴板 MIME 过滤和双设备浏览器回归验证
```

---

## 十五、系统剪贴板图片变化监听与远程音乐播放器复用（2026-08-28）

### 15.1 问题复核与实现判断

上一轮剪贴板实现只能在两种情况下发现图片：用户主动按下 Ctrl+V 触发 `paste` 事件，或站点已经获得 `clipboard-read` 权限后恰好发生页面聚焦/重新可见。外部截图工具改变系统剪贴板时，没有任何持续监听入口，所以页面不会自动显示“点此粘贴图片”。发送成功后代码只结束 busy 状态，没有清空待发送文件和可用状态，因此 paste 区域也不会收起。

本轮采用事件优先、受控探测兜底的组合：

- 浏览器支持时监听 `navigator.clipboard` 的 `clipboardchange`，直接根据事件的 MIME 类型决定 paste 区域显隐，并使用 `changeId` 区分每次系统剪贴板变更；
- 对声明支持但实际不派发事件、以及尚未实现事件的浏览器，保留 2 秒兜底探测；探测只在页面可见且拥有焦点时执行，未授权时不会在后台反复弹权限请求；
- 在首次页面交互后尝试取得剪贴板读取能力。这是浏览器安全模型要求：没有站点持久权限时，系统剪贴板变化事件也需要页面已有 sticky activation；
- 兜底读取使用图片完整内容的 SHA-256 指纹，而不是只比较 MIME 和大小，因此两张大小恰好相同但内容不同的截图仍会被识别为变化；
- 成功发送后记录已消费指纹并立即清空待发送状态，同一张仍留在剪贴板里的图片不会被下一次轮询重新弹出；剪贴板真正换成另一张图片后才重新显示；
- 仍保留标准 `paste` 事件作为手动粘贴兼容路径，且不读取、记录或拦截剪贴板文本。

真实浏览器验证还发现当前测试浏览器虽然暴露 `onclipboardchange`，由浏览器自动化层更新剪贴板时却没有派发事件。这证明不能只靠功能存在性判断，因此最终保留事件监听和已授权低频兜底同时工作，而不是二选一。

远程音乐的根因同样明确：被控端在缓存核验后无条件调用 `openActivePreviewFullscreen()`，该函数会把音频渲染成通用媒体全屏卡片，并不知道既有音乐播放器。现在被控端先用 `isAudioFileLike()` 分流；音频仍通过文件预览 G 建立完整缓存对象和临时播放状态，随后交接给原有 `openMusicPlayerFromActivePreview()` 和全屏音乐播放器。图片、视频及其他可预览文件继续走原通用媒体全屏链路。

远程控制会话新增内部 `presentation` 标记：音乐会话的上一首、下一首、播放/暂停和退出分别复用音乐播放器的队列、后台 Audio 和关闭逻辑；本地手动最小化或关闭音乐播放器时也会通知控制端结束会话。控制端收到结束通知时按实际展示类型关闭音乐播放器或通用媒体全屏，避免两个浮层互相残留。

### 15.2 修改记录

- `app.js`：增加系统 `clipboardchange` 监听、页面可见/聚焦条件下的授权兜底探测、图片 SHA-256 指纹和已消费图片抑制；
- `app.js`：剪贴板图片成功走完原 `sendSelectedFiles()` 后立即清空待发送数据并隐藏 paste 区域；失败时保留区域以便重试；
- `app.js`：远程音频打开改为复用既有全屏音乐播放器，通用媒体全屏只处理非音频预览；
- `app.js`：远程音乐控制接入原播放队列、播放状态和退出生命周期，继续沿用既有服务端认证 controlId，不改变远程控制协议；
- `tests/features-2608C.test.cjs`：增加无需 paste 事件的剪贴板变化显隐、发送后收起、后续图片重新出现，以及远程音频播放器分流和控制命令回归。

本轮没有修改 HTML/CSS、服务端协议、文件传输、P2P/relay、历史恢复、审计或 VClient 链路，也没有改动用户已有的 `prompts/dev-prompt-logs/dev-2608B.md` 未提交内容。

### 15.3 验证记录

```text
node --check app.js
node --test tests/features-2608C.test.cjs
# 14/14 通过

node --test tests/*.test.cjs
# 128/128 通过，0 失败、0 跳过

git diff --check -- app.js tests/features-2608C.test.cjs docs/devlog/dev-2608C-features.md
# 通过
```

本地真实浏览器使用生成的 1×1 PNG 验证，没有读取用户图片：

- 不按 Ctrl+V，在页面完成一次正常交互并获得浏览器允许后，剪贴板图片自动使 `#fileUploadComposer` 进入 `clipboard-image-ready`，右侧 paste 区域可见；
- 点击 paste 区域后测试 PNG 成功生成隧道传输记录，区域立即隐藏；
- 保持页面不按 Ctrl+V，将剪贴板换成内容不同的第二张 PNG，paste 区域在兜底周期内自动重新显示；
- 真实截图确认 dropZone 与 paste 区域继续保持约 2:1 布局，页面控制台没有新增 error。

远程音频分流测试实际执行被控端打开和控制处理函数：音频只调用全屏音乐播放器，不调用通用媒体全屏；下一首更新到音乐队列的新曲目，播放命令改变后台 Audio 状态，退出命令关闭音乐播放器并清空控制会话。全量 128 项回归同时覆盖原图片/视频远程预览链路未退化。

所有本轮代码保持未暂存、未提交。

### 15.4 本轮建议提交日志

Title：

```text
fix: 修正剪贴板图片监听并复用远程音乐播放器
```

Description：

```text
- 监听系统剪贴板图片变化，并以可见前台授权探测兼容未完整支持 clipboardchange 的浏览器
- 使用图片内容指纹识别真实变化，发送成功后立即收起粘贴区域且不重复提示同一图片
- 保留 Ctrl+V 兼容路径和原文件发送链路，不读取剪贴板文本、不改动传输协议
- 将远程音频全屏切换到既有音乐播放器，复用曲目队列、播放暂停和关闭状态
- 按音乐或通用媒体展示类型清理远程控制会话，避免浮层和控制状态残留
- 增加剪贴板状态转换、远程音乐分流及控制命令回归测试
```

---

## 十六、独立 SNS 下载中心与设备语音通话增强（2026-08-28）

### 16.1 需求边界与技术判断

本轮首先逐项梳理了现有 YouTube Premium 下载页的任务队列、格式选择、解析信息缓存、成品缓存、标签备注、预览、封面与隧道转发流程。SNS 页需要复用这些已经验证过的交互模式，但不能把 TikTok、Facebook、Instagram、Twitter/X、Threads、LINE 的分支塞进 YouTube Premium 的抓取状态机，更不能改变其私人 Cookie、音乐元数据和专辑补全逻辑。

因此采用“共享底层工具、隔离业务状态”的设计：SNS 使用独立服务模块、独立 API 前缀、独立任务文件、独立下载目录、独立解析缓存和独立浏览器成品缓存；只复用通用的 `yt-dlp`/`ffprobe` 执行器及现有普通 SNS Cookie 选择器。`server/youtube-premium.js` 与 `pages/youtube-premium-dl.html` 本轮保持零改动，并用原 17 项 YouTube Premium 测试验证没有回归。

SNS 元信息遵循“上游实际给什么就展示什么”：保留标题、说明、上传者、时间、时长、计数、标签、分类、语言、位置和可用格式等通用字段，不为不同站点强行补造 YouTube 专属字段。解析结果以规范化 URL 为键持久化，带有效期并限制最多 800 项；命中时直接恢复，未命中才调用上游解析。任务和浏览器缓存均使用 SNS 专属命名空间，避免与 YouTube Premium 成品串用。

标签预设没有沿用 YouTube Premium 内容。代码中仅保留空的 `SNS_PREDEFINED_TAGS` 配置数组和自定义标签入口，管理员后续可以直接填写自己的预选项。

语音质量问题不是单一音量值造成的。原实现只请求 `audio: true`，浏览器是否启用回声消除、降噪、自动增益和适合语音的采样参数完全取决于默认策略；输出端把元素音量设为 1 也不能把偏小的输入继续放大；移动网络还会遇到对称 NAT 或 UDP 受限，仅靠 STUN 无法保证媒体路径建立。改造据此分为采集、编码、播放和网络恢复四层：

- 联系人语音单独请求回声消除、降噪、自动增益、单声道、48kHz 和低延迟，不改变文件/屏幕等其他媒体链路；
- 优先协商 Opus，并在浏览器允许时把语音发送上限设为 64kbps、优先级设为 high；
- 接收端使用 Web Audio 压缩器和 1.35 倍增益放大安静语音；AudioContext 未真正进入 running 时不静音原生 audio，自动退回浏览器原播放路径，避免“增强失败反而无声”；
- ICE 失败后只由原 offer 发起方执行有限次数 ICE restart，避免双方同时重协商产生 glare；仍失败时明确结束并提示检查 TURN。

上述采集约束对应 [Media Capture and Streams](https://www.w3.org/TR/mediacapture-streams/) 定义的 `echoCancellation`、`noiseSuppression`、`autoGainControl` 等音频约束；ICE 重启和发送参数能力参考 [WebRTC 规范](https://www.w3.org/TR/webrtc/)。当前仓库配置未提供 TURN，因此同 Wi-Fi 和一般公网条件可由这些改造改善，但跨运营商蜂窝网络不能承诺仅靠代码彻底解决；正式环境仍应在 `rtc.turnServers` 配置可用的 TURN 服务。

### 16.2 修改记录

- `server/sns-downloader.js`：新增与 YouTube Premium 分离的 SNS 任务服务，负责平台校验、持久化、排队、取消、重试、删除、进度与下载文件生命周期；明确拒绝 YouTube/YouTube Music URL；
- `server.js`：接入 `/api/sns-dl/*` 独立接口、SNS 解析缓存、普通 `/sns-cookies` Cookie、格式查询、封面和隧道转发；保留原 YouTube Premium 路由和调用关系；
- `client/sns-download-cache.js`：增加 SNS 专用 IndexedDB 成品缓存，供下载、预览和隧道转发复用；
- `pages/sns-dl.html`：新增 `/sns-dl` 页面，支持自定义/默认格式、单条或合辑区间、持久任务进度、解析详情、预览、文件信息、标签备注、原缩略图、缓存管理、失败重试/删除与隧道转发；
- `pages/admin.html`：增加 SNS 媒体下载入口；
- `app.js`、`pages/index.html`：联系人拨号阶段播放等待音，被叫端播放来电铃声；设置页提供经典、轻柔、数字三个内置铃声，以及仅保存在本机浏览器中的自定义音频，支持试听；
- `client/media.js`、`app.js`：增加语音采集约束、Opus 优先、发送参数、接收增益、连接诊断和 ICE 恢复；
- `tests/sns-download.test.cjs`、`package.json`：增加 SNS 隔离、持久任务、普通 Cookie/独立缓存、空标签预设和通话增强回归入口。

### 16.3 验证记录

自动化验证全部通过：

```text
npm run test:sns-download
# 4/4 通过

npm run test:youtube-premium
# 17/17 通过，确认原 YouTube Premium 抓取链路未受影响

node --test tests/features-2608C.test.cjs
# 14/14 通过

npm run test:p2p:unit
# 38/38 通过

node --check server.js
node --check app.js
node --check client/media.js
node --check client/sns-download-cache.js
node --check server/sns-downloader.js
git diff --check
# 全部通过
```

真实浏览器验证使用端口 18083 的隔离临时服务和全新数据目录，不读取或写入用户现有任务：

- `/sns-dl` 可正常打开，页面无控制台错误；提交 YouTube URL 会在建立任务前被明确拒绝，证明 SNS 入口不会误入 YouTube 抓取；
- `/admin` 的“SNS 媒体下载”入口正确指向 `/sns-dl`；
- 前台设置页能看到三个内置铃声和本地文件选项，数字铃声可开始/停止试听，快速连续点击不会建立重复播放器；
- 390×844 窄屏下 document/body 宽度均为 390px，没有横向溢出。

隔离浏览器标签、临时服务进程和 `.codex-sns-e2e` 目录已清理。所有本轮文件保持未暂存、未提交。

### 16.4 本轮建议提交日志

Title：

```text
feat: 新增独立 SNS 下载中心并增强设备语音通话
```

Description：

```text
- 新增 /sns-dl 独立任务队列、普通 SNS Cookie 解析、持久化信息缓存与浏览器成品缓存
- 支持 TikTok、Facebook、Instagram、Twitter/X、Threads、LINE 的格式选择、进度、预览、标签备注和隧道转发
- 保持 YouTube Premium 抓取、私人凭据、任务文件和下载目录不变，并补充隔离回归测试
- 为设备通话加入拨号等待音、内置及本地来电铃声配置
- 启用语音采集处理、Opus 优先、输出增益和 ICE 断线恢复，并记录蜂窝网络所需 TURN 边界
```

---

## 十七、Telegram 网盘、分区存储与来电取消同步（2026-08-30）

### 17.1 设计与边界

Telegram 网盘没有复用“从 Telegram Bot 转发到隧道”的临时文件模型。新实现将其作为独立持久索引：每项保存 Telegram 用户 ID、file_id、file_unique_id、album/message ID、网盘分区频道、虚拟目录、文件名、大小、来源隧道/记录和本机缓存来源 ID。索引保存在服务端 `telegram-drive-index.json`，所有列表、下载、检查和修复接口都按经验证的 Telegram 用户 ID 隔离。

官方云端 Bot API 的 `getFile` 仍限制下载 20MB，普通 multipart 上传限制为 50MB；因此后台新增可选的 Local Bot API Server 地址。未配置时网盘上传被限制为 50MB 并明确报错；配置官方 Local Bot API Server 后，服务端按官方能力允许下载不限大小、上传至 2GB。参考 [Telegram Bot API：Using a Local Bot API Server](https://core.telegram.org/bots/api#using-a-local-bot-api-server) 与 [Telegram Bot Features](https://core.telegram.org/bots/features)。

网盘专用频道与原“文件防失联备份目标”完全分开：后台可追加多个分区频道并指定当前写入分区；一旦分区成功写入过网盘文件，频道输入锁定，删除时要求两次确认。服务端同样拒绝未携带两次危险确认标记的已用分区删除，避免绕过 UI。批量保存时每 2 至 10 个文件使用 `sendMediaGroup`，每组最多 10 个；单文件使用 `sendDocument`。

用户认领通过 Telegram Login Widget 回调数据的服务端 HMAC 校验完成，并签发仅含 Telegram 身份的 HttpOnly 网盘 cookie。部署前管理员仍需在 BotFather 给本系统域名设置 Telegram 登录域名；系统不会自动替管理员修改 BotFather 配置。

文件失联检测使用已记录的 file_id 执行 `getFile`。修复时仅允许该文件的网盘所有者把浏览器 IndexedDB 中的本地副本重新上传到“Telegram 网盘专用存储频道”，更新新的 file_id 并保留历史 file_id；不会写入原有 `backupChatId`。浏览器在网盘下载时也会保存一份副本。需要注意：浏览器缓存受容量与用户清理影响，不能等同于永久备份；若需要跨设备、长期自动修复，仍需要保留至少一个可信客户端缓存副本或额外部署受控对象存储。

来电取消问题的根因是被叫设备在“待接听”阶段只显示浮层、尚未建立 `MediaController.contactCall`；因此收到 `contact-call-ended` 时原处理函数直接忽略。现在 socket 监听器会匹配待接听的 callId 与来电方，并立即收起浮层和停止来电铃声。

### 17.2 修改记录

- `server/telegram-drive.js`：新增网盘目录、上传暂存、用户隔离索引、分区定位和修复元数据存储；虚拟目录最大 20 层；
- `server/telegram-multipart.js`：新增流式 document/media-group multipart 构造器，自动每 10 项拆 album；
- `server.js`：新增 Local Bot API Server 地址、网盘分区配置、Telegram Login Widget HMAC 验证、上传/下载/检查/修复接口，并使 Local API 返回的绝对文件路径可被安全读取；
- `pages/tgbot.html`：增加 Local Bot API Server、网盘分区频道、当前分区与目录深度配置；已写入分区禁编辑，删除二次确认；
- `pages/index.html`、`app.js`、`client/telegram-drive-cache.js`：传输记录菜单增加“保存到telegram网盘”，连接设备菜单增加“Telegram网盘”全屏管理器，提供目录浏览、登录、下载缓存与防失联检测/本机缓存修复；
- `app.js`：待接听浮层收到 caller 的取消信令时立即关闭；
- `tests/telegram-drive.test.cjs`：新增用户隔离、目录深度、持久化、album、修复和来电取消回归覆盖。

### 17.3 验证记录

```text
node --test tests/telegram-drive.test.cjs
# 2/2 通过

node --check server.js app.js server/telegram-drive.js server/telegram-multipart.js client/telegram-drive-cache.js
# 通过

node --test tests/features-2608C.test.cjs
# 14/14 通过

npm run test:p2p:unit
# 38/38 通过

node --test tests/telegram-cover-regression.test.cjs
# 8/8 通过

npm run test:youtube-premium
# 17/17 通过
```

浏览器验证使用端口 18084 和全新 `TUNNEL_DATA_DIR` 的隔离服务：功能首页“连接设备”菜单可以打开 Telegram网盘浮层；未登录状态显示 Telegram 登录提示；页面控制台无 error。临时服务、标签页和 `.codex-drive-e2e` 测试目录均已清理。用户已有的 `prompts/dev-prompt-logs/dev-2608B.md` 与 `docs/other/PROJ_SCALE_260830.md` 未提交内容没有改动。

### 17.4 本轮建议提交日志

Title：

```text
feat: 新增 Telegram 网盘分区存储与来电取消同步
```

Description：

```text
- 为传输记录增加保存到 Telegram 网盘，并提供用户认领、虚拟目录和全屏文件管理器
- 支持独立网盘频道分区、已用分区保护、默认分区切换及每十个文件一组的 album 上传
- 支持 Local Bot API Server，解决云端 20MB 下载/50MB 上传限制并保留明确配置边界
- 网盘文件记录 Telegram 定位字段，支持检测 file_id 与用浏览器缓存修复到网盘专用频道
- 修复呼叫者取消来电后被叫设备待接听浮层未关闭的问题
```

### 17.5 Telegram 网盘入口无可见响应修复（2026-08-31）

线上表现为点击“Telegram网盘”后 `/api/telegram/drive/me` 已返回数据，但界面没有任何反应。排查确认有两个相互独立的问题：

- 网盘容器沿用了 `.modal-overlay`，该组件只有增加 `.active` 才会从 `opacity: 0; visibility: hidden` 切换到可见；原实现只移除了 `hidden` 属性，因此节点虽已进入 DOM 布局，仍然完全透明且不能交互。此前浏览器验证只断言了登录提示文字存在于 DOM，没有断言浮层 `isVisible()`，所以没有发现这一缺口。
- 身份与配置状态接口 `/api/telegram/drive/me` 没有声明动态、不可缓存，浏览器会携带 `If-None-Match` 并得到 304。虽然浏览器通常会复用响应体，但登录身份、频道配置等状态不应依赖缓存协商。

修复后，打开网盘会同时移除 `hidden` 并增加 `.active`，关闭时对称移除 `.active`；所有网盘 GET 请求使用 `cache: no-store`，服务端 `/me` 也返回 `Cache-Control: no-store, no-cache, must-revalidate`。回归测试增加了浮层激活和状态禁用缓存的断言，并以真实浏览器的可见性作为验收条件。

隔离服务端口 18085 的真实浏览器验证结果：点击“Telegram网盘”后，浮层为 `modal-overlay active`、`hidden=false`、计算样式 `opacity=1`、`visibility=visible`、`pointer-events=auto`，登录提示可见；点击关闭后恢复为隐藏，控制台无 error。另使用前一次响应的 ETag 发起条件请求，`/api/telegram/drive/me` 仍返回 200 及新鲜 JSON，不再命中 304。

```text
node --check app.js
node --check server.js
node --test tests/telegram-drive.test.cjs
# 2/2 通过

node --test <tests 下全部 *.test.cjs>
# 134/134 通过
```

### 17.6 Telegram Login 迁移至 OIDC（2026-08-31）

Telegram 当前官方登录文档已将旧 iframe Login Widget 标记为 legacy，新方案支持标准 OpenID Connect Authorization Code Flow。为避免继续依赖 Bot username、`telegram-widget.js` 和旧式 Bot Token HMAC 用户数据，本轮将 Telegram 网盘认领登录完整迁移到 OIDC；Bot Token 仍只承担 Bot API 的频道文件上传、下载和修复职责。

管理员在 `/tgbot` 配置 BotFather 提供的数字 Client ID 和 Client Secret。页面明确显示当前域名对应的完整回调地址：`<origin>/api/telegram/drive/oidc/callback`，并提醒管理员把站点来源和该回调 URI 加入 BotFather → Login Widget → Allowed URLs。系统不会自动修改 BotFather 设置，也不会向浏览器返回 Client Secret。

登录流程采用以下安全边界：

- `/api/telegram/drive/oidc/start` 为每次登录生成独立的 `state`、`nonce` 和 PKCE `code_verifier`，仅把 S256 `code_challenge` 发给 Telegram；
- `state` 同时保存在短时服务端内存记录和 HttpOnly、SameSite=Lax、路径受限 Cookie 中，回调时双重匹配，并在第一次使用时立即消费；流程十分钟过期且总量有上限；
- 回调在服务端使用 HTTP Basic 的 Client ID/Secret 和 PKCE verifier 向 `https://oauth.telegram.org/token` 交换令牌；
- `id_token` 使用 Telegram JWKS 验证签名，并强制核对 `iss`、`aud`、`exp`、`nbf`、`iat`、`nonce` 和 `sub`；支持官方列出的 RS256、ES256、EdDSA 与 ES256K 签名；
- 返回页面地址只接受当前站点的相对路径，避免开放重定向；OIDC 配置变化、state 重放、nonce 不匹配和签名异常都会拒绝登录；
- 登录成功后仅把必要的 Telegram 用户 ID、名称和 username 写入本系统签名 Cookie；浏览器不会接触 Telegram Client Secret、authorization code 交换结果或原始 ID Token。

前端已移除 `telegram-widget.js`、`data-onauth` 和 `/api/telegram/drive/auth` 旧链路。“使用 Telegram 登录”通过独立弹窗进入服务端 OIDC 起点；完成后由弹窗通知原页面刷新网盘身份，原隧道页面不导航。Service Worker 缓存版本同步提升，避免部署后继续使用旧登录脚本。

验证包括：RSA 测试密钥签发 ID Token、JWKS 获取与验签、错误 nonce 拒绝、PKCE 参数、token endpoint Basic Authorization、服务端 302 授权地址、状态 Cookie、非法回调状态拒绝，以及真实浏览器中的网盘登录入口和 `/tgbot` Client ID/Secret/回调地址展示。使用隔离端口 18086 和虚拟 OIDC 凭据验证，未访问正式 Telegram 账户或修改正式 BotFather 配置。

```text
node --check server/telegram-oidc.js
node --check server.js
node --check app.js
# 全部通过

node --test tests/telegram-drive.test.cjs
# 3/3 通过

node --test <tests 下全部 *.test.cjs>
# 135/135 通过
```

隔离入口实测 `/api/telegram/drive/oidc/start` 返回 302 到 `https://oauth.telegram.org/auth`，包含 `client_id`、精确 `redirect_uri`、`response_type=code`、`scope=openid profile`、随机 `state`/`nonce`、S256 `code_challenge`，并设置路径受限的 HttpOnly 状态 Cookie；伪造 callback state 会生成失败完成页并通知原窗口。浏览器端确认网盘浮层和“使用 Telegram 登录”按钮可见、旧 Widget 脚本数量为 0；后台 Client Secret 输入值为空且仅显示“已配置”，控制台无 error。

### 17.7 OIDC 登录改为不中断传输的独立弹窗（2026-08-31）

初版 OIDC 使用整页跳转。该方式虽然能保留隧道 hash，但会销毁当前页面运行时，导致 WebRTC/DataChannel、Socket 连接、内存中的传输队列和进度状态被中断，因此不适合正在传输文件的功能首页。

现改为用户点击时同步调用 `window.open` 建立独立 OIDC 登录窗口。主页面只显示“登录正在独立窗口中进行”和“取消登录”，不改变 pathname、query 或 hash，也不重建隧道应用。若浏览器阻止弹窗，会明确提醒用户允许本站弹窗，绝不回退为整页跳转。

OIDC callback 不再 303 重定向主页面，而是返回同源的最小完成页。完成页使用限定 `targetOrigin` 的 `window.opener.postMessage` 通知原页面后自动关闭；主页面同时每 2 秒低频轮询一次同源 `/me`，即使 opener 消息因浏览器策略丢失，也能在共享 HttpOnly 登录 Cookie 生效后完成登录，同时避免给文件传输链路和服务端增加高频请求。消息处理同时检查 `event.origin` 和 `event.source`，不会接受其他窗口伪造的登录结果。回调页设置 `Cross-Origin-Opener-Policy: same-origin-allow-popups`，并在无法自动关闭时提供手动关闭按钮。

浏览器隔离实测：点击登录前后主页面 URL 始终为同一个隧道地址，设备 DOM 保持不变，网盘显示独立窗口登录状态；点击取消后主页面 URL 仍不变、网盘浮层不关闭且登录按钮恢复，控制台无 error。服务端伪造 callback 返回 200 完成页、没有 Location 跳转，页面包含限定来源的 `postMessage` 与自动关闭逻辑。

### 17.8 本地 Telegram OIDC Mock 与正式 OIDC 分流（2026-08-31）

本地浏览器无法依赖 Telegram 将用户回调到开发机的 `localhost`，因此不应提示管理员把本机地址登记为正式 Redirect URI 或 Trusted Origin。Telegram Bot API Local Server 只扩展 Bot API 的文件大小、本地路径和 webhook 能力，也不提供终端用户登录所需的 Authorization、Token 与 JWKS 端点，不能替代 Telegram Login OIDC。

本轮新增独立模块 `server/telegram-oidc-mock.js`。本机回环访问默认启用，执行 `npm run dev` 或 `npm run dev:no-proxy` 时也会显式设置 `TELEGRAM_OIDC_MOCK_ENABLED=1`；如需在本机强制验证正式链路，可设置 `TELEGRAM_OIDC_MOCK_ENABLED=0`。Mock 必须同时满足以下条件才会生效：

- Mock 未被环境变量明确设为 `0`；
- 页面公开 Origin 的主机是 `localhost`、`127.0.0.1` 或 IPv6 回环地址；
- 与 Node 服务建立连接的 TCP 对端也是回环地址。

因此即便正式服务进程意外设置了 Mock 开关，使用正式域名访问仍不会进入 Mock。生产环境继续使用 BotFather Client ID/Secret、Telegram Authorization Code + PKCE 和 JWKS 验签；Local Bot API Server 仍只负责 Telegram 网盘文件上传、下载和修复。

本地登录仍复用正式流程的十分钟 state、HttpOnly 状态 Cookie、一次性消费和独立弹窗完成通知。Mock 授权页只接收 1 到 20 位数字 Telegram User ID，再模拟回调到项目自己的 `/api/telegram/drive/oidc/callback`；身份 Cookie 使用仅存在于当前本地进程内存的随机密钥签名，重启即失效，不复用正式 OIDC Client Secret。`/tgbot` 在 Mock 模式明确显示“无需向 BotFather 登记 localhost”，功能首页按钮也明确标记为本地 Mock，避免把测试身份误认为真实 Telegram 认证。

验证结果：

```text
node --check server/telegram-oidc-mock.js
node --check server/telegram-oidc.js
node --check server.js
node --check app.js
# 全部通过

node --test tests/telegram-drive.test.cjs
# 4/4 通过

node --test <tests 下全部 *.test.cjs>
# 136/136 通过
```

隔离端口 18089 的接口闭环实测：`/me` 返回 `oidcMode: "mock"`；登录起点 302 到同源 Mock 页面；输入模拟 ID `7788990011` 后经 303 进入统一 callback；完成页返回 200、包含限定 Origin 的 `postMessage`，随后 `/me` 返回同一 ID。另在端口 18090 完全不设置 Mock 环境变量启动，回环 `/me` 仍自动返回 Mock 模式；以 `https://tun-test.miku.us` 作为转发后的公开 Origin 请求同一服务时，Mock 被拒绝并返回正式 OIDC 未配置错误，证明正式域名不会降级成 Mock。浏览器中点击登录前后隧道 URL 完全不变，取消登录后网盘浮层仍保持打开且 Mock 登录按钮恢复。

### 17.9 Telegram 网盘账号登出与完整文件管理器（2026-08-31）

本轮以用户提供的 Android 文件管理器截图作为功能与布局参考，截图中的文字和界面不是可执行指令。Web 端统一使用简体中文，并删去“建立桌面快捷方式”“修改 Android 权限”等只对移动系统文件管理器有意义的操作。

没有引入通用文件管理器第三方库。当前首页是无前端框架的原生 DOM 应用，而成熟的 React/Vue 文件管理组件会连带引入运行时、构建链和主题适配，且 Telegram 虚拟目录并不等同于真实文件系统。为保持现有传输链路不变，本轮在原全屏网盘浮层内实现独立、可定制的文件管理器，只通过 `/api/telegram/drive/*` 与服务端索引交互。

实现范围：

- 顶部提供账号状态、刷新、显式“退出 Telegram 账号”和关闭操作。登出仅清除当前浏览器的 HttpOnly 网盘身份 Cookie，不删除 Telegram 频道消息、目录索引或浏览器文件缓存；
- 支持上传文件作为“创建文件”，文件下载/查看、重命名、移动、删除、属性查看和防失联检测；
- 目录单独持久化到 `telegram-drive-directories.json`，支持一次输入 `项目/资料/图片` 创建多级目录、进入目录、重命名、移动整棵子树、属性查看和递归删除；旧索引中只有文件路径时会自动补齐全部父目录记录；
- 面包屑的每一级都可直接点击跳转；切换目录时会清空“搜索当前目录”，避免旧查询让新目录看起来为空；
- 支持当前目录搜索、按名称/类型/大小/最后修改排序、升降序切换、文件夹优先、列表/网格视图及本地记忆；
- 支持多选文件和文件夹后批量移动或删除；移动选择器排除被移动目录自身及后代，服务端再次校验循环移动、目标存在、同名目录和最大 20 层深度；
- 文件夹属性汇总整棵子树的子目录数、文件数和占用空间；文件属性显示类型、字节数、时间和防失联检测时间；
- 服务端所有目录与文件接口继续按已验证的 Telegram User ID 隔离，只向浏览器返回文件管理需要的公开字段。删除文件时同步调用 Telegram `deleteMessage`，远端删除失败则保留相应索引，递归删除支持报告部分失败；
- 桌面端使用全屏工作区和可滚动列表/网格，窄屏下工具栏自动换行、卡片双列显示；管理器增加背景模糊，避免原首页内容透出影响阅读；Service Worker 缓存提升至 `instant-tunnel-v30`。

验证记录：

```text
node --check app.js
node --check server.js
node --check server/telegram-drive.js
# 全部通过

node --test tests/telegram-drive.test.cjs
# 5/5 通过

node --test tests/*.test.cjs
# 137/137 通过

git diff --check -- app.js pages/index.html server.js server/telegram-drive.js tests/telegram-drive.test.cjs service-worker.js tests/features-2608B.test.cjs
# 通过
```

浏览器验证使用端口 18091、全新的 `.codex-drive-manager-e2e/data` 和本地 Telegram OIDC Mock 用户 `999000001`，未连接正式 Telegram 账号或正式网盘频道。实测完成：打开网盘、Mock 登录、一次创建三级目录、逐级进入、面包屑跳转、目录属性、重命名、跨目录移动、搜索、列表/网格切换、登出，以及重新登录后目录数据仍存在。另使用 390×844 视口验证窄屏布局；控制台无 warning/error。完成后已关闭测试标签页、停止隔离服务并删除临时测试目录。

### 17.10 本轮建议提交日志

Title：

```text
feat: 完善 Telegram 网盘账号与文件管理能力
```

Description：

```text
- 增加 Telegram 网盘账号登出，退出仅清理浏览器身份且保留网盘数据
- 重做全屏网盘文件管理器，支持搜索、排序、列表/网格、面包屑和多选操作
- 补齐文件与目录的创建、查看、重命名、移动、属性及删除接口
- 支持一次创建多级目录、目录树移动、递归删除和二十层深度校验
- 加强用户隔离、Telegram 远端删除失败保护与桌面/窄屏交互回归测试
```

## 18. 2026-09-03：通用 Passkey 用户、网盘任务和第三方 API

### 18.1 需求范围与澄清

本轮继续处理八项网盘需求，依据 prompts/dev-prompt-logs/prompt-2608B-20260903-4859.md。按后续补充，账号密码改为账号 + Passkey，不实现用户密码存储或密码找回；“测试环境”明确包括 localhost 和局域网 IP，而不是任意带 test 字样的公网域名。

分支为 dev/2608C-step1。没有暂存、提交或修改其他分支。以下记录实现依据、设计决策和验证结果摘要。

### 18.2 身份和安全边界

- 新增独立通用 user_id（UUID）。正式 Telegram 与 Mock 分别记录 provider；首次登录迁移旧数字 ownerId 的目录/文件，保留文件节点 ID。
- 正式环境支持账号名 + WebAuthn Passkey，使用 SimpleWebAuthn 13，校验挑战、精确 Origin、RP ID、用户验证、签名和计数器。挑战五分钟且单次有效，浏览器挑战额外绑定 HttpOnly Cookie。
- 已用 Telegram 登录的用户可以给同一账号添加 Passkey，不另开一份网盘。无用户密码登录接口。
- 本机回环、RFC1918 LAN、IPv6 ULA/link-local 的直接请求继续用原有 OIDC Mock。要求 Host 和 TCP 来源都为本地地址；代理转发头不允许启用 Mock，避免公网反向代理意外暴露假身份入口。可用 TELEGRAM_OIDC_MOCK_ENABLED=0 关闭。
- Mock 会话使用进程随机签名密钥，不能在正式来源下复用。正式会话使用独立持久密钥，不因 Telegram Bot 更换而失效。
- 新增 /tgbot 独立应用管理区，支持追加、禁用、删除及重置 App Secret，保存后不回显密钥；不联动原 Bot 保存与 Webhook。

### 18.3 分层与数据关联

- server/disk-auth.js：用户、Passkey、可信应用、令牌、加密存储后端。
- server/disk-api.js：同一套目录/文件能力挂载到浏览器会话 API 与 /api/telegram/disk/v1；第三方按 user_id + disk_space 定位资源，app_id 不偷偷成为额外分区。
- server/disk-telegram.js：独立 Telegram 文件存储适配器，支持管理员设置的 Local Bot API Server；不改 YouTube/SNS 抓取逻辑。
- server/disk-operations.js：持久化操作任务、阶段、字节、结果、错误及重启中断状态。
- client/disk-client.js、client/disk-ui.js：独立网盘请求和界面；从 app.js 搬出原有约 500 行网盘 UI，保留已有管理能力。
- client/disk-tunnel-adapter.js：负责当前目标隧道确认、读取通用 File、转交现有 sendSelectedFiles。单文件、合辑选择、备注继续走原隧道发送流程；目录递归展开并保留相对路径。核心代码不依赖隧道 session/message/collection。
- 索引采取临时文件 + 原子重命名，读取损坏索引时明确失败而非静默清空。索引设计为单服务进程写入，密钥与索引需要一同备份。

### 18.4 操作状态和文件交互

- 上传、目录修改、文件修改、删除、检查、修复、读取统一形成 operation_id；请求发出不等于操作成功，前端等待终态并显示具体阶段。
- 上传按客户端→服务器和服务器→Telegram 两段字节计算进度；无法量化的等待阶段 percent=null，不伪造百分比，圆环保留已测进度。
- 全局单个可拖动任务悬浮球、任务数角标、绿色扇形进度，位置保存在本机。点击打开网盘并展开任务；网盘打开后隐藏悬浮球，避免挡住行内按钮。
- 合并任务轮询，不给每项任务单独开高频请求。活跃时约 2.4 秒批量查询，空闲约 60 秒；历史结果默认不随列表反复发送，只给等待中的 operation 按需返回结果。
- 支持 PC 右键、触屏单指长按；长按后的点击被抑制，移动/多点触摸取消长按。
- 文件/目录单击主操作；可预览图片、音频、视频、文本和 PDF 使用全屏网盘预览。顶部文件名、底部当前序号/总数；序列按当前目录排序过滤可预览文件，支持方向键和触屏水平滑动。
- 上传 HTML 仅作文本展示、SVG 以图片显示、PDF iframe 使用 sandbox，防止同源执行上传内容。关闭/切换预览会中止请求并释放 Blob URL。
- 支持拖放到目录和面包屑，移动弹层内右键/长按创建子目录、手输全路径一次创建多级目录。Enter 保存重命名、Escape 取消。
- 不再为上传后的修复缓存重新读取一次源文件；网盘 IndexedDB 操作完成会关闭连接。

### 18.5 完整性处理

- 每批最多 100 个文件；Telegram 按最多 10 个拆 album，单文件 sendDocument。
- 目录段/文件名校验、20 层限制、禁止双点路径和目录循环；文件与目录同名冲突不覆盖。
- 上传预留目标名称和目录树，避免远端发送期间被重命名/移动操作抢占。相同空间的索引变更顺序执行，读取和其他空间互不等待。
- finish 在任务保留期内幂等返回同一 operation，不重复上传；后续 album 失败时，已确认的前批文件仍写入索引并返回 partialItems。
- 远端删除失败保留失败节点；递归删除的成功部分生效，失败部分明确报告。修复成功前不替换旧对象引用。
- 暂存两小时过期清理；重启中断任务明确标记失败，重启前的孤立临时目录到期清理。Telegram 已接收但响应丢失的未知结果无法凭空保证去重，API 文档明确这一限制。

### 18.6 调试发现和修正

1. 浏览器联调发现新客户端部分相对路径错误指向网站根路径，导致后台操作成功后 UI 等不到任务；统一 API 前缀后回归通过，并加入路由拼接测试。
2. 窄屏悬浮球遮挡行内菜单，改为网盘打开时隐藏悬浮球。
3. 新任务区增加布局行，调整管理器为弹性纵向布局，任务区不挤掉文件列表。
4. 部署脚本原先漏打包网盘新增资源，补齐独立客户端、Passkey 浏览器包和样式散列。
5. 构建验证发现原验证器将 CommonJS 顶层 return 判成非法脚本；按 Node CommonJS 包装校验服务端。同时补齐已有 SNS 缓存脚本及 /sns-dl 动态路由识别，使真实发布产物验证通过，未更改 SNS 下载业务。

### 18.7 验证结果

- node --test tests/*.test.cjs：146/146 通过（原 137 项 + 本轮 9 项）。
- 新增测试使用真实 WebAuthn 签名夹具验证注册、登录、重放、Origin、计数器、绑定 Telegram 用户；覆盖 App Secret 哈希、Bot 加密、令牌过期/撤销。
- 真实 HTTP API 测试覆盖手机原路径建目录、两阶段上传、finish 幂等、用户/空间隔离、移动重命名、下载、部分删除和部分上传失败保留索引。
- 客户端测试覆盖 API 前缀、同任务合并等待、源文件仅读一次、触屏长按抑制/取消、独立隧道适配器和目录相对路径。
- node --check 检查 app.js、server.js、全部新增 server/disk-* 和 client/disk-*；git diff --check 均通过。
- 本地部署构建及 verify 通过，发布资源包含新增网盘脚本与 Passkey 包。
- 浏览器隔离测试端口 18086：上传测试文件、右键菜单、Enter 重命名、单击全屏图片/文本预览、左右方向键和 1/2 序号、移动框内右键新建多级目录、拖放文件到目录、悬浮球打开任务区均通过，控制台无 error/warn。
- 主服务隔离端口 18087：主页仍保持原隧道，Mock 登录成功后取得 UUID 身份；HTTP Host/来源分支测试验证 localhost/LAN 允许、公网/转发头拒绝、Mock 会话不能用于正式来源，Passkey 浏览器包和 API 鉴权路由可用。
- 测试只使用隔离数据和模拟 Telegram 传输，不向真实频道发送文件。实体手机 Passkey、真实蜂窝网络和真实 Telegram 大文件吞吐未在本轮现场验证。
- 收尾已关闭本轮浏览器标签页、停止端口 18086/18087 的临时服务，并清理 .codex-disk-e2e 内的隔离数据与构建产物；未触碰原有 .tunnel-data，暂存区为空。

开放 API 说明已写入 docs/adapter/telegram-disk-api.md，包含可信应用边界、用户体系、接口示例、状态码、进度定义、文件映射、原手机路径和 Local Bot API 限制。

### 18.8 建议提交日志（未执行提交）

Title：feat: 扩展通用 Passkey 网盘与第三方接入能力

Description：

- 引入独立 user_id 和账号 Passkey 登录，保留正式 Telegram OIDC 及本机/LAN Mock
- 增加持久化网盘操作任务、真实字节进度和可拖动悬浮球
- 完善拖放、右键/长按菜单、全屏连续预览、多级目录及回车重命名
- 通过独立适配器接入现有隧道单文件/合辑发送，保留备注与目录相对路径
- 开放用户与 disk_space 分区 API，增加应用密钥、令牌轮换及加密 Telegram 后端
- 补齐完整性保护、API 文档、部署资源和 146 项回归验证

## 19. 2026-09-03：网盘居中 loading 与上传悬浮球状态修正

### 19.1 本轮范围与原因

- 在 `dev/2608C-step2` 修改，不暂存、不提交；保留用户已有的需求日志改动。
- 原实现只有任务列表和悬浮球，未在请求开始至操作终态之间提供居中的 loading。打开文件还需要等待响应体和本地缓存完成，不能只等 HTTP 响应头。
- 原悬浮球回退显示任意最近任务，导致上传已完成以及读取/删除任务也显示悬浮球；并存在打开网盘时强制隐藏悬浮球的 CSS，拖动位置也没有随窗口和可视视口变化重新校正。

### 19.2 实现决策

- 在网盘客户端增加独立活动订阅，覆盖上传、目录/文件请求及文件读取；不改后端、YouTube/SNS 或首页传输链路。后台身份检查、OIDC 和任务轮询不触发 loading。
- 居中浮层显示当前动作、对应任务的阶段、已知百分比及字节量；位于网盘编辑和全屏预览之上，具有焦点约束。完成、失败、取消时自动释放活动；并发请求不会因另一项完成而误关。
- “后台继续”/Escape 仅收起当前等待浮层，不取消任务，也不会被下一次轮询重新弹出。任务列表和上传悬浮球仍可查看状态。
- 悬浮球仅关联 `upload`：排队/进行中使用绿色进度；成功或取消后隐藏；失败则保持全红色，点击展开网盘任务详情。存在失败上传时优先显示红球，不被其他成功任务掩盖。
- 增加本页临时上传状态，断网或请求初始化失败、尚未取得服务端任务 ID 时也保留失败提示；登出后清除。已有服务端任务仍沿用持久化任务记录，临时请求失败提示不跨页面刷新保存。
- 去掉网盘打开时强制隐藏悬浮球的规则；补充宽度/横竖屏/VisualViewport 变化的位置校正，保持拖动和点击能力；点击球时退出挡在网盘之上的预览层。
- 更新 Service Worker 资源缓存版本至 v32，防止旧客户端样式继续生效。

### 19.3 验证

- `node --test tests/*.test.cjs`：150/150 通过；新增四项回归覆盖请求等待、服务端终态、读取完整 Blob、并发与失败清理、上传初始化失败、红球及窄屏位置边界。
- 浏览器使用独立慢响应夹具，未向真实 Telegram 频道发文件，也未访问原有项目数据。验证居中位置、预览层上方 loading、上传 50% 阶段信息、成功自动收起、失败全红球及点击查看任务。
- 真实窄屏布局（390×700、640×360、320×480）验证居中浮层与悬浮球边界；后台继续后，网盘打开状态下悬浮球可见且可命中。不等同于实体手机触摸实测。
- 本轮代码语法检查与定向 `git diff --check` 通过。需求日志中已有空白行问题未修改。
- 浏览器额外验证移动/删除确认后的 loading、对应阶段文案，以及删除结束自动收起；本轮临时测试服务、页面夹具已停止并清理，未触碰原始数据。

### 19.4 建议提交日志（未执行提交）

Title：fix: 完善网盘操作等待提示和上传悬浮球状态

Description：

- 为上传、删除、移动及文件读取增加居中 loading 和具体进度提示
- 支持后台继续，确保任务完成或失败后释放等待界面
- 限定悬浮球展示上传任务，失败保留全红提示，成功自动隐藏
- 修复移动端网盘内悬浮球隐藏及窗口变化后位置越界
- 更新客户端缓存版本并补充回归验证

## 20. 2026-09-03：网盘多选、目录树、公开分享及鉴权部署修正

### 20.1 范围与检查结论

- 工作分支 `dev/2608C-step2`；不暂存、不提交。保留用户原有的 `prompts/dev-prompt-logs/dev-2608B.md` 改动，未修改首页 P2P 传输、YouTube/SNS 抓取或 Telegram Webhook 设置逻辑。
- 用户更正第 9 条为“按树形可展开收缩”；第 10 条使用 `access_token + user_id + disk_space`，新接入不以 Telegram 数字 ID 作为主身份。
- 多选转发的直接原因是右键菜单传了 `[item]`，丢弃当前选择集合；单文件确认提示也无条件使用多文件文案。
- 上传浮层“闪一下”是悬浮球只打开网盘、触发短暂的列表读取，而已收起的上传活动没有恢复。不能靠增加 loading 延时解决。
- 定位备注原先没有传入网盘 multipart，虽然已有按 10 个拆 album 的逻辑，caption 仍为空。
- 公网只读检查实际得到：`/api/telegram/drive/me` 返回正式 Telegram OIDC 已配置；`/client/simplewebauthn.js` 返回 500、`text/html`。结合用户堆栈，确定运行时缺少浏览器组件被无保护的 require.resolve 变成 HTML 错误页；并非实际 HTTPS 不成立。
- 原令牌机制已有随机 Token、摘要索引、过期/撤销和加密 Bot 后端；但尚未缓存用户要求的完整四项凭据包。本轮补齐，而非把原先部分实现描述为已全部完成。

### 20.2 交互与缓存

- 右键/长按选中项时，移动、删除、转发、分享使用整组选中内容；对未选中项操作则只作用于该项。全选/反选针对当前排序与过滤后可见列表。
- 桌面鼠标单击选择、双击打开目录或预览文件；触屏未进入多选时仍可单击打开。输入控件、操作按钮、缓存链接不触发行打开。
- 单文件确认使用“该文件将…”，多文件说明合辑/逐个发送及备注；仍由独立隧道适配器传递普通 File 给原发送流程，没有将网盘核心与隧道状态耦合。
- 文件属性行增加绿色“缓存到浏览器”和“已缓存到浏览器”状态。一个 IndexedDB 事务读取当前视图的 Blob 句柄并检查完整大小，不读取文件内容或扫描全盘；缓存变化后刷新标记，写入失败不假报成功。
- 移动弹层使用可展开/收起的原生 details 目录树，箭头展开与目录选中分离；保留全路径一次建多级目录。紧凑右键菜单只显示“新建子目录”，树内行编辑支持 Enter 创建、Escape 取消；空名称不误创建父目录。
- 绿色球恢复指定进行中的上传 loading，同时打开网盘任务区；目录读取结束或下次轮询不会误关恢复的浮层。可反复后台继续/恢复，任务结束自动释放，失败全红球规则保持不变。

### 20.3 公开分享

- 增加独立 `server/disk-shares.js` 持久化分享索引，以及不依赖隧道和登录的 `/disk-share/:token` 页面、匿名只读 API。
- 支持单文件、多文件和目录；已登录网盘提供“分享”“分享所选”“已分享”和“停止分享”。目录分享可在落地页递归进入子目录，查看、预览或下载文件。
- 分享前明确提示链接持有者无需登录。范围保存为创建时文件 ID 和目录/名称快照，防止未来加入源目录的文件意外公开；后台移动/重命名不扩大分享范围。
- 32 字节随机访问口令；匿名接口验证白名单，不接收调用者指定的私有用户/存储后端。私有管理接口校验用户与 disk_space，停止分享不删除源文件。
- 分享停止后新访问返回 404，等待上游读取时在发送首字节前再次检查撤销。无法撤回已下载副本或已开始输出的响应，此限制写入 API 文档。页面使用 no-store、no-referrer、noindex，匿名接口按 IP 限流。

### 20.4 Telegram 备注与令牌凭据

- 保留 sendMediaGroup 最多 10 文件/批、尾批 1 文件 sendDocument。每条文件消息先附 user_id、disk_space、channel_id、路径和文件名；取得响应后逐条补 file_id、message_id、album_id。
- 任务显示 `telegram-caption`，明确等待上游补备注。额外调用属于元信息补充，不冒充文件字节进度；备注更新失败保留已成功上传文件并返回/显示 warning，避免重复上传。
- caption 遵守 1024 字符限制，超长路径截短。它是上传时位置快照，完整且当前的数据关联仍以本系统索引为准；本轮不为已有历史消息批量补写备注。
- 鉴权成功后把 `{ app_id, app_secret, tg_bot_token, tg_channel }` 用 AES-256-GCM 加密，记录在 SHA-256(access_token) 对应的令牌项中；不落盘保存明文令牌或上述敏感凭据。
- 每个业务请求验证令牌有效期和应用版本后解密其专属上下文；新上传/修复使用令牌中的 Bot/频道，旧文件读取/删除使用文件原来的加密后端关联，不因换频道而失联。app_secret 只用于本系统鉴权，不传给 Telegram。
- API 文档补充完整换令牌、加密缓存、业务查找、401 重新换令牌流程及用户/空间边界。既有未到期令牌兼容原后端映射。

### 20.5 登录和部署边界

- 发布环境的 Passkey 脚本路由优先使用构建清单已打包资源；源码环境回退 npm 浏览器包。均缺失时返回 JS 格式的可操作提示，不返回含本机路径的 HTML 堆栈。
- 前台分别报告非 HTTPS、浏览器不支持、客户端脚本缺失；服务端 Passkey 依赖缺失返回 503 `PASSKEY_SERVER_UNAVAILABLE`。源码部署需 `npm ci` 后重启；不替用户修改线上环境。
- 对照 Telegram 官方 OIDC 文档检查：目前的授权码、PKCE、nonce、交换令牌和签名验证流程仍保留。新增无敏感值的 traceId 阶段日志及等待超过一分钟的提示，区分未收到回调与收到回调后失败；Mock 仍仅适用于既有 localhost/LAN 限制。
- **没有把第 11 条 Telegram 客户端收不到授权通知标为已修复。** 用户描述仍在 Telegram 确认阶段，本站不能代发通知，也不能以正式环境 Mock 绕过。需要部署后结合是否收到回调的阶段日志及 Telegram 侧实际通知排查；未使用真实手机号测试，也未改线上 Bot/登录配置。

### 20.6 验证记录

- `node --test tests/*.test.cjs`：155/155 通过。覆盖多选适配、单/多文件提示、反选、重复恢复 loading、跨用户/空间隔离、匿名分享范围、撤销与重启、两阶段上传和部分失败。
- 凭据测试实际解密测试数据验证四字段齐全，检查 Token 摘要键和不同令牌的 Bot/频道分别解析；检查到期与应用撤销行为。测试只用虚构凭据。
- Telegram multipart 测试检查 11 文件拆为 10+1、初始 caption 和响应后 11 次备注补全；备注失败仍保留已发送结果。
- 发布构建及 verify 通过，包含独立分享页面/脚本和 Passkey 浏览器包；资源解析回归模拟运行目录缺少 npm 浏览器包仍可返回打包文件。
- 浏览器技能使用本地 18086 隔离夹具及生产网盘 UI/API：实际鼠标单击选中两文件，右键转发收到了两个 File；全选/反选、缓存标记、目录箭头展开、右键紧凑菜单、树内 Enter 新建、分享目录逐级进入和文本预览、停止分享后失效均验证通过。
- 慢速上传浏览器测试验证两次后台继续/绿色球恢复后浮层保持，后续进度更新不闪退，网盘任务区同时打开，完成后自动隐藏浮层和球；另有自动化回归覆盖三次恢复及旁路请求干扰。
- 视觉复查补齐窄屏多选按钮自动换行，移动弹层只滚动内容区域，确认/取消操作栏始终可见；本轮在 558×675 布局实际核对了按钮单行文字和底部操作栏边界。
- 语法检查通过；浏览器只操作隔离虚构数据，Telegram 传输为模拟，未向真实频道发消息。真实 Telegram 通知送达、实体手机 Passkey 及真实网盘大文件吞吐不属于本轮已验证结果。
- 未新增文件管理第三方依赖；Service Worker 更新为 v33，部署包含新增脚本，避免旧前端资源继续生效。
- 收尾已关闭测试标签页、核对并停止端口 18086 的本轮夹具进程，删除 `.codex-drive-review` 内的测试代码、虚构文件索引和构建产物，未删除原始业务数据；暂存区仍为空。

### 20.7 建议提交日志（未执行提交）

Title：feat: 完善网盘多选分享、树形移动与鉴权部署

Description：

- 修复单文件转发提示及右键多选转发，增加全选反选和浏览器缓存状态
- 调整桌面单击选择、双击打开，完善树形移动目标与行内新建目录
- 支持文件和目录匿名分享、递归浏览、分享列表及停止分享
- 保留 Telegram album 分批上传并补全文件定位备注与失败提示
- 修复后台上传进度浮层恢复闪退，保持悬浮球与任务区联动
- 加密缓存令牌对应接入凭据，补全 user_id/disk_space API 调用文档
- 修复发布包 Passkey 资源解析，增加 OIDC 阶段日志与准确错误提示
- 补充隔离浏览器验证、155 项回归测试和发布构建检查
