# dev-2607B 开发记录

## 2026-07-16：选择性恢复 260716_0239 的前端与管理页需求

### 恢复范围

- 以 `77962c88d4bf2c78597883af0f3e553398bf008c` 为基线，对照错误代码备份 `27745c26a230c16c715249c3ae61d267b8816b0b`，仅恢复需求 1～6。
- 未移植错误版本中的 P2P、ICE、Socket.IO relay、文件资产请求、恢复队列、重试、超时与服务端传输调度改动。
- `client/file-assets.js`、`server/file-assets.js` 和 `server.js` 的传输链路保持当前 HEAD 逻辑。

### 截图粘贴传输

- 在隧道页面的非输入区域粘贴剪贴板图片时，将图片转换为带时间戳文件名的文件并进入现有文件发送流程。
- 输入框、富文本编辑器及其他可编辑区域继续保留原生粘贴行为，避免影响文字和富文本图片编辑。
- 发送前复用隧道的文件发送权限检查，并记录失败原因。

### 移动端页面缩放限制

- viewport 增加最大缩放、禁止用户缩放及安全区适配配置。
- 对移动端 Safari 的 gesture 事件和多点 touchmove 增加阻止逻辑，避免双指捏合破坏三栏页面布局。
- 桌面端不启用额外的触摸缩放拦截。

### 多文件拖放与文件夹拦截

- 拖放入口完整收集多个文件，并统一进入现有的“合辑或拆分发送”选择流程。
- 同时兼容 `getAsFileSystemHandle()` 与 `webkitGetAsEntry()`，识别并忽略拖入的目录。
- 文件和目录混合拖入时继续处理合法文件，并在发送选择层或 toast 中提示被忽略的目录数量。
- 拆分发送时单个文件失败不再中断其余文件处理，失败项写入历史日志并汇总提示。

### 传输进度抽屉 UI

- 抽屉状态扩展为展开、收缩、隐藏三态，并持久化用户选择。
- 收缩态和展开态均提供“隐藏传输进度”按钮；隐藏操作仅改变 UI，不修改任务或传输状态。
- 展开态保留点击标题收缩、点击进度项定位传输记录锚点的原有交互。
- 进行中任务数大于 0 时，在顶栏设置按钮右侧显示跳动数字入口；进行中任务归零后隐藏入口。
- 点击顶栏数字入口可重新展开已隐藏的抽屉；没有任何任务时抽屉保持隐藏。

### Admin 隧道短码

- Admin 隧道列表读取 `/api/sessions` 已返回的 `shortCode`，校验后以大写五位短码显示。
- 缺少或格式无效的短码显示为 `—`，不影响原有会话 ID、状态和操作按钮。

### 多语言补充

- 补充文件夹拖放提示及传输进度隐藏/查看入口的多语言映射。

## 2026-07-16：移动端双指手势回归修复

- 将移动端页面根节点的 `touch-action` 从仍允许浏览器参与缩放的 `manipulation` 调整为仅允许纵向原生滚动的 `pan-y`，并关闭页面级 overscroll。
- 三栏滑动手势增加多触点隔离：第二根手指落下时立即取消并复位正在进行的横向位移，在所有触点离开前不再接受三栏拖动，避免双指捏合期间页面左右移动闪烁。
- 修改仅涉及移动端触摸 UI 状态，不改变传输链路、任务调度或进度状态逻辑。

## 2026-07-16：多文件拖放 Chromium 异常修复

- 修复多文件拖放时逐项等待 `getAsFileSystemHandle()`，导致后续条目脱离 `drop` 事件有效时机、进而可能触发 Chromium `RESULT_CODE_KILLED_BAD_MESSAGE` 的问题。
- 所有必要的文件系统句柄请求现在都在 `drop` 回调的同一个同步阶段发起，再并行等待结果。
- 小于外部文件句柄绑定阈值的普通文件不再调用实验性的句柄 API，继续直接使用兼容性更好的 `File` 对象；大文件仍可保留外部句柄能力。
- 文件夹识别和拦截、多文件合辑/拆分选择流程保持不变，未修改传输链路逻辑。

## 2026-07-16：传输抽屉可见性回归修复

- 重新按 HEAD 审计传输相关差异，确认 `client/file-assets.js`、`server/file-assets.js`、`server.js` 以及 `app.js` 内的 WebRTC、ICE、P2P、relay、文件拉取、重试和 watchdog 逻辑均未修改。
- 移除未在需求中约定的抽屉隐藏状态持久化，避免用户曾经隐藏抽屉后，同源下的新隧道和后续任务继续继承隐藏状态。
- 顶栏任务入口改为只要存在进行中、等待中、建链中或停滞任务就显示；有进度增长时数字表示进行中数量并保持跳动，没有增长时显示总任务数但不跳动。
- 当前任务批次完全清空后，隐藏状态自动复位为收缩状态，使下一批新任务能够正常弹出抽屉。
- 本次仅修复传输状态的可见性，不修改任务产生、调度、链路选择或数据传输行为。

## 2026-07-16：独立 LAN-only WebRTC 文件链路

- 新增仅用于文件传输的独立 LAN WebRTC 连接，使用空 `iceServers` 收集本机 host 候选，避免公网 STUN 候选先被选中而绕行公网地址。
- LAN 连接使用独立的 `lan-connect-request`、`lan-offer`、`lan-answer`、`lan-ice-candidate` 信令类型和独立信令队列，不与原有 WebRTC 的 SDP、ICE candidate 或连接状态混用。
- 设备发现后优先预建 LAN 连接；文件传输发起时如果尚未预建成功，会再按需尝试一次。LAN 探测通道在 1.5 秒内打开时，文件 DataChannel 直接复用该连接。
- LAN 建链失败、超时、连接关闭、对端为旧版本或服务端不支持 LAN 信令时，关闭本次 LAN 尝试并回到原有 STUN WebRTC 链路；原链路后续仍可按既有逻辑降级到 Socket.IO relay。
- LAN 失败后为对应设备设置 30 秒冷却，避免不可用网络环境下每个文件都重复创建连接；冷却期间直接使用原有链路。
- 增加 LAN 连接状态和选中 candidate pair 诊断日志，便于确认同一 Wi-Fi 下是否选中 `host` 直连，以及区分 LAN 超时和原链路降级。
- 保持 `client/file-assets.js`、文件分片、并发调度、重试、进度条和 Socket.IO relay 实现不变，本次只在其 `connectPeer/getPeer/ensurePeerOffer` 依赖入口前增加 LAN 优先选择。
